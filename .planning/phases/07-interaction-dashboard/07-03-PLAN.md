---
phase: 07-interaction-dashboard
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/interview/reports/route.ts
  - app/reports/page.tsx
autonomous: true
requirements: [REQ-16]

must_haves:
  truths:
    - "A signed-in student sees their own interview reports newest-first at /reports"
    - "Each row shows type, interviewer, date, status and scores, and clicks through to the full report"
    - "FAILED reports stay listed so their retry stays reachable"
    - "IN_PROGRESS (abandoned) sessions never appear, and are never sent to the browser"
    - "Another user's reports are never visible"
  artifacts:
    - path: "app/api/interview/reports/route.ts"
      provides: "owner-scoped list of the caller's non-IN_PROGRESS reports"
      exports: ["GET"]
    - path: "app/reports/page.tsx"
      provides: "the My Reports list UI"
      min_lines: 60
  key_links:
    - from: "app/api/interview/reports/route.ts"
      to: "prisma.interviewReport"
      via: "findMany scoped to the authenticated user id"
      pattern: "prisma\\.interviewReport\\.findMany"
    - from: "app/api/interview/reports/route.ts"
      to: "lib/interview/report-dto.ts"
      via: "rows.map(toInterviewReportDTO)"
      pattern: "toInterviewReportDTO"
    - from: "app/reports/page.tsx"
      to: "/api/interview/reports"
      via: "fetch on mount"
      pattern: "fetch\\(.*api/interview/reports"
    - from: "app/reports/page.tsx"
      to: "/interview/{typeSlug}/report/{id}"
      via: "row click navigation"
      pattern: "/report/"
---

<objective>
Build `/reports` — a list of the signed-in student's interview reports — plus the
owner-scoped list endpoint behind it.

Purpose: The student sidebar gains a "My Reports" entry in plan 07-06, and today a finished
interview's report URL is the only handle to it — nothing lists it. Without this, a student
who closes the tab loses the report permanently.

Output: `GET /api/interview/reports` and the `/reports` page. This is pulled forward from
Phase 9, which consequently becomes filtering/grouping polish.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-interaction-dashboard/07-CONTEXT.md

# The DTO to reuse verbatim — do not write a second mapping
@lib/interview/report-dto.ts

# The auth + ownership pattern to copy
@app/api/interview/report/[reportId]/route.ts

# The report page this list links into (for its route shape and score presentation)
@app/interview/[type]/report/[reportId]/page.tsx
</context>

<constraints>
- **Reuse `toInterviewReportDTO()` from `lib/interview/report-dto.ts` verbatim.** Do NOT
  hand-roll a second scores/status mapping — that is how visual/vocal would drift away from
  "always null". Never spread a Prisma row into a response; the DTO's explicit field-by-field
  mapping is what keeps the S3 transcript pointer and resume snapshot out of the browser.
- **Copy the owner-scoped auth pattern exactly** from
  `app/api/interview/session/start/route.ts` / `app/api/interview/report/[reportId]/route.ts`:
  read the cookie named `siteConfig.auth.cookie.name` → `getCurrentUser(token)` → 401 if
  absent; scope every query by `userId: currentUser.id`; **404 never 403**; respond with
  `Cache-Control: no-store`.
- **Filter IN_PROGRESS server-side**, in the `where` clause. "Hidden entirely" means the
  browser is never told those rows exist. Do not fetch-then-filter client-side.
- **FAILED reports MUST remain listed and clickable** — retry lives on the report page, and
  keeping a failed evaluation reachable is the entire reason it is not discarded.
- No middleware change is needed for the API: `STUDENT_ROUTES` already matches the
  `/api/interview` prefix. The **page** route `/reports` gets its `STUDENT_ROUTES` entry in
  plan 07-06 — **do not edit `middleware.ts` here**, that plan owns the file.
- No database migration. Do not run `npm run setup`. Do not edit `.env`/`.env.local`.
  Dev server needs the inline local URL:
  `DATABASE_URL="postgresql://ajabreu79@localhost:5432/leadership_avatar_dev" npm run dev`.
  A missing-table error means STOP, not migrate.
- `npx tsc --noEmit` is authoritative and MUST report ZERO errors — it is clean on the
  current tree. If `.next/types/validator.ts` errors appear after a route deletion, run
  `rm -rf .next` and re-run — do NOT ignore them. ESLint is broken repo-wide; `next build` is
  known-broken on `/about` — do not gate on either.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Owner-scoped reports list endpoint</name>
  <files>app/api/interview/reports/route.ts</files>
  <action>
Create `app/api/interview/reports/route.ts` (plural, no `[reportId]` segment) exporting a
`GET` handler.

Auth: copy the cookie → `getCurrentUser` → 401 sequence from
`app/api/interview/report/[reportId]/route.ts` verbatim.

Query (the `@@index([userId, createdAt])` on `InterviewReport` already covers this exactly):

```ts
const rows = await prisma.interviewReport.findMany({
  where: { userId: currentUser.id, status: { not: "IN_PROGRESS" } },
  orderBy: { createdAt: "desc" },
});
```

Respond `{ reports: rows.map(toInterviewReportDTO) }` with `Cache-Control: no-store`, using
the same response construction the single-report route uses.

Note `reportMarkdown` rides along in the DTO and will be sent for every row. That is
acceptable and keeps the single-source-of-truth mapping intact — do NOT strip fields by
building a second DTO shape. If you judge the payload genuinely too large, say so in the
SUMMARY rather than forking the mapping.

Two `IN_PROGRESS` rows already exist in the local dev database from Phase 6 testing — they
are your fixture for proving the filter works.
  </action>
  <verify>
1. `npx tsc --noEmit` — MUST report ZERO errors. It is clean on the current tree. If
   `.next/types/validator.ts` errors appear after a route deletion, run `rm -rf .next` and
   re-run — do NOT ignore them.
2. Start the dev server with the inline local `DATABASE_URL` and call the endpoint with a
   real logged-in student's cookie: expect 200, a `reports` array ordered newest-first, and
   **zero** entries with `status: "IN_PROGRESS"` (confirm against the known local rows).
3. Call it with no cookie: expect 401.
4. `grep -n "toInterviewReportDTO" app/api/interview/reports/route.ts` matches.
5. `grep -n "IN_PROGRESS" app/api/interview/reports/route.ts` shows the filter is in the
   `where` clause, not in a `.filter()` after the query.
  </verify>
  <done>`GET /api/interview/reports` returns only the caller's own non-IN_PROGRESS reports, newest-first, as `InterviewReportDTO[]`, 401 without a cookie, `no-store`.</done>
</task>

<task type="auto">
  <name>Task 2: The /reports page</name>
  <files>app/reports/page.tsx</files>
  <action>
Create `app/reports/page.tsx` as a client component ("use client") titled "My Reports".

Fetch `/api/interview/reports` on mount with `credentials: "include"` (match how the existing
report page fetches). Handle three states: loading skeleton, empty state, and the list.

Empty state copy should point the student at the dashboard — something like "You haven't
finished a practice session yet." with a link to `/`. Honest and non-blaming; mirror the tone
of the Phase 6 "Not yet measured" cards.

Each row shows, per REQ-16:
- interaction/interview type — derive the display label from `typeSlug` (a small local map or
  a title-cased fallback is fine; this page may NOT import `lib/interactions`, which carries
  no interview-variant data, and pulling in `lib/interview` internals here is unnecessary —
  keep it local).
- `interviewerName` (may be null — render a neutral dash, never "undefined")
- date from `completedAt ?? startedAt`, formatted readably
- status chip: READY / PENDING / FAILED
- scores: content and behavioral. Visual and vocal are always null by design — either omit
  them from the row or render them with the established "Not yet measured" treatment; do NOT
  render `null` as `0` or as a blank that reads like a zero.

Every row is clickable through to `/interview/{typeSlug}/report/{id}` — the whole row, not a
tiny link. **FAILED rows are clickable too**, since retry lives on the report page.

Layout: reuse the existing card/palette language rather than inventing one. The page wrapper
should carry the same padding the deleted `app/student-cases/layout.tsx` provided
(`py-8 md:py-10 px-4 md:px-6 lg:px-8` inside a `max-w-7xl mx-auto`) since there is no shared
student layout. Whether rows are HeroUI `<Card>`s or a table is your discretion; a card/row
list reads better on mobile.
  </action>
  <verify>
1. `npx tsc --noEmit` — MUST report ZERO errors. It is clean on the current tree. If
   `.next/types/validator.ts` errors appear after a route deletion, run `rm -rf .next` and
   re-run — do NOT ignore them.
2. Dev server with inline local `DATABASE_URL`: visit `/reports` as a logged-in student with
   at least one READY report. Confirm the report is listed, the two known local IN_PROGRESS
   sessions are absent, and clicking a row lands on that report's full page.
3. `grep -n "api/interview/reports" app/reports/page.tsx` matches.
  </verify>
  <done>`/reports` lists the student's own reports newest-first with type, interviewer, date, status and scores; abandoned sessions are absent; clicking any row (including a FAILED one) opens its full report page.</done>
</task>

</tasks>

<verification>
- Two real users: a report belonging to user A never appears in user B's list. This plan does
  not verify that itself — plan 07-07's human walkthrough owns it (log in as the second
  seeded student `bob.williams@case.edu` / `student123` and confirm the first student's
  reports are absent from `/reports`).
- No `prisma/` file changed; no migration run.
- `lib/interview/report-dto.ts` is unchanged (reused, not edited).
</verification>

<success_criteria>
- A student who finished an interview can find that report again without a saved URL.
- FAILED reports remain reachable; abandoned sessions are invisible.
- The list endpoint follows the Phase 6 auth contract exactly (401 unauthenticated, owner-scoped, no-store).
</success_criteria>

<output>
After completion, create `.planning/phases/07-interaction-dashboard/07-03-SUMMARY.md`
</output>
