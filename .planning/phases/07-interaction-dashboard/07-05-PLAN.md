---
phase: 07-interaction-dashboard
plan: 05
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - app/case-play/page.tsx
autonomous: true
requirements: [REQ-15]

must_haves:
  truths:
    - "A student can browse published cases at /case-play with no cohort membership and no admin assignment"
    - "Unpublished cases never appear in that list"
    - "Picking a case opens /case-play/{caseId} and the case plays without a cohortId"
    - "An empty list explains itself rather than looking broken"
  artifacts:
    - path: "app/case-play/page.tsx"
      provides: "published-case index page"
      min_lines: 50
  key_links:
    - from: "app/case-play/page.tsx"
      to: "/api/case/list?publishedOnly=true"
      via: "fetch on mount"
      pattern: "publishedOnly=true"
    - from: "app/case-play/page.tsx"
      to: "/case-play/{caseId}"
      via: "card click navigation, no cohortId query param"
      pattern: "/case-play/"
---

<objective>
Build the `/case-play` index: a student-facing list of published cases that routes into the
existing `/case-play/[caseId]` experience.

Purpose: This is what makes Case Studies a real dashboard destination. The dashboard tile
routes here, and the dashboard itself stays a pure launcher that knows nothing about case
data. It is also the surface that removes cohort membership from the student case path
(ROADMAP success criterion 5).

Output: `app/case-play/page.tsx`. Nothing else changes — the play page already works without
a cohort.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-interaction-dashboard/07-CONTEXT.md

# The published flag and the filtered endpoint this page consumes (built in 07-02)
@.planning/phases/07-interaction-dashboard/07-02-SUMMARY.md
@app/api/case/list/route.ts
@types/index.ts

# The existing case card component and grid to reuse
@components/case-card.tsx
@app/case-management/page.tsx
</context>

<constraints>
- **There is NO bare `/case-play` route today** — only the `[caseId]` dynamic segment. This
  page is net-new, not a modification.
- **No cohort filter, no assignment lookup.** Browse ALL published cases. Do not call
  `/api/student/cases` (that route is the cohort-scoped listing being retired — it returns
  `[]` for any student in zero cohorts, which is structurally every self-directed student).
  Do not call `s3Storage.listCohorts()`. Do not read `cohortIds` off `CaseStudy` — it is a
  dead field (written once as `[]`, never read anywhere).
- **`published` is a DISCOVERY filter, NOT an access control.** This page hides unpublished
  cases. `/case-play/{caseId}` must still play an unpublished case by direct URL — that is
  the deliberate staff draft-preview mechanism. **Do not add a published check to
  `/api/case/get` or to `app/case-play/[caseId]/page.tsx`.** Do not "harden" this.
- **Expect the list to be EMPTY on first run.** Every existing S3 case predates the flag and
  reads as unpublished. That is correct, not a bug. Publish one case manually via
  `/case-management` to validate the flow.
- **Per-case avatar-minutes limits are intentionally dropped.** The old `/student-cases`
  showed a `heygenMinutesLimit` chip sourced from `CaseAssignment`; there is no equivalent in
  the no-cohort model. Entering `/case-play/{caseId}` with no `cohortId` simply skips that
  lookup and plays in unlimited-avatar-time mode. This is an accepted consequence of REQ-15,
  not an oversight — do not reintroduce a cohort lookup to recover it.
- **Do NOT edit `app/case-play/[caseId]/page.tsx`.** Its four hardcoded `/student-cases`
  links are repointed by plan 07-06, which owns that file.
- No database migration. Never run `npm run setup`. Never edit `.env`/`.env.local`. Dev
  server needs the inline local URL:
  `DATABASE_URL="postgresql://ajabreu79@localhost:5432/leadership_avatar_dev" npm run dev`.
  A missing-table error means STOP, not migrate.
- `npx tsc --noEmit` is authoritative and MUST report ZERO errors — it is clean on the
  current tree. If `.next/types/validator.ts` errors appear after a route deletion, run
  `rm -rf .next` and re-run — do NOT ignore them. ESLint is broken repo-wide; `next build` is
  known-broken on `/about` — do not gate on either.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Published-case index page</name>
  <files>app/case-play/page.tsx</files>
  <action>
Create `app/case-play/page.tsx` as a client component ("use client") titled "Case Studies".

Fetch `GET /api/case/list?publishedOnly=true` on mount with `credentials: "include"`, typed
as `CaseStudy[]` from `@/types`. Middleware already gates `/case-play` under
`STUDENT_ROUTES`, and `/api/case/list` is reachable by any authenticated user — no new auth
work is needed here, and no page-level auth check should be added (that would be
inconsistent with the codebase's middleware-based model).

Render the results in a card grid. Reuse `components/case-card.tsx` (`<CaseCard caseStudy
onClick />`) exactly as `app/case-management/page.tsx:88` does — it already renders the cover
image, name and a click handler. Match that page's grid classes so the two surfaces look
like one product.

`onClick` navigates to `/case-play/${caseStudy.id}` with **no `cohortId` query param**. The
play page defaults `cohortId` to `""` and gates every downstream use of it, so the case
plays fully.

Three states:
- loading: skeleton or spinner consistent with other pages
- empty: a clear, non-broken-looking message — "No case studies are available yet." plus a
  line telling the student to check back. Do NOT surface anything about publishing state or
  staff workflow to students.
- error: readable failure, not a blank page

Page wrapper padding: `py-8 md:py-10 px-4 md:px-6 lg:px-8` inside `max-w-7xl mx-auto` —
there is no shared student layout, and `app/case-play/[caseId]/layout.tsx` is a bare
pass-through that does not apply to this route's own segment.

Add a back affordance to `/` (the dashboard) so this page is not a one-way trip.
  </action>
  <verify>
1. `npx tsc --noEmit` — MUST report ZERO errors. It is clean on the current tree. If
   `.next/types/validator.ts` errors appear after a route deletion, run `rm -rf .next` and
   re-run — do NOT ignore them.
2. `grep -n "publishedOnly=true" app/case-play/page.tsx` matches.
3. `grep -n "cohort" app/case-play/page.tsx` returns NOTHING.
4. Dev server with the inline local `DATABASE_URL`, logged in as a student:
   - `/case-play` initially renders the empty state (expected — nothing is published yet).
   - Publish one case via `/case-management` (the toggle from 07-02), reload `/case-play`:
     that case and ONLY that case appears.
   - Click it → lands on `/case-play/{id}` with no `cohortId` in the URL and the case intro
     renders.
   - Confirm an UNPUBLISHED case's direct URL `/case-play/{draftId}` still plays — this is
     required behavior, not a leak.
  </verify>
  <done>`/case-play` lists exactly the published cases with no cohort involvement, picking one enters the case, an empty list reads as deliberate, and unpublished cases remain playable by direct URL.</done>
</task>

</tasks>

<verification>
- `grep -rn "student/cases\|listCohorts\|cohortIds" app/case-play/page.tsx` returns nothing.
- `app/api/case/get/route.ts` and `app/case-play/[caseId]/page.tsx` are unchanged by this plan.
- No `prisma/` file changed; no migration run.
</verification>

<success_criteria>
- A student with zero cohort memberships can browse and start a case study.
- Only published cases are discoverable; draft preview by direct URL still works.
- The dashboard's Case Studies tile now has a real destination.
</success_criteria>

<output>
After completion, create `.planning/phases/07-interaction-dashboard/07-05-SUMMARY.md`
</output>
