---
phase: 07-interaction-dashboard
plan: 06
type: execute
wave: 3
depends_on: ["07-03", "07-04", "07-05"]
files_modified:
  - app/settings/page.tsx
  - app/settings/layout.tsx
  - app/student-cases/page.tsx
  - app/student-cases/layout.tsx
  - app/student-cases/settings/page.tsx
  - config/site.ts
  - middleware.ts
  - app/login/page.tsx
  - app/join/[accessCode]/page.tsx
  - app/interview/[type]/page.tsx
  - app/interview/[type]/report/[reportId]/page.tsx
  - app/case-play/[caseId]/page.tsx
autonomous: true
requirements: [REQ-14]

must_haves:
  truths:
    - "The student sidebar reads Practice, My Reports, Settings and every entry leads somewhere real"
    - "Settings is reachable at /settings and no longer nested under a cases concept"
    - "A student logging in lands on the dashboard, not a 404"
    - "Finishing or leaving an interview, a report, or a case returns the student to the dashboard"
    - "/reports and /settings are gated by the same student route list as every other student surface"
    - "No /student-cases route or link remains anywhere in the app"
  artifacts:
    - path: "app/settings/page.tsx"
      provides: "top-level settings page moved out of the cases subtree"
      min_lines: 100
    - path: "app/settings/layout.tsx"
      provides: "the page padding the deleted student-cases layout used to supply"
    - path: "config/site.ts"
      provides: "studentNavItems: Practice, My Reports, Settings"
      contains: "My Reports"
    - path: "middleware.ts"
      provides: "STUDENT_ROUTES entries for /reports and /settings, /student-cases removed"
      contains: "/reports"
  key_links:
    - from: "config/site.ts"
      to: "/, /reports, /settings"
      via: "studentNavItems hrefs rendered by components/auth-navbar.tsx"
      pattern: "\"/reports\""
    - from: "app/login/page.tsx"
      to: "/"
      via: "post-login redirect"
      pattern: "window.location.href = \"/\""
    - from: "middleware.ts"
      to: "STUDENT_ROUTES"
      via: "prefix match gating /reports and /settings"
      pattern: "\"/settings\""
---

<objective>
Retire `/student-cases`: move settings to a top-level `/settings`, rebuild the student
sidebar, update the middleware route lists, delete the old route tree, and repoint every
remaining `/student-cases` link to the dashboard.

Purpose: `/student-cases` is the cohort-scoped listing that is structurally empty for a
self-directed student. Every destination it pointed at now exists (dashboard `/`, case index
`/case-play`, reports `/reports`), so this plan removes the old surface and makes every link
in the app lead somewhere real.

Output: `/settings` exists, `/student-cases` does not, the sidebar is Practice · My Reports ·
Settings, and a repo-wide grep for `student-cases` in app code returns nothing.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-interaction-dashboard/07-CONTEXT.md

# The destinations that must already exist before this plan runs
@.planning/phases/07-interaction-dashboard/07-04-SUMMARY.md
@.planning/phases/07-interaction-dashboard/07-05-SUMMARY.md
@.planning/phases/07-interaction-dashboard/07-03-SUMMARY.md

@config/site.ts
@components/auth-navbar.tsx
@middleware.ts
@app/student-cases/settings/page.tsx
@app/student-cases/layout.tsx
</context>

<constraints>
- **`/student-cases` is DELETED outright — no redirect stub.** Locked decision. Old external
  links may 404; that is accepted at this stage.
- **`app/page.tsx` is NOT in this plan's scope.** Plan 07-04 already removed its
  `/student-cases` redirect and owns that file. Do not edit it.
- **Do NOT delete `app/api/student/cases/route.ts`, `app/join/[accessCode]/`, or any cohort
  machinery.** Cohort and staff teardown is Phase 11 and is explicitly out of scope. This
  plan only repoints links and removes the `/student-cases` pages.
- **Do NOT rename any "CaseBridge" copy.** The product rename to "Leadership Avatar" is its
  own deferred phase.
- **Work from a fresh grep, not from any list — including the one in this plan.** Research
  found 13 call sites across 7 files where earlier discussion assumed 3. Two easily-missed
  ones are the post-login redirect and the post-cohort-join button.
- `npx tsc --noEmit` is authoritative and MUST report ZERO errors — it is clean on the
  current tree. If `.next/types/validator.ts` errors appear after a route deletion, run
  `rm -rf .next` and re-run — do NOT ignore them.
  **This plan DELETES `app/student-cases/`, which is exactly the condition that generates
  stale `.next/types/validator.ts` errors — `rm -rf .next` and re-run rather than waving
  them through.** ESLint is broken repo-wide (pre-existing). `next build`
  is known-broken on `/about` — do not gate on it. Dev server needs the inline local URL:
  `DATABASE_URL="postgresql://ajabreu79@localhost:5432/leadership_avatar_dev" npm run dev`.
  A missing-table error means STOP, not migrate. Never run `npm run setup`; never edit
  `.env`/`.env.local`.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Move settings to /settings and delete the student-cases tree</name>
  <files>app/settings/page.tsx, app/settings/layout.tsx, app/student-cases/page.tsx, app/student-cases/layout.tsx, app/student-cases/settings/page.tsx</files>
  <action>
Move `app/student-cases/settings/page.tsx` → `app/settings/page.tsx`. Use `git mv` so history
follows. All of its imports are absolute `@/...`, so no import rewriting is needed — verify
that with a read rather than assuming.

Its `handleSave` is a 500ms `setTimeout` with no backend call (confirmed), so this is a pure
file move with no API contract to preserve. Do not "fix" that here; it is out of scope.

Create `app/settings/layout.tsx` reproducing what `app/student-cases/layout.tsx` provided,
since that wrapper is about to be deleted and there is no shared student layout:

```tsx
<section className="flex flex-col gap-4 py-8 md:py-10 px-4 md:px-6 lg:px-8">
  <div className="w-full max-w-7xl mx-auto">{children}</div>
</section>
```

Then delete the rest of the tree: `git rm app/student-cases/page.tsx
app/student-cases/layout.tsx` and remove the now-empty `app/student-cases/settings/`
directory. Confirm `app/student-cases/` no longer exists.

Nothing on the deleted `/student-cases` page is worth carrying forward: its cohort-filtered
case grid is replaced by `/case-play`, its `pendingCohortJoin` localStorage banner is cohort
machinery being retired, and its per-case avatar-minutes chips have no equivalent in the
no-cohort model (an accepted REQ-15 consequence, already noted in 07-05).

**Record this user-visible consequence in the SUMMARY — do not skip it.** `pendingCohortJoin`
is written by `app/join/[accessCode]/page.tsx:37` and consumed ONLY by the page being
deleted. `app/login/page.tsx` never reads `returnTo` (zero occurrences — a pre-existing gap).
So after this deletion **the logged-out join-by-code path stops completing**: a logged-out
student who opens `/join/{code}` will log in and the join never finishes, leaving an orphaned
`pendingCohortJoin` key in localStorage. The **logged-in** join path (`join:142`) still works.
This is accepted here — cohort machinery is retired and **Phase 11 owns the removal** — but it
must be written down in the SUMMARY and carried into 07-07's deferred-items list. Do NOT
attempt to fix the join flow in this plan.
  </action>
  <verify>
1. `ls app/student-cases` fails (directory gone); `ls app/settings` shows `page.tsx` and
   `layout.tsx`.
2. `npx tsc --noEmit` — MUST report ZERO errors. It is clean on the current tree. If
   `.next/types/validator.ts` errors appear after a route deletion, run `rm -rf .next` and
   re-run — do NOT ignore them.
3. `git status` shows the settings file as a rename, not an add+delete of unrelated content.
  </verify>
  <done>`/settings` renders the former settings page with the same padding; `app/student-cases/` no longer exists.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild student nav and the middleware route lists</name>
  <files>config/site.ts, middleware.ts</files>
  <action>
**config/site.ts** — replace `studentNavItems` (lines 29-40) with exactly three entries:

1. `{ label: "Practice", href: "/", icon: "Briefcase" }`
2. `{ label: "My Reports", href: "/reports", icon: "History" }`
3. `{ label: "Settings", href: "/settings", icon: "Settings" }`

Icon names MUST exist in `components/auth-navbar.tsx`'s `iconMap` (lines 32-43), which
currently holds: Home, Settings, Users, ChartColumnBig, Mail, Video, Briefcase, History,
GraduationCap, QrCode. `Briefcase`, `History` and `Settings` are all present — if you choose
different icons, add them to `iconMap`, or the icon silently renders as nothing (the render
is guarded by `IconComponent && (...)` at line 124).

Leave staff `navItems` (lines 6-28) completely untouched.

**middleware.ts** — in `STUDENT_ROUTES` (starts line 200):
- REMOVE `"/student-cases"` (line 201). The route no longer exists.
- ADD `"/reports"` and `"/settings"`.
- Leave `"/api/student/cases"` in place — the endpoint still exists and Phase 11 owns its
  removal. Removing it here would change that endpoint's gate as a side effect.
- Leave `"/interview"`, `"/case-play"`, `"/api/interaction"`, `"/api/interview"` and the
  avatar/profile/audio entries untouched. `/api/interview/reports` (07-03) is already covered
  by the `/api/interview` prefix, and the new `/case-play` index (07-05) by the `/case-play`
  prefix — neither needs a new entry.

Without the two additions, `/reports` and `/settings` fall through to the generic "any
authenticated role" path, which is a materially looser gate than every sibling student
surface. That is the whole point of this edit.
  </action>
  <verify>
1. `npx tsc --noEmit` — MUST report ZERO errors. It is clean on the current tree. If
   `.next/types/validator.ts` errors appear after a route deletion, run `rm -rf .next` and
   re-run — do NOT ignore them.
2. `grep -n "student-cases" middleware.ts config/site.ts` returns NOTHING.
3. `grep -n '"/reports"\|"/settings"' middleware.ts` shows both inside `STUDENT_ROUTES`.
4. Dev server with the inline local `DATABASE_URL`, logged in as a student: the sidebar reads
   Practice · My Reports · Settings, all three icons render, and each link lands on a real
   page (`/`, `/reports`, `/settings`).
  </verify>
  <done>Student nav is Practice · My Reports · Settings with working icons and live destinations; `STUDENT_ROUTES` gates `/reports` and `/settings` and no longer lists `/student-cases`.</done>
</task>

<task type="auto">
  <name>Task 3: Repoint every remaining /student-cases link</name>
  <files>app/login/page.tsx, app/join/[accessCode]/page.tsx, app/interview/[type]/page.tsx, app/interview/[type]/report/[reportId]/page.tsx, app/case-play/[caseId]/page.tsx</files>
  <action>
**Scope note (context pressure):** this plan is the heaviest in the phase — 3 tasks, 12
files, a `git mv`, a directory deletion, two route-list edits and 13 link repoints. If you
find yourself running low on context partway through, **stop after Task 2 and split this
task out as `07-06b`**: Task 3 is pure link repointing driven by its own grep, depends on
nothing in Tasks 1-2 beyond the deletion already being done, and is cleanly separable.
If you DO split: create `07-06b-PLAN.md` with `wave: 3` and `depends_on: ["07-06"]`, ADD
`07-06b` to 07-07's `depends_on`, and bump 07-07 to `wave: 4` after it. Record the split in
`07-06-SUMMARY.md` and in the ROADMAP plan list. Without this binding, 07-07 becomes eligible
while 13 `/student-cases` links are still live, and its checks fail purely on sequencing.

**Start by running the grep yourself** — do not work from the list below:

```
grep -rn "student-cases" --include="*.ts" --include="*.tsx" app components lib config middleware.ts
```

Repoint every remaining hit to `/` (the dashboard). Known at planning time:

- `app/login/page.tsx:30` — `window.location.href = "/student-cases"` on successful login.
  **This is the critical one**: miss it and a student's very first navigation after signing
  in 404s. Change to `"/"`.
- `app/join/[accessCode]/page.tsx:226` — post-cohort-join button `router.push`. Change to
  `"/"`. Do not otherwise modify the join flow (Phase 11 owns cohort teardown).
- `app/interview/[type]/page.tsx:160` — "Back to practice" button on the unknown-interview-
  type error card. Change to `"/"`; the label stays accurate.
- `app/interview/[type]/page.tsx:198` — the wizard's back link. Change to `"/"`.
- `app/interview/[type]/report/[reportId]/page.tsx:157` and `:169` — the not-found button and
  the `onBack` handler. **Consider sending these to `/reports` instead of `/`** — a student
  leaving a report is more likely to want the report list; use your judgment and state which
  you chose and why in the SUMMARY. Either is acceptable; a dead link is not.
- `app/case-play/[caseId]/page.tsx` at lines 1071 (`handleFinish`), 1107
  (`handleSaveAndExit`), 1142 (the "Case not found" card) and 1154 (the intro back arrow).
  Send these to `/case-play` (the case index from 07-05) rather than `/` — a student
  finishing one case most likely wants another. At line 1107 the current code appends a
  `cohortId` query string (`/student-cases${q}`); **drop that query string entirely** — the
  new index takes no cohort param. State this choice in the SUMMARY.

Change only the navigation target at each site. Do not refactor surrounding logic, do not
touch `lib/interview/prompts.ts` (a contract), and do not modify `app/page.tsx`.
  </action>
  <verify>
1. `grep -rn "student-cases" --include="*.ts" --include="*.tsx" app components lib config
   middleware.ts` returns **NOTHING**. This is the gate for REQ-14.
2. `npx tsc --noEmit` — MUST report ZERO errors. It is clean on the current tree. If
   `.next/types/validator.ts` errors appear after a route deletion, run `rm -rf .next` and
   re-run — do NOT ignore them.
3. Dev server with the inline local `DATABASE_URL`: log out and log back in as a student →
   land on the dashboard, not a 404. Open `/interview/nonexistent-type` → the error card's
   button goes somewhere real. Open a case from `/case-play` and use the intro back arrow →
   returns to `/case-play`.
  </verify>
  <done>Zero occurrences of the string `student-cases` remain in application code, and every former link lands on the dashboard, the case index, or the reports list.</done>
</task>

</tasks>

<verification>
- Repo-wide grep for `student-cases` in `app/`, `components/`, `lib/`, `config/` and
  `middleware.ts` is empty.
- `app/page.tsx` diff is empty for this plan (07-04 owns it).
- `app/api/student/cases/route.ts` still exists (Phase 11's job, not this one).
- No `prisma/` file changed; no migration run; `.env`/`.env.local` untouched.
- `npx tsc --noEmit` reports ZERO errors (run `rm -rf .next` first if the deletion left stale
  validator errors).
- The SUMMARY records the logged-out join-by-code consequence as a deferred item.
</verification>

<success_criteria>
- A student can log in, land on the dashboard, reach Practice / My Reports / Settings from
  the sidebar, and never hit a 404 from an in-app link.
- `/reports` and `/settings` are gated exactly like the other student surfaces.
- The cohort-scoped `/student-cases` surface is gone.
</success_criteria>

<output>
After completion, create `.planning/phases/07-interaction-dashboard/07-06-SUMMARY.md`
</output>
