---
phase: 07-interaction-dashboard
plan: 07
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05", "07-06"]
files_modified: []
autonomous: false
requirements: [REQ-11, REQ-12, REQ-13, REQ-14, REQ-15, REQ-16]

must_haves:
  truths:
    - "A signed-in student lands on the interaction dashboard, not on assigned cases"
    - "A student can start a practice interview without being assigned one and without typing a URL"
    - "The dashboard routes into the right experience per interaction type"
    - "Adding a new interaction type would be a registry record, not a new page"
    - "No surface on the student path depends on cohort membership or admin assignment"
    - "No database migration was performed anywhere in this phase"
  artifacts:
    - path: ".planning/phases/07-interaction-dashboard/07-07-SUMMARY.md"
      provides: "verbatim per-step results of the static sweep and the human validation run"
  key_links:
    - from: "student sign-in"
      to: "dashboard at /"
      via: "post-login redirect"
      pattern: "window.location.href = \"/\""
    - from: "dashboard tile"
      to: "/interview/general and /case-play"
      via: "registry route field"
      pattern: "listInteractionTypes"
---

<objective>
Prove Phase 7 actually works: a read-only static sweep for constraint violations, then a real
human walkthrough of the student path end to end.

Purpose: Every prior plan verified its own slice. This plan verifies the seams between them —
the places where six independently-executed plans typically fail: a nav link to a page that
renders but errors, a tile routing to a 404, a constraint quietly violated by an executor
who "improved" something.

Output: a SUMMARY recording verbatim per-step results and any deferred open items.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-interaction-dashboard/07-CONTEXT.md

@.planning/phases/07-interaction-dashboard/07-01-SUMMARY.md
@.planning/phases/07-interaction-dashboard/07-02-SUMMARY.md
@.planning/phases/07-interaction-dashboard/07-03-SUMMARY.md
@.planning/phases/07-interaction-dashboard/07-04-SUMMARY.md
@.planning/phases/07-interaction-dashboard/07-05-SUMMARY.md
@.planning/phases/07-interaction-dashboard/07-06-SUMMARY.md
</context>

<constraints>
- This plan is **verification-first**. If the sweep finds a genuine defect in this phase's own
  work, fix it and record the fix; do not expand scope into pre-existing issues, and do not
  start new features.
- `npx tsc --noEmit` is authoritative and **MUST report ZERO errors** — it is clean on the
  current tree. Plan 07-06 deletes `app/student-cases/`, which is exactly the condition that
  generates stale `.next/types/validator.ts` errors. If any appear, run `rm -rf .next` and
  re-run — **do NOT ignore them**; waving post-deletion noise through is how a genuine error
  in the new `app/reports`, `app/settings` or `app/case-play` routes gets masked.
  ESLint is broken repo-wide (pre-existing, `plugin:@next/next/recommended is invalid`) —
  confirm that against an untouched file rather than attempting a repair. `next build` is
  known-broken on `/about` (missing `EDGE_CONFIG`) — do not gate on it.
- Dev server for the walkthrough MUST carry the local DB inline:
  `DATABASE_URL="postgresql://ajabreu79@localhost:5432/leadership_avatar_dev" npm run dev`.
  A bare `npm run dev` targets the SHARED production database. **A missing-table error means
  STOP, not migrate.**
- **Never run `npm run setup`** (`scripts/setup.mjs:211` runs `migrate deploy` against the
  shared database). **Never edit `.env`/`.env.local`** — they are one file (`.env` is a
  symlink).
- `.planning/` is gitignored, so doc commits no-op. Expected. Do not edit `.gitignore`.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Static constraint sweep</name>
  <files>(read-only; fixes only if a defect in this phase's work is found)</files>
  <action>
Run each check and record the literal output in the SUMMARY. A check that "looks fine" without
recorded output does not count.

**Build / types**
1. `npx tsc --noEmit` — MUST report **ZERO** errors. If `.next/types/validator.ts` errors
   appear (07-06 deleted a route tree), run `rm -rf .next` and re-run. Do NOT record them as
   "expected"; a non-empty result after `rm -rf .next` is a real defect to investigate.
2. Confirm ESLint's repo-wide breakage is pre-existing by running it against an untouched file
   (e.g. `lib/languages.ts`). Record, do not fix.

**No migration happened (hard constraint 1)**
0. **Establish the baseline FIRST.** `main` is **NOT** the Phase 7 baseline — it predates
   Phases 1-6, whose `prisma/` and `lib/interview/prompts.ts` changes are legitimate and
   already merged into this branch. Diffing against `main` reports +365 lines under `prisma/`
   and +308 in `prompts.ts` — both expected, neither a Phase 7 violation.
   **NEVER revert any file to `main`. Doing so deletes Phases 1-6.**
   Derive the real baseline: `PHASE7_BASE=$(git log --oneline --grep='^feat(07-\|^fix(07-' --format=%H | tail -1)^`
   (the commit immediately before this phase's first commit). If no `07-` commit exists yet,
   `PHASE7_BASE=HEAD`. Record the resolved SHA in the SUMMARY.
3. `git diff $PHASE7_BASE --stat -- prisma/` — MUST be empty.
4. `git log $PHASE7_BASE..HEAD --oneline -- prisma/` — MUST be empty.
5. `grep -rn "prisma migrate\|migrate deploy\|migrate dev" .planning/phases/07-interaction-dashboard/*-SUMMARY.md` — any hit means an executor ran one; investigate immediately.
6. `ls prisma/migrations | tail -5` — confirm no migration folder dated during this phase.

**REQ-14 completeness**
7. `grep -rn "student-cases" --include="*.ts" --include="*.tsx" app components lib config middleware.ts` — MUST return nothing.
8. `ls app/student-cases` — MUST fail.
9. `grep -n '"/reports"\|"/settings"' middleware.ts` — both present in `STUDENT_ROUTES`.

**REQ-11 decoupling**
10. `grep -rn "lib/interview" lib/interactions/ components/interactions/` — MUST return nothing.
11. `grep -ric "leading a meeting" lib/interactions/` — MUST be 0.
12. Count the registry: exactly 5 types, exactly 2 `live`.

**REQ-13 inertness**
13. `grep -n "onClick\|onPress\|href\|role=\"button\"" components/interactions/InteractionTile.tsx` — every match must be inside the live branch. Read the file to confirm, don't just count.

**REQ-15 correctness — the two-Case trap**
14. `grep -rn "prisma.case\|isPublished" app/case-play/ app/api/case/ components/interactions/` — MUST return nothing. A hit means someone wired the student list to the wrong (staff gradebook, 3-seed-row) Case table.
15. `git diff $PHASE7_BASE -- app/api/case/get/route.ts` — MUST be empty. `published` must remain a
    discovery filter, not an access control.
    **Known and ACCEPTED, do not file as a defect:** `publishedOnly` is opt-in and
    `/api/case/list` sits in neither `ADMIN_ROUTES` nor `STUDENT_ROUTES`, so the unfiltered
    default is reachable by any authenticated user and unpublished cases are enumerable. This
    is consistent with `published` being a visibility filter and is recorded as accepted in
    07-02's constraints. **Do NOT "fix" it in this phase** — record it in the deferred list.

**REQ-16**
16. `grep -n "toInterviewReportDTO" app/api/interview/reports/route.ts` — present (no second DTO).
17. `grep -n "IN_PROGRESS" app/api/interview/reports/route.ts` — the filter sits in the Prisma `where`, not a client-side `.filter()`.

**Out-of-scope guard**
18. `git diff $PHASE7_BASE --stat -- lib/interview/prompts.ts` — MUST be empty (it is a contract).
    NOT `git diff main` — see check 0; prompts.ts legitimately differs from `main` by +308 lines.
19. Confirm nothing implements a deferred idea: no interview customization UI, no
    student-authored scenarios, no Pitches/Difficult Conversations/Networking experience
    beyond a registry record, no cohort teardown, no CaseBridge→Leadership Avatar rename.
  </action>
  <verify>All 19 checks recorded with literal output in the SUMMARY; every MUST-be-empty check is empty.</verify>
  <done>No constraint violation remains unexplained. Any genuine defect in this phase's work is fixed and re-checked; anything pre-existing is recorded as deferred, not fixed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human walkthrough of the student path</name>
  <what-built>
The complete Phase 7 student experience: a `lib/interactions` registry, the interaction
dashboard at `/`, a published-case index at `/case-play`, a reports list at `/reports`,
settings moved to `/settings`, a rebuilt sidebar, and `/student-cases` deleted with every
link repointed.
  </what-built>
  <how-to-verify>
Start the dev server against the LOCAL database — the inline URL is required:

```
DATABASE_URL="postgresql://ajabreu79@localhost:5432/leadership_avatar_dev" npm run dev
```

If you see a missing-table error, STOP and report it. Do not migrate.

Then, signed in as a **student**:

1. **Log in.** You land on the dashboard at `/`, not a 404 and not a cases list.
2. **The dashboard** shows five tiles — Practice Interviews and Case Studies first, then
   Pitches, Difficult Conversations, Networking. Each tile shows an icon, a name, a one-line
   description and an estimated duration. There is NO "Leading a Meeting" tile.
3. **Click a coming-soon tile.** Nothing happens: no navigation, no modal, no toast. It looks
   deliberately inactive (greyed, "Coming soon" badge), not broken. Tab to it with the
   keyboard — it should not take focus as a button.
4. **Click Practice Interviews.** You reach `/interview/general` — an interview you were never
   assigned, reached without typing a URL.
5. **Use the wizard's back link.** It returns you somewhere real (the dashboard), not a 404.
6. **Click Case Studies.** You reach `/case-play`. If nothing is published yet, the empty
   state reads as deliberate ("No case studies are available yet"), not as an error.
7. **Publish a case:** sign in as an admin, open `/case-management`, open a case, flip the
   Published switch, save. Sign back in as the student and reload `/case-play` — that case,
   and only that case, is listed.
8. **Open the published case.** It plays with no cohort and no assignment; the URL has no
   `cohortId`. Use the intro back arrow — it returns to `/case-play`.
9. **Draft preview still works:** paste the URL of an UNPUBLISHED case
   (`/case-play/{thatId}`) directly. It should still open. This is intended — `published` is
   a visibility filter, NOT an access control.
10. **Sidebar** reads Practice · My Reports · Settings, all three icons render, and all three
    links land on real pages.
11. **`/reports`** lists your finished interview reports newest-first with type, interviewer,
    date, status and scores. Abandoned (IN_PROGRESS) sessions do NOT appear. Clicking a row
    opens its full report. If a FAILED report exists, it is listed and clickable.
12. **`/settings`** renders the settings page with normal page padding.
13. **Sign in as an admin and visit `/`** — you still see the Cases / Cohorts / Avatars
    management cards, unchanged.
14. **Try `/student-cases`** — a 404 is the correct, expected result.
15. **Cross-user isolation.** Sign out and sign in as the *other* seeded student. The two
    seeded accounts are `alice.johnson@case.edu` and `bob.williams@case.edu`, both password
    `student123`. Open `/reports` — the first student's reports must be **completely absent**;
    you see only this account's own reports (or the empty state). Any leak here is a blocker.

Report any step that fails, with what you saw instead.
  </how-to-verify>
  <action>
Start the dev server with the inline local `DATABASE_URL` shown above, then walk the human
through the 15 numbered steps in `<how-to-verify>` and record their verbatim result for each
one in the SUMMARY. Do not mark a step passed that the human did not actually confirm.
  </action>
  <verify>All 15 steps have a recorded human result; failures are reported with what was seen instead.</verify>
  <done>The human types "approved", or every reported failure is fixed and the failing steps re-confirmed.</done>
  <resume-signal>Type "approved" or describe which numbered step failed and what you saw.</resume-signal>
</task>

</tasks>

<verification>
- All 19 static checks recorded with literal output.
- All 15 walkthrough steps human-confirmed, or failures recorded and addressed.
- The five ROADMAP Phase 7 success criteria are each traceable to a specific walkthrough step:
  (1) → step 1, (2) → step 4, (3) → steps 4 and 6, (4) → static check 12 plus the registry's
  shape, (5) → steps 7-9 and static check 14.
</verification>

<success_criteria>
- A student can sign in, choose a leadership interaction, and be routed into it, with no
  cohort membership and no admin assignment anywhere on the path.
- No database migration was performed in this phase.
- Deferred/open items are recorded in the SUMMARY rather than silently fixed or forgotten.

**The deferred-items list in the SUMMARY MUST include, at minimum, these three known and
accepted consequences — they are recorded so a later reader does not rediscover them as
regressions:**
1. **Logged-out join-by-code no longer completes.** `pendingCohortJoin` is written by
   `app/join/[accessCode]/page.tsx:37` and was consumed only by the deleted
   `app/student-cases/page.tsx`; `app/login/page.tsx` never reads `returnTo`. A logged-out
   student opening `/join/{code}` logs in and the join never finishes, leaving an orphaned
   localStorage key. The logged-in join path (`join:142`) still works. **Phase 11 owns the
   removal.**
2. **`/api/case/list` stays enumerable.** `publishedOnly` is opt-in and the route is in
   neither `ADMIN_ROUTES` nor `STUDENT_ROUTES`, so any authenticated user can list
   unpublished cases. Accepted — `published` is a discovery filter, not access control.
3. **Per-case avatar-minutes limits are dropped** on the student path, an accepted
   consequence of REQ-15's no-cohort model.
</success_criteria>

<output>
After completion, create `.planning/phases/07-interaction-dashboard/07-07-SUMMARY.md` with
verbatim per-step results and a deferred-items list.
</output>
