---
phase: 07-interaction-dashboard
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - components/interactions/InteractionTile.tsx
  - components/interactions/InteractionDashboard.tsx
  - app/page.tsx
autonomous: true
requirements: [REQ-12, REQ-13]

must_haves:
  truths:
    - "A signed-in student landing on / sees a grid of leadership interaction tiles, not assigned cases"
    - "Each tile shows an icon, name, one-line description and estimated duration"
    - "Live tiles appear before coming-soon tiles"
    - "Clicking the Practice Interviews tile starts an interview without typing a URL"
    - "A coming-soon tile cannot be clicked and goes nowhere"
    - "Staff landing on / still see the existing management cards"
  artifacts:
    - path: "components/interactions/InteractionTile.tsx"
      provides: "one tile, live and inert variants"
      min_lines: 40
    - path: "components/interactions/InteractionDashboard.tsx"
      provides: "responsive card grid over the registry"
      min_lines: 30
    - path: "app/page.tsx"
      provides: "student-vs-staff branch at the root route"
      contains: "InteractionDashboard"
  key_links:
    - from: "components/interactions/InteractionDashboard.tsx"
      to: "lib/interactions"
      via: "listInteractionTypes()"
      pattern: "listInteractionTypes"
    - from: "app/page.tsx"
      to: "components/interactions/InteractionDashboard"
      via: "rendered when user.role === student"
      pattern: "InteractionDashboard"
    - from: "components/interactions/InteractionTile.tsx"
      to: "type.route"
      via: "navigation only when availability is live"
      pattern: "coming-soon"
---

<objective>
Build the interaction dashboard and make it what a signed-in student sees at `/`.

Purpose: This is the phase's centerpiece. `/interview/general` is currently reachable only by
typing the URL; the dashboard is the navigation that leads there — and it is deliberately one
level ABOVE interview, so future interaction types are tiles rather than new landing pages.

Output: `InteractionTile`, `InteractionDashboard`, and a rewritten `app/page.tsx` that
branches student → dashboard, staff → the existing management cards.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-interaction-dashboard/07-CONTEXT.md

# The registry this renders (built in 07-01)
@lib/interactions/index.ts
@.planning/phases/07-interaction-dashboard/07-01-SUMMARY.md

# The card-grid and palette pattern to follow (interviewer picker, lines ~190-300)
@app/interview/[type]/page.tsx

# The "honest inert state" idiom the user validated and asked to mirror
@components/interview/ReportScoreCards.tsx

# The file being rewritten
@app/page.tsx
</context>

<constraints>
- **The dashboard must NOT import from `lib/interview` or from case-play internals.** It reads
  `lib/interactions` and nothing else about the experiences. This is a LOCKED decision —
  the dashboard is a pure launcher.
- **Coming-soon tiles are NOT clickable.** Not a disabled button that still focuses into a
  dead end, not a modal, not a toast, not a tooltip promising a date. Render them as a
  non-interactive element (`<div>`, not `<button>`/`<Card isPressable>`), greyed,
  `cursor-default`, with a small "Coming soon" badge. This mirrors the Phase 6 report cards'
  "Not yet measured" treatment, which the user validated. No dead ends, no false promises.
- **Do NOT create a `/dashboard` route.** `/` itself serves the dashboard for students — a
  locked decision.
- **Staff keep the existing management cards at `/` unchanged.**
- **Do NOT add a recent-reports strip, a welcome banner, or any first-run onboarding.** Five
  labeled cards are their own explanation — locked decision. No per-user dismissal state.
- The Practice Interviews tile routes to `/interview/general` only. Preset/variant selection
  is Phase 8 and is OUT OF SCOPE.
- **This plan owns `app/page.tsx` outright**, including deleting the existing
  `router.replace("/student-cases")` effect. Plan 07-06 repoints the other thirteen
  `/student-cases` call sites and must not touch this file.
- `npx tsc --noEmit` is authoritative and MUST report ZERO errors — it is clean on the
  current tree. If `.next/types/validator.ts` errors appear after a route deletion, run
  `rm -rf .next` and re-run — do NOT ignore them. ESLint is broken repo-wide; `next build` is
  known-broken on `/about` — do not gate on either. Dev server needs the inline local DB URL:
  `DATABASE_URL="postgresql://ajabreu79@localhost:5432/leadership_avatar_dev" npm run dev`.
  A missing-table error means STOP, not migrate. Never run `npm run setup`; never edit
  `.env`/`.env.local`.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: InteractionTile with live and inert variants</name>
  <files>components/interactions/InteractionTile.tsx</files>
  <action>
Create a client component taking `{ type: InteractionType }` from `@/lib/interactions`.

Resolve `type.icon` (a string) through a local lucide-react map, exactly the way
`components/auth-navbar.tsx:32-43` does with its `iconMap` — and guard the render so an
unmapped name degrades to no icon rather than crashing. Include every icon name used by the
five registry records.

Two mutually exclusive render branches keyed off `type.availability`:

**live** — an interactive card (HeroUI `<Card isPressable>` or a plain `<button>`, your
choice; the interviewer picker at `app/interview/[type]/page.tsx:242-267` uses a plain
`<button>` for image treatments, and HeroUI `<Card>` is simpler here). Navigates to
`type.route` via `useRouter().push`. Hover/focus affordance. Shows icon, name, one-line
description, and estimated duration (e.g. "~20 min") — duration is REQUIRED on every tile,
the user is explicit that it decides whether an undergraduate starts now or defers.

**coming-soon** — a NON-interactive `<div>`. No `onClick`, no `href`, no `isPressable`, no
`role="button"`, not focusable. `cursor-default`. Muted palette (the codebase's established
inactive tones are `#78909b` / `#b8cbd3`, used by `ProgressItem` in
`app/interview/[type]/page.tsx:303-305`). Small "Coming soon" badge. Same four content
fields so the tile still tells the student what is arriving.

Palette for live tiles (inline arbitrary Tailwind values — these hexes are not in
`tailwind.config`): `#0a7391` primary teal, `#102331` body text, `#d4e2e9` border,
`#eaf5f8` soft accent, `#526c7b` muted text.

Write the branch so `type.route` being `null` is structurally unreachable in the live path —
never render a navigation target for a coming-soon type.
  </action>
  <verify>
1. `npx tsc --noEmit` — MUST report ZERO errors. It is clean on the current tree. If
   `.next/types/validator.ts` errors appear after a route deletion, run `rm -rf .next` and
   re-run — do NOT ignore them.
2. `grep -n "onClick\|onPress\|router.push" components/interactions/InteractionTile.tsx` —
   every match must sit inside the live branch.
3. `grep -n "Coming soon" components/interactions/InteractionTile.tsx` matches.
  </verify>
  <done>A live tile navigates on click and shows icon/name/description/duration; a coming-soon tile renders the same content greyed with a badge, has no click handler and no focusable interactive element.</done>
</task>

<task type="auto">
  <name>Task 2: InteractionDashboard grid</name>
  <files>components/interactions/InteractionDashboard.tsx</files>
  <action>
Create a client component that calls `listInteractionTypes()` from `@/lib/interactions` and
maps each record to `<InteractionTile>`, keyed by slug.

Grid: `grid gap-4 sm:grid-cols-2 xl:grid-cols-3` — matches the locked "2-3 across on desktop"
and the existing interviewer-picker grid. One column on mobile.

`listInteractionTypes()` already returns live-first (built in 07-01), so do NOT re-sort here;
just render in the order given. If 07-01's SUMMARY says otherwise, sort live-first here
instead and note it.

Add a short page heading above the grid — a plain title plus a one-line subhead naming what
this is (practice a leadership interaction). No welcome banner, no onboarding, no dismissible
explainer.

Wrap in the standard student page padding (`py-8 md:py-10 px-4 md:px-6 lg:px-8` inside
`max-w-7xl mx-auto`) — the deleted `app/student-cases/layout.tsx` was the only thing
providing this and there is no shared student layout.

The component must import nothing from `lib/interview`, `app/case-play`, or any experience's
internals.
  </action>
  <verify>
1. `npx tsc --noEmit` — MUST report ZERO errors. It is clean on the current tree. If
   `.next/types/validator.ts` errors appear after a route deletion, run `rm -rf .next` and
   re-run — do NOT ignore them.
2. `grep -rn "lib/interview\|case-play" components/interactions/` returns nothing.
3. `grep -n "listInteractionTypes" components/interactions/InteractionDashboard.tsx` matches.
  </verify>
  <done>The component renders all five registry tiles in a responsive grid, live first, importing only from `lib/interactions`.</done>
</task>

<task type="auto">
  <name>Task 3: Serve the dashboard at / for students</name>
  <files>app/page.tsx</files>
  <action>
Rewrite the root page's role branch.

**Delete the `useEffect` that calls `router.replace("/student-cases")`** (lines 15-19) —
entirely, along with the now-unused `useEffect`/`useRouter` imports if nothing else needs
them (the staff cards still use `router.push`, so `useRouter` likely stays).

Keep the existing `loading` guard. Then:

```tsx
if (user?.role === "student") {
  return <InteractionDashboard />;
}
```

Everything below — `navigationCards` (Cases / Cohorts / Avatars) and the existing staff JSX —
stays EXACTLY as it is. Staff keep the management cards at `/`. Do not restyle them, do not
rename "CaseBridge" (the product rename is its own deferred phase).

Decide deliberately what an unauthenticated visitor sees and state it in the SUMMARY —
`middleware.ts` already gates `/`, so in practice this branch is reached with a user; do not
add a new page-level auth check, which would be inconsistent with the codebase's
middleware-based security model.

Do NOT touch any other `/student-cases` reference in the repo — thirteen others exist across
seven files and plan 07-06 owns all of them.
  </action>
  <verify>
1. `npx tsc --noEmit` — MUST report ZERO errors. It is clean on the current tree. If
   `.next/types/validator.ts` errors appear after a route deletion, run `rm -rf .next` and
   re-run — do NOT ignore them.
2. `grep -n "student-cases" app/page.tsx` returns NOTHING.
3. Dev server with inline local `DATABASE_URL`: log in as a student, then navigate DIRECTLY
   to `/` (type the URL / click the logo). **Do NOT judge this by where login lands you** —
   `app/login/page.tsx:30` still redirects to `/student-cases` until plan 07-06 (wave 3)
   repoints it, so the post-login landing is expected to be the OLD cases page and is NOT a
   failure of this task. **Do NOT edit `app/login/page.tsx` — plan 07-06 owns that file** and
   editing it here creates a collision.
   At `/`: five tiles render; click "Practice Interviews" → lands on `/interview/general`;
   click a coming-soon tile → nothing happens, URL unchanged. Log in as an admin and navigate
   to `/` → still shows Cases/Cohorts/Avatars.
  </verify>
  <done>A student at `/` sees the interaction dashboard and can start an interview by clicking; an admin at `/` sees the unchanged management cards; no redirect to `/student-cases` remains in this file.</done>
</task>

</tasks>

<verification>
- `grep -rn "lib/interview" components/interactions/` returns nothing (registry stays decoupled).
- No `/dashboard` route was created.
- Coming-soon tiles have no click handler, no href, and no `role="button"`.
- `npx tsc --noEmit` reports ZERO errors.
- `git status` shows `app/login/page.tsx` untouched by this plan (07-06 owns it).
</verification>

<success_criteria>
- ROADMAP success criteria 1, 2 and 3 are observably true for a student: lands on the dashboard, starts an interview without typing a URL, and tiles route per type.
- Criterion 4 holds: a sixth type would be a registry record, since the dashboard renders whatever `listInteractionTypes()` returns.
- Coming-soon types are advertised honestly and lead nowhere.
</success_criteria>

<output>
After completion, create `.planning/phases/07-interaction-dashboard/07-04-SUMMARY.md`
</output>
