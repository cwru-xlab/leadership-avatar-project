---
phase: 06-interview-evaluation-and-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - prisma/schema.prisma
  - prisma/migrations/
  - lib/interview/transcript.ts
  - lib/s3-client.ts
autonomous: true
requirements: [REQ-01, REQ-02, REQ-04]

must_haves:
  truths:
    - "An InterviewReport row can be created for a user with nullable visual/vocal scores and a status of IN_PROGRESS."
    - "A transcript written to S3 under a server-derived key can be read back intact."
    - "Migration SQL exists on disk for team review and was never applied to a shared database."
    - "remark-gfm is in package.json before any wave-2 or wave-3 plan runs, and no other plan installs it."
  artifacts:
    - path: "package.json"
      provides: "remark-gfm dependency — the phase's only new package, installed here in wave 1 so the lockfile stops moving before anything else reads node_modules"
      contains: "remark-gfm"
    - path: "prisma/schema.prisma"
      provides: "InterviewReport model + InterviewReportStatus enum + User relation"
      contains: "model InterviewReport"
    - path: "lib/interview/transcript.ts"
      provides: "InterviewTranscript type, turn type, builder and speaker-labeled formatter"
      exports: ["InterviewTranscript", "InterviewTranscriptTurn", "buildInterviewTranscript"]
    - path: "lib/s3-client.ts"
      provides: "saveInterviewTranscript / getInterviewTranscript on S3AvatarStorage"
      contains: "saveInterviewTranscript"
  key_links:
    - from: "lib/s3-client.ts"
      to: "sanitizePathSegment"
      via: "server-derived key construction for userId and reportId"
      pattern: "sanitizePathSegment\\((userId|reportId)"
    - from: "prisma/schema.prisma"
      to: "model User"
      via: "interviewReports relation field"
      pattern: "interviewReports\\s+InterviewReport\\[\\]"
---

<objective>
Create the durable storage layer for interview reports: the `InterviewReport` Prisma model
(with migration SQL generated for team review, applied only locally), the purpose-built
`InterviewTranscript` type, and S3 read/write methods keyed server-side.

Purpose: Every other plan in this phase reads or writes this layer. Nothing can persist,
evaluate, or display a report until the table and the transcript object shape exist.
Output: `InterviewReport` model + migration SQL, `lib/interview/transcript.ts`, two new
methods on `S3AvatarStorage`.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-interview-evaluation-and-report/06-CONTEXT.md
@prisma/schema.prisma
@lib/interview/types.ts
@lib/s3-client.ts
</context>

<constraints>
**HARD CONSTRAINTS — violating any of these fails the plan:**
1. NEVER run `prisma migrate deploy`. NEVER run `prisma db push`. NEVER run any migration
   command against a shared/remote `DATABASE_URL`. Every database command carries the inline
   local `DATABASE_URL` (see Path A) — a bare `prisma` command targets the shared RDS.
   NEVER edit `.env` or `.env.local`; they must keep pointing at the shared RDS.
2. Do NOT add `cohortId`, `caseId`, `assignmentId`, `attemptNumber`, or any
   staff/admin-visibility field to the new model. This is an individual-ownership product.
   Do NOT model `InterviewReport` on `Attempt` or `CaseAssignment`.
3. Do NOT reuse `InteractionLog` (in `types/`) for the transcript. It is built around case
   `roleInteractions`, `caseId`, and role-scoped event types that mean nothing for a
   single-interviewer interview.
4. S3 keys are derived server-side from the authenticated userId and a server-generated
   reportId. Never from client input.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Add InterviewReport model and generate migration SQL (local only)</name>
  <files>prisma/schema.prisma, prisma/migrations/</files>
  <action>
**STEP 0 — do this FIRST, before touching the schema and before `prisma generate`.**

```
npm install remark-gfm
```

This is the ONLY `npm install` in Phase 6, and it lives here rather than in 06-02 because both
plans run in wave 1: an `npm install` mid-wave rewrites `node_modules` and re-runs
`@prisma/client`'s postinstall, which would transiently break this plan's own
`node_modules/.prisma/client/index.d.ts` check for an unrelated reason. Doing it first, in the
same task that later runs `prisma generate`, removes the race entirely. `remark-gfm` is consumed
by 06-07 (the report's Category Breakdown table) in wave 3; 06-07 only greps for it and must not
install it. Do not change the pinned `react-markdown ^10.1.0`.

Then add to `prisma/schema.prisma`:

```prisma
enum InterviewReportStatus {
  IN_PROGRESS
  PENDING
  READY
  FAILED
}

model InterviewReport {
  id                  String                @id @default(uuid())
  userId              String
  typeSlug            String
  interviewerAvatarId String?
  interviewerName     String?

  // Resume context snapshot — makes a report reproducible without re-parsing the PDF.
  // resumeText is a snapshot, NOT a pointer; the PDF itself is never exposed.
  resumeId            String?
  resumeText          String?               @db.Text

  // Canonical transcript lives in S3; this is the server-derived key.
  transcriptKey       String?
  turnCount           Int                   @default(0)

  status              InterviewReportStatus  @default(IN_PROGRESS)

  // Nullable by design. Visual and Vocal stay null until a real metrics pipeline exists.
  visualScore         Int?
  vocalScore          Int?
  contentScore        Int?
  behavioralScore     Int?

  reportMarkdown      String?               @db.Text
  failureReason       String?               @db.Text
  evalModel           String?

  startedAt           DateTime              @default(now())
  completedAt         DateTime?
  createdAt           DateTime              @default(now())
  updatedAt           DateTime              @updatedAt

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, createdAt])
  @@index([userId, typeSlug])
}
```

Add `interviewReports InterviewReport[]` to the `model User` relationships block
(alongside `attempts`). Do not touch any other model.

No unique constraint: repeat interviews are unlimited and each is its own report.

Then generate the migration SQL. Read `.env` / `.env.local` FIRST and check where
`DATABASE_URL` points.

IMPORTANT: `scripts/setup.mjs` establishes that this repo's `DATABASE_URL` is a SHARED,
firewalled database ("reset would wipe every teammate's data"). Assume it is shared unless
the host is explicitly localhost/127.0.0.1.

**Path A — USE THIS. The local database is the confirmed target; this is a settled decision.**

Homebrew `postgresql@17` is installed and running via `brew services`, the database
`leadership_avatar_dev` exists, and the role `ajabreu79` has both SUPERUSER and CREATEDB (Prisma
needs CREATEDB to create and drop its shadow database). `npx prisma migrate status` against
this URL was confirmed working during planning. The user has confirmed the install stays and
that `postgresql://ajabreu79@localhost:5432/leadership_avatar_dev` is THE migration and validation
target for this phase — here and in the end-to-end run in 06-08. Do not re-litigate it, and do
not look for a way to use the shared database instead.

**REQUIREMENT (not an assumption): `.env` and `.env.local` must both still point at the shared
RDS when this task runs.** This plan does not assert that as settled fact and does not repair it
— Task 1's `<verify>` gates on it, hard, both before and after the migrate command. Pass the
local URL inline on the command line, every time.

```
DATABASE_URL="postgresql://ajabreu79@localhost:5432/leadership_avatar_dev" \
  npx prisma migrate dev --name add_interview_report
```

Do NOT edit `.env` or `.env.local`, and do not create an env copy anywhere inside the repo.
`scripts/setup.mjs` asserts the two files agree and expects the shared `DATABASE_URL`; a local
value left in there would break setup for the user and for teammates. Note that in this repo
`.env` is a symlink to `.env.local` — one real file — so there is no split-brain hazard to
"fix" and no reason to touch either path.

PRE-FLIGHT GATE, before the migrate command — this is a STOP condition, not a repair step:

```
grep -h "^DATABASE_URL" .env .env.local | grep -c "localhost\|127.0.0.1"
```

must return **0**. Covers BOTH files. If it returns anything other than 0, one of the env files
has a local `DATABASE_URL` in it: **STOP this plan and report it.** Do NOT edit, restore, rewrite,
or back up `.env` or `.env.local`, do not read or restore anything under
`~/.casebridge-env-backups/`, and do not create an env copy anywhere in or out of the repo.
Whether to change what is in those files is the user's decision, handled outside this phase.
Re-run the same gate immediately AFTER the migrate command to prove this plan did not move them.

Because the URL is inline, a BARE `prisma` command in this repo targets the SHARED database.
Never run a bare `prisma migrate dev`, `migrate deploy`, `migrate reset`, or `db push` here.
Every database command in this phase carries the inline local `DATABASE_URL`.

Expect this to apply the 3 pre-existing migrations first (`20260228044702_init`,
`20260302180438_add_student_dashboard_fields`, `20260916165041_add_user_and_auth_models`) and
then create the new one. That is correct and wanted: it makes the local database a faithful
replica, which is what lets plan 06-08 run a real signed-in session against real `User` rows.

Prisma may automatically run `prisma db seed` after applying migrations to a freshly-provisioned
database, because `package.json` declares a `prisma.seed` script. If that fires, let it — it
inherits the inline local `DATABASE_URL`, so it seeds the LOCAL database and creates the dev
accounts plan 06-08 needs to sign in with. It does pull `tsx` via `npx` from the network (tsx is
not a repo dependency); that is expected here and is the one permitted exception to 06-02's
no-`tsx` constraint, which governs verification checks, not Prisma's own seed hook.

Note: `postgresql@17` is keg-only, so `psql` / `createdb` are not on the default PATH. Prisma
does not need them (it uses its own driver), but if you invoke them directly, prefix with
`export PATH="/opt/homebrew/opt/postgresql@17/bin:$PATH"`.

**Path B — emergency fallback only, if the local server is somehow unreachable.** Check
`brew services list | grep postgres` first and start it (`brew services start postgresql@17`)
before resorting to this. Path B is expected NOT to be used; taking it means 06-08 cannot run
its end-to-end validation, so exhaust Path A first.
Do NOT fall back to
`prisma migrate dev --create-only`: that still requires a reachable database AND a shadow
database, so it fails for the same reason. Use the fully offline datamodel-to-datamodel diff:
```
git show HEAD:prisma/schema.prisma > /tmp/schema-before.prisma
npx prisma migrate diff \
  --from-schema-datamodel /tmp/schema-before.prisma \
  --to-schema-datamodel prisma/schema.prisma \
  --script > /tmp/add_interview_report.sql
```
Then hand-create `prisma/migrations/<UTC timestamp>_add_interview_report/migration.sql`
(timestamp format `YYYYMMDDHHMMSS`, matching the existing folders in `prisma/migrations/`)
and move the SQL into it. Review the SQL by eye: it must contain exactly one `CREATE TYPE`
for the enum, one `CREATE TABLE "InterviewReport"`, two `CREATE INDEX`, and one
`ALTER TABLE ... ADD CONSTRAINT ... FOREIGN KEY` — and nothing touching any other table.

State plainly in the SUMMARY which path was taken, which host (if any) the migration was
applied to (expected: `localhost:5432/leadership_avatar_dev`, never the shared RDS), and — if Path B —
that the phase cannot be end-to-end validated (plan 06-08) until a database actually has the table.

Also record the handoff path for the team, because it is not manual: once the migration folder is
committed, `npm run setup` runs `npx prisma migrate deploy` against the shared `DATABASE_URL`
(`scripts/setup.mjs:211`), so any teammate who runs setup after pulling this branch applies
`add_interview_report` to the shared database. That is the intended route. This plan does not
apply it; the teammate's next `npm run setup` does. 06-08 restates this in the phase SUMMARY.

Either way, finish with `npx prisma generate`. That is schema-only and needs no database, so
the Prisma client types are available for every downstream plan under both paths.

Record the migration directory path in the SUMMARY so the team can review the SQL.
  </action>
  <verify>
`npx prisma validate` exits 0.
Note: this plan runs in wave 1 alongside 06-02. `npx tsc --noEmit` is repo-wide, so a transient
error originating in `lib/interview/evaluation.ts` while 06-02 is mid-edit is expected and is not
this plan's failure — judge only this plan's files, and treat only the final end-of-wave state as
authoritative. Do not run `npm run lint`: it is `eslint --fix` across the whole repo and would
rewrite 06-02's in-flight file. Lint is REQUIRED for this plan's files, scoped:
`npx eslint --fix lib/interview/transcript.ts lib/s3-client.ts` — run it, do not treat it as
optional. `lib/s3-client.ts` is reached by no other plan's lint scope, so if this does not run it
is never linted until 06-08's no-`--fix` final gate, which cannot repair anything.

HARD ENV GATE — run this BOTH immediately before AND immediately after the migrate command:
`grep -h "^DATABASE_URL" .env .env.local | grep -c "localhost\|127.0.0.1"` must return **0**
both times. Anything else: STOP the plan and report it. Do not edit, restore, or back up either
file, and do not touch `~/.casebridge-env-backups/`. Record both readings in the SUMMARY.

`grep -n '"remark-gfm"' package.json` is non-empty — this plan installed it (step 0).
`ls prisma/migrations/*add_interview_report*/migration.sql` shows the file.
`grep -c "cohort\|caseId\|assignment\|attemptNumber" prisma/migrations/*add_interview_report*/migration.sql` returns 0.
`grep -n "InterviewReport" node_modules/.prisma/client/index.d.ts | head -1` is non-empty (client regenerated).
  </verify>
  <done>`InterviewReport` + `InterviewReportStatus` exist in schema.prisma, `User.interviewReports` relation exists, migration SQL file exists on disk, Prisma client types include `InterviewReport`, and no shared database was migrated.</done>
</task>

<task type="auto">
  <name>Task 2: Define the InterviewTranscript type and formatter</name>
  <files>lib/interview/transcript.ts</files>
  <action>
Create `lib/interview/transcript.ts`. Purpose-built for a single-interviewer interview —
do NOT import or extend `InteractionLog`.

Export:

```ts
export interface InterviewTranscriptTurn {
  role: "user" | "assistant";
  content: string;
  /** epoch ms */
  timestamp: number;
}

export interface InterviewTranscript {
  /** Schema version so later phases can migrate stored objects. */
  version: 1;
  reportId: string;
  userId: string;
  typeSlug: string;
  interviewer: { avatarId: string | null; name: string | null };
  startedAt: number;
  updatedAt: number;
  turns: InterviewTranscriptTurn[];
  progress: InterviewProgress;   // import type from "./types"
}
```

Also export:
- `buildInterviewTranscript(input: Omit<InterviewTranscript, "version" | "updatedAt">): InterviewTranscript`
  — stamps `version: 1` and `updatedAt: Date.now()`.
- `normalizeTurns(raw: unknown): InterviewTranscriptTurn[]` — defensive parse for turns
  arriving over HTTP from the browser. Drops anything whose `role` is not `"user"` or
  `"assistant"` or whose `content` is not a non-empty string after trim. Coerces a missing
  or non-finite `timestamp` to `Date.now()`. Truncates any single `content` to 20000 chars.
  Caps the array at 400 turns (keep the FIRST 400 — the opening matters for grading).
- `formatTranscriptForEvaluator(transcript: InterviewTranscript): string` — speaker-labeled
  plain text, one block per turn, `Interviewer:` for `assistant` and `Candidate:` for `user`,
  blank line between turns. This is what the evaluator's `full_transcript` input receives.

Full TSDoc on the module explaining why `InteractionLog` is not reused.
  </action>
  <verify>`npx tsc --noEmit` exits 0. `grep -c "InteractionLog" lib/interview/transcript.ts` returns 0.</verify>
  <done>`lib/interview/transcript.ts` exports `InterviewTranscript`, `InterviewTranscriptTurn`, `buildInterviewTranscript`, `normalizeTurns`, and `formatTranscriptForEvaluator`; typecheck is clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add transcript read/write to S3AvatarStorage</name>
  <files>lib/s3-client.ts</files>
  <action>
In `lib/s3-client.ts`:

1. Add a prefix constant next to the existing ones near the top of the file (where
   `INTERVIEW_RESUMES_PREFIX` is declared):
   `const INTERVIEW_TRANSCRIPTS_PREFIX = "interviews/";`

2. Add two methods to the `S3AvatarStorage` class, placed directly after the existing
   `saveInterviewResume` method so the interview storage code stays together:

```ts
async saveInterviewTranscript(
  userId: string,
  reportId: string,
  transcript: InterviewTranscript
): Promise<string>
```
Builds the key exactly like `saveInterviewResume` does — via the private
`this.sanitizePathSegment(userId, "userId")` and `this.sanitizePathSegment(reportId, "reportId")` —
as `${INTERVIEW_TRANSCRIPTS_PREFIX}${safeUserId}/${safeReportId}.json`. `PutObjectCommand`
with `ContentType: "application/json"`, body `JSON.stringify(transcript)`. Returns the key.

```ts
async getInterviewTranscript(
  userId: string,
  reportId: string
): Promise<InterviewTranscript | null>
```
Same key derivation, `GetObjectCommand`, parse the body. Returns `null` on `NoSuchKey`
(check `error.name === "NoSuchKey"` or `$metadata.httpStatusCode === 404`), rethrows anything else.

3. Import the type: `import type { InterviewTranscript } from "./interview/transcript";`
   alongside the other type imports at the top.

Do not gzip — these objects are small and the existing resume path does not compress either.
Note in a comment that keys are server-derived and a client-supplied reportId must be
validated as a UUID by the caller before reaching here.
  </action>
  <verify>
`npx tsc --noEmit` exits 0.
`grep -n "INTERVIEW_TRANSCRIPTS_PREFIX\|saveInterviewTranscript\|getInterviewTranscript" lib/s3-client.ts` shows all three.
`grep -n "sanitizePathSegment" lib/s3-client.ts` shows the new methods calling it for both userId and reportId.
  </verify>
  <done>`s3Storage.saveInterviewTranscript` and `s3Storage.getInterviewTranscript` exist, derive keys through `sanitizePathSegment`, write under `interviews/{userId}/{reportId}.json`, and typecheck is clean.</done>
</task>

</tasks>

<verification>
- `npx prisma validate` passes and `npx tsc --noEmit` passes for this plan's files (repo-wide run;
  transient errors from 06-02's in-flight edits mid-wave are expected — only the final wave-1
  state is authoritative).
- Lint IS run (required, not optional) and is SCOPED:
  `npx eslint --fix lib/interview/transcript.ts lib/s3-client.ts`.
  Never `npm run lint`, which is repo-wide `eslint --fix`. `lib/s3-client.ts` has no other lint
  gate in this phase before 06-08's read-only sweep, so this is its only chance to be fixed.
- Migration SQL exists at `prisma/migrations/*add_interview_report*/migration.sql`.
- No shared database was migrated — confirm in the SUMMARY which `DATABASE_URL` was used
  (expected `postgresql://ajabreu79@localhost:5432/leadership_avatar_dev`, passed inline) and whether
  the migration was applied or only generated.
- `grep -h "^DATABASE_URL" .env .env.local | grep -c "localhost\|127.0.0.1"` returns 0, checked
  before and after the migrate command. Neither file was edited, restored, or backed up by this
  plan, and `~/.casebridge-env-backups/` was not touched.
- `remark-gfm` is in `package.json`; `git diff --stat package.json` shows that addition and
  nothing else.
- `grep -rn "cohortId\|CaseAssignment" prisma/schema.prisma` shows only pre-existing models,
  nothing inside `InterviewReport`.

Note: do NOT run `next build` to verify. `next build` currently fails prerendering `/about`
because `EDGE_CONFIG` is unset. That failure is unrelated to this phase and out of scope.
Use `npx tsc --noEmit` plus `npx eslint` SCOPED to this plan's own files (never repo-wide
`npm run lint`, which is `eslint --fix` across the whole repo). instead.
</verification>

<success_criteria>
- `InterviewReport` model exists with nullable `visualScore`/`vocalScore`, four-value status enum, `User` relation, and `(userId, createdAt)` + `(userId, typeSlug)` indexes.
- No cohort, assignment, attempt-number, or staff-visibility field anywhere in the new model.
- Migration SQL is on disk for team review; the shared DB is untouched by this plan, and the
  SUMMARY names `npm run setup` as the handoff path that will apply it for teammates.
- `.env` / `.env.local` are unedited and the localhost gate returned 0 before and after migrate.
- `remark-gfm` is installed here, in wave 1, and no other plan in this phase runs `npm install`.
- `InterviewTranscript` is purpose-built; `InteractionLog` is not referenced.
- S3 transcript write/read exist with server-derived, sanitized keys.
</success_criteria>

<output>
After completion, create `.planning/phases/06-interview-evaluation-and-report/06-01-SUMMARY.md`.
Include the migration directory path and an explicit statement of which database (if any)
the migration was applied to.
</output>
