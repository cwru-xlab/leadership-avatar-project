---
phase: 06-interview-evaluation-and-report
plan: 08
type: execute
wave: 4
depends_on: ["06-06", "06-07"]
files_modified:
  - .planning/phases/06-interview-evaluation-and-report/06-08-SUMMARY.md
autonomous: false
requirements: [REQ-02, REQ-07, REQ-08, REQ-09, REQ-10]

must_haves:
  truths:
    - "A real authenticated LiveAvatar interview with a real PDF produces a READY report end to end."
    - "Transcript checkpoints are visible in S3 while the interview is still running."
    - "A second authenticated student requesting that reportId gets 404."
    - "Visual and Vocal show 'Not yet measured' while Content and Behavioral show scores."
    - "A failed evaluation renders a readable reason with a working retry, and the transcript is still in S3."
    - "The migration SQL is on disk for team review, was applied only to the local leadership_avatar_dev database, and was never applied to the shared RDS."
  artifacts:
    - path: ".planning/phases/06-interview-evaluation-and-report/06-08-SUMMARY.md"
      provides: "Signed-off validation checklist results and the migration handoff note"
  key_links:
    - from: "the live session"
      to: "S3 interviews/ prefix"
      via: "mid-interview checkpoint writes observable before the interview ends"
      pattern: "interviews/"
---

<objective>
Run the phase's integration and validation checklist against a real authenticated LiveAvatar
session with a real PDF, and confirm the shared database was never migrated.

Purpose: Every prior plan was verified in isolation. Only a real session proves the transcript
actually survives, the evaluator actually returns scores, and the ownership 404 actually holds.
Output: a signed-off checklist in the SUMMARY plus the migration SQL handoff note for the team.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-interview-evaluation-and-report/06-CONTEXT.md
@.planning/phases/06-interview-evaluation-and-report/06-01-SUMMARY.md
@.planning/phases/06-interview-evaluation-and-report/06-06-SUMMARY.md
@.planning/phases/06-interview-evaluation-and-report/06-07-SUMMARY.md
</context>

<constraints>
**HARD CONSTRAINTS:**
1. PREREQUISITE: Task 2 needs a database that actually HAS the `InterviewReport` table, plus a
   real `User` row to sign in as. That database is the local PostgreSQL 17 install:
   `postgresql://ajabreu79@localhost:5432/leadership_avatar_dev` (Homebrew `postgresql@17` under
   `brew services`). It is already installed and running, and plan 06-01 applies the migration
   to it. This is the confirmed migration + validation target — do not treat it as optional.

   Run the dev server against that local URL for this plan, NOT the shared database:
   ```
   DATABASE_URL="postgresql://ajabreu79@localhost:5432/leadership_avatar_dev" npm run dev
   ```
   Do NOT edit `.env` or `.env.local` to point at the local database. Both files must stay
   pointed at the shared RDS — `scripts/setup.mjs` asserts they agree, and editing them would
   break that check. Pass the local URL inline on the command, every time.

   If 06-01 somehow fell back to its offline Path B (SQL generated, never applied), stop and say
   so — do not apply the migration to the shared database to unblock this plan.
   SIGNING IN: this app has NO signup/registration flow — `app/api/auth/` contains only
   `login`, `logout`, `me`, `cwru-sso-callback`, `kiosk-auto-login`. Do not go looking for one.
   Dev accounts come from `prisma/seed.ts` (declared as `prisma.seed` in package.json). Seed the
   LOCAL database only, with the URL inline:
   ```
   DATABASE_URL="postgresql://ajabreu79@localhost:5432/leadership_avatar_dev" npx prisma db seed
   ```
   Then log in at `/login` with `alice.johnson@case.edu` / `student123` for the main run, and
   `bob.williams@case.edu` / `student123` as the SECOND student for step 9's cross-student 404
   test. (Email/password login works because `npm run dev` sets NODE_ENV=development;
   `authenticateUser` disables it in production.)

   NEVER run `npm run setup` or `npm run setup -- --seed` in this phase. `scripts/setup.mjs:211`
   runs `npx prisma migrate deploy` against the SHARED `DATABASE_URL` from `.env` before it
   seeds — that would apply `add_interview_report` to the shared RDS and destroy REQ-02 and
   success criterion 6. The seed command above is the only seeding path permitted here.
2. Do NOT apply the migration to a shared database during this plan. Verification is read-only.
   No `prisma migrate deploy`, no `db push`, no `migrate reset` — against any host.
3. Do NOT attempt to fix the `/about` prerender failure. `next build` fails because `EDGE_CONFIG`
   is unset; that is a pre-existing, unrelated blocker and is explicitly out of scope. Do not
   spend time on it and do not gate this plan on a clean `next build`.
4. If the checklist fails, record the failure precisely in the SUMMARY. Do not patch production
   code inside this plan beyond a one-line obvious fix — a real defect becomes a gap-closure plan.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Static sweep and migration handoff note</name>
  <files>.planning/phases/06-interview-evaluation-and-report/06-08-SUMMARY.md</files>
  <action>
Run the full static gate and collect the evidence:

1. `npx tsc --noEmit` — must exit 0.
2. `npx eslint app/api/interview app/interview components/interview lib/interview lib/s3-client.ts` — must report
   no errors. Note this is `npx eslint` WITHOUT `--fix`, not `npm run lint` (which is
   `eslint --fix` and would silently rewrite the code this gate is supposed to be judging).
   This plan runs alone in wave 4, so a broad sweep is safe here — the earlier plans deliberately
   scoped their lint to their own files to avoid clobbering same-wave siblings.
3. `npx prisma validate` — must exit 0.
4. Constraint sweep, each must return zero matches:
   - `grep -rn "cohortId\|CaseAssignment\|attemptNumber" prisma/migrations/*add_interview_report*/`
   - `grep -rn "403" app/api/interview/session/ app/api/interview/report/`
     (scoped to routes THIS phase creates. A repo-wide sweep of `app/api/interview/`
     returns 2 pre-existing hits in `interviewers/route.ts:83,87` — upstream LiveAvatar
     status passthrough shipped in Phase 2, NOT ownership 403s. Do not "fix" that file.)
   - `grep -rn "findUnique" app/api/interview/`
   - `grep -rn "beforeunload\|sendBeacon" components/interview/ app/interview/`
   - `grep -rn "rehype-raw\|dangerouslySetInnerHTML" components/interview/ app/interview/`
5. `git diff HEAD -- lib/interview/prompts.ts` — must be empty (0 lines). `INTERVIEW_EVALUATOR_PROMPT`
   was not rewritten by this phase.
   Use `HEAD`, NOT `main` and NOT an older commit. The whole interview feature is branch-only and
   does not exist on `main`, so `git diff main -- lib/interview/prompts.ts` is ~314 lines for a
   reason that has nothing to do with this phase and would fail the gate falsely. Diffing against
   `HEAD` is the assertion that actually matters here: nobody modified the evaluator prompt while
   executing Phase 6. If prompts.ts was legitimately committed mid-phase, also run
   `git log --oneline 8cfdc92..HEAD -- lib/interview/prompts.ts` and confirm it is empty.
6. Env-safety gate: `grep -h "^DATABASE_URL" .env .env.local | grep -c "localhost\|127.0.0.1"`
   must return **0**. Both files must still point at the shared RDS — no plan in this phase was
   allowed to edit them, and every local database command carried the URL inline instead. If this
   returns anything other than 0, record it as a FAIL in the SUMMARY and STOP. Do NOT edit,
   restore, or back up `.env` or `.env.local`, and do not touch `~/.casebridge-env-backups/`;
   what happens to those files is the user's decision, not this plan's.
7. Migration handoff: print the migration SQL
   (`cat prisma/migrations/*add_interview_report*/migration.sql`) and record in the SUMMARY:
   the migration directory name, the full SQL, and which `DATABASE_URL` host it was applied to
   (expected: `localhost:5432/leadership_avatar_dev` only).

   Then write the handoff note in full — not just the negative half. It must say BOTH of these:
   - **"NOT applied to the shared database by this phase."** Nobody ran `migrate deploy` or
     `db push` against the shared RDS.
   - **"The committed migration folder IS the handoff path."** `npm run setup` runs
     `npx prisma migrate deploy` against the shared `DATABASE_URL` (`scripts/setup.mjs:211`), so
     any teammate who runs `npm run setup` after pulling this branch will apply
     `add_interview_report` to the shared database automatically. That is intended, not an
     accident — call it out explicitly so the team knows the migration lands on their next setup
     run and can review the SQL beforehand.

Write the results into `06-08-SUMMARY.md` as a checklist with pass/fail per item. Do not mark
anything pass on assumption; paste the actual command output.
  </action>
  <verify>All seven gates run and their real output is recorded in the SUMMARY; every constraint sweep returns zero matches; the env-safety gate returns 0; the migration SQL and its handoff note are in the SUMMARY.</verify>
  <done>Typecheck, lint (including `lib/s3-client.ts`), and prisma validate pass; no constraint violations; the localhost grep across `.env` and `.env.local` returns 0; migration SQL captured with an explicit not-applied-to-shared-DB statement.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end validation with a real session</name>
  <files>.planning/phases/06-interview-evaluation-and-report/06-08-SUMMARY.md</files>
  <action>
Pause and ask the user to run the 11-step end-to-end checklist below against a real
authenticated LiveAvatar session with a real PDF resume. Make sure the dev server is already running against the LOCAL database
(`DATABASE_URL="postgresql://ajabreu79@localhost:5432/leadership_avatar_dev" npm run dev`) before handing over. Record their reported outcome, step by step, in the SUMMARY.
  </action>
  <verify>The user reports the result of all 11 steps. Every failure is recorded verbatim in the SUMMARY with its step number.</verify>
  <done>The user has typed "approved", or the failing steps are documented precisely enough to seed a gap-closure plan.</done>
  <what-built>
The full Phase 6 path: report row created on the first real turn, transcript checkpointed to S3
after every assistant turn, End-interview confirm modal → finish endpoint → background
evaluation → polling report page with rubric score cards and the markdown body, plus owner-only
404 enforcement and a retryable FAILED state.
  </what-built>
  <how-to-verify>
Start the dev server against the local database
(`DATABASE_URL="postgresql://ajabreu79@localhost:5432/leadership_avatar_dev" npm run dev`) and sign in as a real student account. Do NOT use
`next build` — it fails on `/about` for an unrelated missing `EDGE_CONFIG`.

Work through the checklist from 06-CONTEXT.md `<validation>`:

1. Go to `/interview/general`. Confirm the interviewer list loads from the live LiveAvatar catalog.
2. Upload a real PDF resume. Confirm the extracted text preview appears and that the response in
   the Network tab contains only `{resumeId, resumeText}` — no S3 URL.
3. Start the interview. Confirm the avatar speaks in streamed chunks (speech begins before the
   full reply has finished generating).
4. After 2-3 exchanges, WITHOUT ending the interview, check S3 for
   `interviews/{yourUserId}/{reportId}.json` and confirm the turns so far are in it. Get the
   reportId from the POST body of `/api/interview/session/checkpoint` in the Network tab.
5. Answer at least 4 questions, then press the End (stop) button. Confirm:
   - a confirm modal appears saying the report will be generated and you can't resume,
   - "Keep going" returns you to the live interview,
   - pressing End again and confirming returns a `reportId` and navigates you to
     `/interview/general/report/{reportId}`.
6. On the report page confirm: the skeleton shows the real layout, the status line reads
   "Reviewing your interview…", and the report appears WITHOUT you reloading the page. Confirm
   the layout does not jump when it lands.
7. Confirm Visual & Environment and Vocal Delivery read "Not yet measured" (greyed), while
   Content and Behavioral show `N / 5` with a word label. Confirm there is no overall/average
   score anywhere. Confirm the Category Breakdown renders as a real table with borders.
8. Separately test the Leave exit: start another interview, press the top-left back arrow, and
   confirm the modal says "Leave without a report" — visibly different from the End modal.
9. Sign in as a SECOND student account (different browser profile or incognito) and request
   `/interview/general/report/{reportId}` from step 5, and also
   `curl` the API directly with that account's cookie. Confirm **404**, and that the response body
   is identical to requesting a made-up reportId.
10. Confirm nothing on the report page exposes the resume PDF or an S3 URL.
11. Failure path (this is the only end-to-end evidence for REQ-07 and success criterion 5, so do
    not skip it). Restart dev with
    `DATABASE_URL="postgresql://ajabreu79@localhost:5432/leadership_avatar_dev" INTERVIEW_EVAL_MODEL=definitely-not-a-model npm run dev`.
    Run a short interview and End it. Confirm the report page reaches FAILED, shows a readable
    `failureReason` and the line "Your interview transcript is saved — nothing was lost", and that
    the transcript JSON is still in S3. Then restart with the default model, press Try again, and
    confirm the same reportId flips to READY with no new row created.

Report any step that fails with the step number and what you saw.
  </how-to-verify>
  <resume-signal>Type "approved" if all 11 steps pass, or list the failing step numbers and what you observed.</resume-signal>
</task>

</tasks>

<verification>
- Static gate output recorded in the SUMMARY with real command output.
- All 11 checklist steps confirmed by the user, or failures recorded verbatim.
- The migration SQL is captured in the SUMMARY with an explicit statement that the shared
  database was not migrated.

Known unrelated blocker, restated so no one chases it: `next build` fails prerendering `/about`
because `EDGE_CONFIG` is unset. Not caused by this phase, not fixed here.
</verification>

<success_criteria>
- A real interview produces a READY report the student reads without reloading.
- Checkpoints are observable in S3 mid-interview.
- Cross-student access returns an indistinguishable 404.
- Visual and Vocal are honestly unmeasured.
- A forced evaluation failure is visible, readable, and retryable, and the transcript survives it.
- The shared database is untouched and the team has the SQL.
</success_criteria>

<output>
After completion, create `.planning/phases/06-interview-evaluation-and-report/06-08-SUMMARY.md`
containing the static gate results, the 11-step checklist outcomes, the migration SQL, and any
defects found (each written up clearly enough to become a gap-closure plan).
</output>
