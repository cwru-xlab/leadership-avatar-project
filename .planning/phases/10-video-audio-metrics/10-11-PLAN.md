---
phase: 10-video-audio-metrics
plan: 11
type: execute
wave: 6
depends_on: ["10-01", "10-02", "10-03", "10-04", "10-05", "10-06", "10-07", "10-08", "10-09", "10-10"]
files_modified: []
autonomous: false
requirements: [REQ-35, REQ-36, REQ-37, REQ-38, REQ-39, REQ-40, REQ-41, REQ-42, REQ-43, REQ-44, REQ-45, REQ-46, REQ-47, REQ-48, REQ-49]

must_haves:
  truths:
    - "Every Phase 10 constraint is proven by a recorded static check, not asserted."
    - "A human has confirmed on real hardware that the camera, microphone, banner, self-view and both report types behave as decided."
    - "Eye contact, framing, speech rate and filler counts are demonstrably measured, not estimated (ROADMAP criterion 1)."
    - "A report generated before this phase still renders correctly with null scores (ROADMAP criterion 2)."
    - "Poor performance and technical failure produce visibly different reports for the same null score."
  artifacts:
    - path: ".planning/phases/10-video-audio-metrics/10-11-SUMMARY.md"
      provides: "verbatim per-check and per-step results plus the consolidated deferred-items list"
      min_lines: 120
  key_links:
    - from: "10-11-SUMMARY.md"
      to: ".planning/REQUIREMENTS.md"
      via: "each of REQ-35..REQ-49 ticked only after its behaviour was demonstrated"
      pattern: "REQ-4"
---

<objective>
Sweep Phase 10 for constraint violations statically, then walk the whole
camera/microphone path with the user on real hardware — the pattern Phases 7, 8
and 9 all used, and the only way to verify live camera behaviour, permission
dialogs, a folding banner and an avatar stream's smoothness, none of which an
agent can observe.

Purpose: this phase's two highest-risk properties are unverifiable by code
reading. First, that a camera-on session does not degrade the live avatar
(REQ-49) — that is a felt, hardware-dependent judgement. Second, that a student
who left frame is docked while a student whose camera died is not (REQ-41 vs
REQ-42) — both produce a null-ish-looking situation and only a side-by-side
human comparison proves they diverge.
Output: `10-11-SUMMARY.md` with verbatim results, plus ticked REQUIREMENTS
checkboxes and an updated STATE.md.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-video-audio-metrics/10-CONTEXT.md
@.planning/phases/10-video-audio-metrics/10-04-SUMMARY.md
@.planning/phases/10-video-audio-metrics/10-07-SUMMARY.md
@.planning/phases/10-video-audio-metrics/10-09-SUMMARY.md
@.planning/phases/10-video-audio-metrics/10-10-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static constraint sweep</name>
  <files>.planning/phases/10-video-audio-metrics/10-11-SUMMARY.md</files>
  <action>
First resolve the Phase 10 BASELINE correctly: the commit immediately BEFORE
10-01's first commit, NOT `main`. `main` predates Phases 1-9 and diffing against
it would misreport already-merged legitimate changes as Phase 10 violations —
the mistake 07-07 caught and 08-08/09-09 both repeated the fix for. Record the
resolved baseline SHA in the SUMMARY.

Run all 22 checks and record each result VERBATIM (command, output, PASS/FAIL).
Fix any FAIL before proceeding to Task 2.

1. `npx tsc --noEmit` — clean. AUTHORITATIVE. (`next build` is pre-existing
   broken on `/about`; eslint is pre-existing repo-wide broken — reconfirm that
   against an untouched file such as `lib/languages.ts` rather than assuming it.)
2. `npx prisma validate` — clean.
3. Exactly ONE new migration directory since the baseline; its SQL contains only
   `ALTER TABLE ... ADD COLUMN` with ZERO `NOT NULL`, zero `DROP`, zero
   `CREATE TABLE`.
4. `npm run setup` never run, and the shared `DATABASE_URL` never migrated:
   confirm via git history that `.env`/`.env.local` were never modified, and that
   every migrate command recorded across the plan SUMMARYs used the inline
   `postgresql://ajabreu79@localhost:5432/leadership_avatar_dev`.
5. **No media retention anywhere.** Repo-wide, excluding `node_modules`/`.next`:
   `grep -rn "MediaRecorder\|captureStream\|toDataURL\|toBlob" lib/metrics/ components/metrics/ app/api/metrics/ app/api/audio/word-metrics/`
   returns NOTHING. Separately confirm `components/HeyGenAvatar/InteractiveAvatar.tsx`
   is diff-empty against the baseline — its existing `MediaRecorder` path records
   the AVATAR's video and must not have been repurposed.
6. No student media reaches S3: `grep -rn "saveInterview\|s3Storage\|S3Client" lib/metrics/ components/metrics/ app/api/audio/word-metrics/` returns nothing.
7. **Zero remaining hardcoded force-to-null:**
   `grep -rn "visualScore: null\|vocalScore: null" lib/ app/` — every hit must be
   either a comment or a legacy-guard conditional expression, never an
   unconditional assignment. Read each hit.
8. `grep -rn "NOT MEASURABLE\|MUST always be null" lib/scenario/prompts.ts` returns nothing.
9. `lib/interview/prompts.ts`'s diff against the baseline is confined ENTIRELY
   to `INTERVIEW_EVALUATOR_PROMPT`'s template literal. List every hunk header
   verbatim. The live interviewer prompt must be byte-unchanged — the Phase 1
   criterion-2 prefix-cache guarantee. Note explicitly that the still-open
   Phase 8 question about reinforcing the interviewer persona inside this file
   was NOT acted on: it is a separate decision and was not authorized by this
   phase's relaxation.
10. Both JSON schemas unchanged: `git diff <baseline> -- lib/interview/evaluation.ts lib/scenario/evaluation.ts | grep "json_schema\|minimum:\|maximum:"` shows no schema-line changes.
11. `coerceScore` unmodified in BOTH evaluation modules — diff its body in each.
12. **The discriminator is not collapsible:** `grep -n "face_detected" lib/metrics/coverage.ts`
    — every hit is inside `isPoorVisualCoverage` or a comment; zero inside
    `resolveVisualOutcome`'s decision logic. Re-run plan 10-01's seven
    discriminator assertions and record them; do not take the earlier SUMMARY on trust.
13. No coverage threshold gates the Visual score anywhere:
    `grep -rn "coverage" components/interview/ReportScoreCards.tsx` — confirm
    coverage only ever produces DISPLAY text and never influences the rendered score.
14. Camera mode has exactly ONE write path:
    `grep -rn "cameraMode" app/api/` — hits only in the two session-START routes,
    never in a finish route, never in any `update` call.
15. `grep -rn "%\|toFixed" components/interview/ReportScoreCards.tsx` returns
    nothing (or only a recorded, justified className exception) — REQ-46's
    bands-not-figures rule.
16. `grep -c "Not yet measured" components/interview/ReportScoreCards.tsx` == 1.
17. The admin case path is untouched: `app/api/interaction/`, `app/api/case/`
    and `app/api/profile/` are all diff-empty against the baseline, and
    `app/case-play/[caseId]/page.tsx`'s legacy `cohortId` start/finish bodies are
    byte-unchanged in the diff.
18. `app/api/audio/transcribe/route.ts` diff-empty — the live push-to-talk STT
    route was not disturbed.
19. Only ONE `getUserMedia` occurrence remains in
    `components/interview/InterviewSessionShell.tsx` (the pre-existing audio-only
    call); the video request lives solely in `lib/metrics/visual-capture.ts`.
20. No cohort/assignment/staff regression: `grep -rn "cohortId\|isStaff\|instructor\|assignment"`
    across `lib/metrics/`, `components/metrics/`, `app/api/metrics/` returns nothing.
21. Consent is enforced server-side, not only in the dialog: confirm the
    force-to-OFF branch exists in BOTH session-start routes and exercise it once
    live against a user with null consent.
22. Pinned assets only: `grep -rn "@latest\|/latest/" lib/metrics/ package.json` returns nothing.
  </action>
  <verify>Every one of the 22 checks recorded verbatim in `10-11-SUMMARY.md` with an explicit PASS/FAIL; any FAIL fixed and re-run, with both the failure and the fix recorded.</verify>
  <done>All 22 static checks PASS against a correctly-resolved baseline, with verbatim evidence for each.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human end-to-end walkthrough on real hardware</name>
  <action>
Present the 24 steps below to the user one section at a time and record their
verbatim result for each. Do not proceed past a FAILED step — fix it under this
checkpoint and re-verify. Steps 13-15 are the phase's load-bearing comparison
and must be run as a pair, not summarised.
  </action>
  <verify>All 24 steps carry a human-confirmed PASS, or a recorded fix plus re-verification.</verify>
  <done>The user has typed "approved", or every issue they raised has been fixed and re-confirmed.</done>
  <what-built>
The complete Phase 10 video/audio metrics pipeline: a pre-session camera choice
locked for the run, one-time in-app consent, a permission block with a camera-off
escape, in-browser face analysis at 6 Hz that stores nothing, live self-view and
fold-away banner, word-timestamp STT plus RMS analysis for vocal metrics, real
Visual/Vocal scores with qualitative bands on BOTH interview and scenario
reports, and four distinguishable causes for an unscored category.
  </what-built>
  <how-to-verify>
Start a dev server on a FREE port with the inline local database URL — NOT port
3000 if anything is already running there, and do NOT `rm -rf .next` while
another dev server is live in this working directory (process finding,
`09-06-SUMMARY.md`):

`DATABASE_URL="postgresql://ajabreu79@localhost:5432/leadership_avatar_dev" npx next dev -p 3021`

Log in as a real seeded student whose `videoAnalysisConsentAt` is NULL (plan
10-05 reset it; confirm before starting).

**Consent and camera choice**
1. Start an interview from `/interview`. Reach the new camera step. Confirm it
   states plainly that the choice is locked once the interview begins.
2. Choose camera-on. Confirm the consent dialog appears, is not dismissable, and
   says nothing is recorded or saved. Choose "Not now" — confirm you land on
   camera-off rather than a dead end, and that the session can still start.
3. Start again, choose camera-on, accept the consent. Confirm the browser's own
   permission prompt appears only AFTER the in-app consent, never before.
4. Start a third interview and confirm the consent dialog does NOT reappear —
   acceptance is remembered.

**The block path**
5. Deny the browser camera permission (or block the site in browser settings).
   Confirm you are BLOCKED with a clear explanation, that "Try again" exists,
   and that "Continue with my camera off" works and starts the session.
6. Re-allow the permission. Open the camera in another app (e.g. Photo Booth or
   another browser tab's video call) and try to start camera-on. Confirm you get
   the "in use by another app" explanation, not a silent downgrade.

**A real camera-on interview**
7. Run a real LiveAvatar interview with the camera on. Answer at least 3-4
   questions by voice. Judge: does the avatar video look as smooth as it does
   with the camera off? Does push-to-talk feel as responsive? Is the interviewer
   as quick to reply? A noticeable degradation is a REQ-49 FAILURE — say so.
8. Confirm the self-view thumbnail shows YOUR video (mirrored), is muted (no
   feedback/echo), and that clicking through it still hits the buttons beneath.
9. Cover the camera or look far away for ~5 seconds. Confirm a small banner
   appears, does NOT block or obscure the conversation, and FOLDS AWAY (animates
   out, not just vanishes) when your face comes back.
10. Confirm nothing anywhere shows you a live score, percentage or coaching
    nudge during the session.
11. End the interview. Confirm the report arrives and the Visual and Vocal cards
    each show a 1-5 score with plain-word bands ("Eye contact: Strong",
    "Pace: Well paced"), NO raw percentages, and that these sit INSIDE the two
    existing cards with no new report section.
12. Sanity-check the numbers against what you actually did: if you spoke quickly
    the pace band should say so; if you said "um" a lot the filler band should
    say so. A band that contradicts your own experience means the metric is not
    really measured — flag it.

**Docked vs not-scored — the phase's hardest distinction**
13. Run a short camera-on interview where you deliberately stay OUT of frame for
    most of it. Confirm Visual gets a LOW SCORE (a number, not a blank), and
    that the card discloses your face wasn't visible for much of the session.
14. Run another camera-on interview and part-way through, physically unplug or
    hard-disable the camera (or revoke the permission in browser settings
    mid-session). Confirm the report says "Insufficient data" with a technical
    explanation — NOT a low score, and NOT the camera-off copy.
15. Put reports 13 and 14 side by side. They must read as clearly different
    situations. If they look the same, REQ-42 has failed.

**Camera-off and typed**
16. Run a camera-off interview. Confirm no permission prompt, no self-view, no
    banner, and that the report's Visual and Vocal cards say you practised with
    your camera off — not "Not yet measured" and not "Insufficient data".
17. Run a camera-on interview answering entirely by TYPING. Confirm Visual
    scores normally while Vocal says you typed your answers, with no penalty
    language.

**Scenarios**
18. From `/case-play`, start one of your own scenarios. Confirm the same camera
    choice appears on the intro screen and the same consent/block behaviour
    applies.
19. Run it camera-on with at least one avatar character, mixing SPOKEN and TYPED
    answers (switch the Text/Avatar toggle mid-run). Finish. Confirm the scenario
    report shows real Visual AND Vocal scores — Vocal must be scored, not
    typed-only, since you did speak.
20. Open a seeded ADMIN case study (e.g. `testing`). Confirm there is NO camera
    choice, the run behaves exactly as before, and it finishes through its old
    pipeline without error.

**Legacy reports (ROADMAP criterion 2)**
21. Open an EXISTING pre-Phase-10 interview report from `/reports`. Confirm
    Visual and Vocal still read "Not yet measured" with "Requires video and audio
    analysis", and the page is otherwise unchanged.
22. Open an existing pre-Phase-10 scenario report. Confirm the same.

**Privacy**
23. Leave (don't End) a camera-on session. Confirm your camera indicator light
    goes OUT immediately.
24. Confirm nothing about the session is downloadable or replayable anywhere —
    no playback control, no saved video, no recording in your reports list.
  </how-to-verify>
  <resume-signal>Type "approved" to sign off Phase 10, or describe any step that failed or felt wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record results and update project state</name>
  <files>.planning/phases/10-video-audio-metrics/10-11-SUMMARY.md</files>
  <action>
Only after the checkpoint is approved.

If the walkthrough found a real defect, fix it under the checkpoint (the
established pattern — 07-07, 08-08 and 09-09 each found and fixed exactly one
real bug this way) and record both the defect and the fix commit. Re-run any
static check the fix could have invalidated; do NOT take the earlier PASS on
trust, matching 08-08's independent re-verification discipline.

Write `10-11-SUMMARY.md` with: the resolved baseline SHA, all 22 static checks
verbatim, all 24 walkthrough steps with their human-confirmed results, the
measured latency and band figures carried forward from `10-04-SUMMARY.md`, and a
consolidated deferred-items list — including at least the known carry-forwards:
- Resumed scenario runs via "Unfinished Sessions" take the legacy finish path and
  therefore produce no metrics (`handleResume` never repopulates
  `scenarioReportId`) — pre-existing from 09-07, inherited not introduced.
- The fourth unapplied migration now sits in the handoff queue; `npm run setup`
  remains the intended path for the shared database.
- Pre-existing repo-wide broken eslint and the `/about` `next build` breakage.
- The six uncommitted CaseBridge-rename files still in the working tree.
- Whether to reinforce the interviewer persona inside `lib/interview/prompts.ts`
  — still the user's call, explicitly NOT decided by this phase.
- If plan 10-04's measurement triggered the `metricsStatus` fallback, carry its
  gap-plan sketch forward here as an explicit follow-up.

Tick REQ-35 through REQ-49 in `.planning/REQUIREMENTS.md`, but ONLY those whose
full user-facing behaviour was actually demonstrated in the walkthrough. Follow
the 09-01/09-02/09-03 precedent recorded in STATE.md: a requirement split across
plans stays unchecked until the behaviour is real end to end. Name any
requirement left unchecked and why.

Update `.planning/STATE.md`: add a "Phase 10 Status: COMPLETE" section in the
same shape as the Phase 7/8/9 sections, add per-plan Progress entries, record the
phase's durable decisions under `## Decisions` (at minimum: the liveness-vs-
detection discriminator design; metrics computed in-flight and delivered at
finish so the single-`status` polling model survives; camera mode written once
at row creation as REQ-35's structural lock; turn-level vocal accounting with a
session-level verdict as the Q4 resolution; self-hosted pinned MediaPipe assets;
the closed posture-flag vocabulary and why `slouching`/`fidgeting` are not
measured), and rewrite `## Next` to point at Phase 11 (Cohort & Staff Teardown).
Note that "Visual/Vocal stay 'Not yet measured' until Phase 10 lands" is now
resolved and should be removed from the carried-forward list.

Mark Phase 10 complete in `.planning/ROADMAP.md` (the `- [ ]` at line 33 and its
plan checkboxes).
  </action>
  <verify>
`10-11-SUMMARY.md` exists with all 22 checks and all 24 steps recorded verbatim.
`grep -c "REQ-3[5-9]\|REQ-4[0-9]" .planning/REQUIREMENTS.md` confirms all fifteen
IDs are present, with each checkbox state matching what the SUMMARY claims was
demonstrated. STATE.md has a Phase 10 COMPLETE section, per-plan Progress
entries, new Decisions entries, and a `## Next` pointing at Phase 11. ROADMAP
Phase 10 checkboxes ticked.
  </verify>
  <done>Phase 10 is signed off with verbatim evidence, requirements ticked only where demonstrated, and STATE.md/ROADMAP.md accurately reflecting a complete phase and the next one.</done>
</task>

</tasks>

<verification>
- All 22 static checks PASS against a correctly-resolved baseline (the commit before 10-01's first commit, never `main`).
- All 24 walkthrough steps human-confirmed, with any defect fixed under the checkpoint and its fix independently re-verified.
- Both ROADMAP success criteria explicitly proven by named steps: criterion 1 by steps 11-13 and 19; criterion 2 by steps 21-22.
- REQ-35..REQ-49 ticked only where demonstrated; every exception named and justified.
- `npm run setup` never run at any point in this plan.
</verification>

<success_criteria>
Phase 10 is provably complete: eye contact, framing, speech rate and filler
counts are measured rather than estimated on both report types; a student who
hid from the camera is docked while a student whose camera failed is not; a
deliberate opt-out reads as a choice; and every report written before this phase
still reads exactly as it did.
</success_criteria>

<output>
After completion, create `.planning/phases/10-video-audio-metrics/10-11-SUMMARY.md`
</output>
