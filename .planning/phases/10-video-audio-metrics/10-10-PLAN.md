---
phase: 10-video-audio-metrics
plan: 10
type: execute
wave: 5
depends_on: ["10-03", "10-04", "10-05", "10-07"]
files_modified:
  - app/case-play/[caseId]/page.tsx
autonomous: true
requirements: [REQ-35, REQ-36, REQ-37, REQ-38, REQ-43, REQ-44, REQ-47, REQ-49]

must_haves:
  truths:
    - "A student starting a scenario chooses camera-on or camera-off on the intro screen, before the run begins, and cannot change it during the run."
    - "Consent and the permission block behave exactly as they do in the interview flow, reusing the same components."
    - "A camera-on scenario run shows the self-view and the folding banner and delivers metrics on finish."
    - "A scenario report shows real Visual and Vocal scores."
    - "The admin case-study path is completely unaffected: no camera choice, no metrics, no behaviour change."
  artifacts:
    - path: "app/case-play/[caseId]/page.tsx"
      provides: "scenario-only camera-mode gate, capture lifecycle, live affordances, metrics on scenario finish"
      min_lines: 1900
  key_links:
    - from: "app/case-play/[caseId]/page.tsx"
      to: "/api/scenario/session/start"
      via: "cameraMode added to the existing scenario start body"
      pattern: "cameraMode"
    - from: "app/case-play/[caseId]/page.tsx"
      to: "/api/scenario/session/finish"
      via: "metrics added to the existing scenario finish body"
      pattern: "metrics:"
    - from: "app/case-play/[caseId]/page.tsx"
      to: "components/metrics/FaceDetectionBanner.tsx"
      via: "rendered during a camera-on scenario run"
      pattern: "FaceDetectionBanner"
---

<objective>
Bring the same camera-mode gate, consent flow, live affordances and metrics
delivery to the scenario player — and only to the scenario path, leaving the
admin case-study flow untouched.

Purpose: CONTEXT.md decided scenarios are in scope alongside interviews, which
is why Phase 10 effectively depends on Phase 9. This is the second of the two
live surfaces, and it is the riskier one: `app/case-play/[caseId]/page.tsx` is
~1865 lines serving BOTH student scenarios and admin cases through one component
with an `isScenario` discriminator, and it has its own mid-session Text/Avatar
toggle that the camera lock must not be confused with.
Output: one file, gated entirely behind `isScenario`.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-video-audio-metrics/10-CONTEXT.md
@.planning/phases/10-video-audio-metrics/10-03-SUMMARY.md
@.planning/phases/10-video-audio-metrics/10-04-SUMMARY.md
@.planning/phases/10-video-audio-metrics/10-05-SUMMARY.md
@.planning/phases/10-video-audio-metrics/10-07-SUMMARY.md
@.planning/phases/10-video-audio-metrics/10-09-SUMMARY.md

@app/case-play/[caseId]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Camera-mode choice on the scenario intro screen</name>
  <files>app/case-play/[caseId]/page.tsx</files>
  <action>
**Concurrency hazard:** BRACKETED path, and plan 10-09 is in the SAME wave
touching `app/interview/[type]/page.tsx`. A `git add` on a bracketed pathspec
glob-matches a sibling's staged files (`08-08-SUMMARY.md`; recurred in Phase 9).
Stage with LITERAL, quoted pathspecs and verify EVERY commit with
`git show --name-only` before continuing. If a commit absorbs a foreign file,
`git reset HEAD~1` and redo — non-destructive, as 08-06 did.

New state alongside the existing `isScenario` (`:88`) and `scenarioReportId`:
`const [cameraMode, setCameraMode] = useState<CameraMode>("OFF")`, plus consent
and block state mirroring plan 10-09's wizard.

In the `pageState === "intro"` branch (`:1241+`), inside the existing
`{isScenario ? (...)}` region at `:1389` — **never in the shared or
admin-case-only markup** — render the same two-option camera choice and
locked-for-the-session copy that plan 10-09 built for the wizard. Reuse
`MetricsConsentDialog` and `requestCameraStream` from this phase's shared
modules; do not write a second consent dialog or a second permission-probe
implementation. Stop the probe's tracks immediately, exactly as the wizard does.

Wire it into `handleStart` (`:444`). Inside the `if (isScenario)` branch at
`:447` ONLY:
- Before the `fetch("/api/scenario/session/start")` at `:452`, run the consent
  check and the camera probe. On a probe failure, do NOT start the run: render
  the same block panel with "Try again" and "Continue with my camera off", which
  sets `cameraMode: "OFF"` and re-invokes the start. Blocking BEFORE the run
  begins is the only place a block is acceptable (REQ-37) — once the run is
  live, degrade with a toast instead, as 10-09 does.
- Add `cameraMode` to the existing scenario start request body (which currently
  sends `{caseId, language}`). Add nothing else. The server validates it and
  independently forces OFF without consent (plan 10-05).
- The legacy `else` branch (admin case, `/api/interaction/start` with its
  `cohortId`) must be BYTE-UNCHANGED. Do not add `cameraMode` to it, do not
  hoist the probe above the branch, and do not touch the `handleStart("explore")`
  path — the "Explore System" button is already hidden for scenarios (09-07).

Do not touch: `isScenario`'s derivation, the `StartAvatarRequest` synthesis for
scenario avatars added by the 09-09 checkpoint fix, `profileId` resolution for
legacy admin cases, `handleResume`, or the avatar-minutes limit logic.
  </action>
  <verify>
`npx tsc --noEmit` clean.
`grep -n "cameraMode" app/case-play/\[caseId\]/page.tsx` — read EVERY hit and
confirm each sits inside an `isScenario` branch or scenario-only state; zero
hits in the admin-case start/finish bodies.
`git diff app/case-play/\[caseId\]/page.tsx | grep -n "cohortId"` returns
nothing — the legacy body is untouched.
`grep -n "interaction/start" app/case-play/\[caseId\]/page.tsx` — confirm that
call site's body is unchanged in the diff.
Live verification on a free port against the local dev DB with a real seeded
student and a real scenario: confirm the camera choice appears on a scenario's
intro screen and does NOT appear on the seeded admin case `testing`; confirm a
camera-on start persists `cameraMode: "ON"` on the `ScenarioReport` row;
confirm a denied permission blocks with a working camera-off continue; confirm
the admin case still starts and finishes through the legacy pipeline with its
`cohortId` echoed back (the 09-07 regression check).
  </verify>
  <done>Scenario runs offer a pre-run camera choice gated by consent and a permission probe; the admin case path shows nothing new and behaves identically.</done>
</task>

<task type="auto">
  <name>Task 2: Capture lifecycle, live affordances and metrics on scenario finish</name>
  <files>app/case-play/[caseId]/page.tsx</files>
  <action>
Mirror plan 10-09's session-shell wiring, gated on `isScenario && cameraMode === "ON"`.

**Visual capture** starts once the run is live (`pageState === "playing"`) and
the avatar has connected — reuse the existing avatar-ready effect near `:186-190`
rather than adding a new lifecycle. Same failure policy as 10-09: a probe that
succeeded in the intro but fails now degrades with a single warning toast and a
null visual block, never a blocked live run.

**Vocal capture.** Attach to the existing push-to-talk audio stream; do not add
a second `getUserMedia({ audio: true })`. Call `submitSpokenTurn` on each
completed voice recording and `recordTypedTurn()` on each typed submission.

**This is where Q4 is resolved, and the resolution must be written into the
code as a comment.** `interactionMode` here is `"text" | "avatar"` and CAN be
switched mid-run (`handleModeSwitch`, `:663`) — unlike the camera mode, which is
locked. So vocal accounting is TURN-LEVEL and the verdict is SESSION-LEVEL:
every turn is recorded as spoken or typed as it happens, and
`resolveVocalOutcome` (plan 10-01) then decides from the aggregate — scored if
there was enough real speech, `TYPED_ONLY` if there was not. A mixed session is
scored on its spoken portion only; typed turns contribute nothing and subtract
nothing (REQ-44). This needs no change to the Text/Avatar toggle and no lock on
it, which is why turn-level accounting was chosen over forcing a second locked
control the user never asked for. State that rationale in the comment so a
future reader does not "simplify" it to a session-level flag and thereby
penalise a student who typed two answers.

**Live affordances.** Render `SelfViewThumbnail` and `FaceDetectionBanner` in
the `pageState === "playing"` branch, gated on `isScenario && cameraMode === "ON"`.
Place them as fixed-position siblings after the existing layout — the player's
`interactionMode === "avatar"` branch at `:1529` has its own geometry and must
not be restructured. Verify the banner does not overlap the avatar-minutes
warning or the finish control.

**Finish.** In `handleFinish` (`:1099`), inside the
`if (isScenario && scenarioReportId)` branch at `:1119` ONLY, stop the visual
capture and `await drain(8000)` immediately before the
`fetch("/api/scenario/session/finish")` at `:1120`, and add
`metrics: { cameraMode, visual, vocal }` to the existing `{reportId, log}` body.
Preserve the branch's existing behaviour exactly: a `409` still counts as a
successful navigation, and success still routes to
`/case-play/{caseId}/report/{reportId}`. Wrap the drain in try/catch and send
null metrics on any throw rather than losing the run.

The legacy `else` finish branch stays byte-unchanged, and `handleResume`'s
documented fallback (it never repopulates `scenarioReportId`, so a resumed run
takes the legacy finish path — a deliberate gap logged in `09-07-SUMMARY.md` and
carried forward in STATE.md) is OUT OF SCOPE. Do not fix it here and do not send
metrics down that path; note in the SUMMARY that a resumed scenario run
therefore produces no metrics, which is a pre-existing limitation this phase
inherits rather than introduces.

**Cleanup.** Stop the visual capture, detach the vocal capture and stop every
camera track in the existing teardown effect and on every exit path — including
the save-and-exit control and a `pageState` change away from `"playing"`. The
camera LED must go out.
  </action>
  <verify>
`npx tsc --noEmit` clean.
`grep -n "getUserMedia" app/case-play/\[caseId\]/page.tsx` — no new occurrence
versus the pre-plan diff.
`git diff app/case-play/\[caseId\]/page.tsx | grep -n "interaction/finish"` returns
nothing — the legacy finish body is untouched.
`grep -n "metrics:" app/case-play/\[caseId\]/page.tsx` — exactly one hit, inside
the scenario finish branch.
`grep -n "handleResume" app/case-play/\[caseId\]/page.tsx` — confirm its body is
unchanged in the diff.
Live end-to-end verification on a free port against the local dev DB with a real
seeded student, a real scenario with at least one avatar character, and real
OpenAI calls:
1. Camera-on avatar-mode scenario run: self-view appears, covering the camera
   raises the banner and uncovering folds it away, finish resolves the
   `ScenarioReport` to READY with non-null `visualScore`/`vocalScore` and band
   rows on the report page.
2. Camera-on run answered entirely by TYPING: Visual scores, Vocal reads the
   typed-only copy.
3. A MIXED run — switch `interactionMode` mid-run, giving some spoken and some
   typed turns — and confirm Vocal still scores (not typed-only) and that the
   WPM is plausible for the spoken portion alone rather than diluted by the
   typed turns. This is the Q4 assertion; record the numbers.
4. Camera-off scenario run behaves exactly as Phase 9 did.
5. The admin case `testing` still runs start-to-finish through the legacy
   pipeline with a real `evalScore` written to its S3 log — the 09-07 zero-
   regression check, repeated.
Record all five verbatim.
  </verify>
  <done>A scenario run captures and delivers metrics, a mixed typed/spoken run is scored on its spoken portion only, the camera is released on every exit, and the admin case path is provably unaffected.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- `app/interview/[type]/page.tsx` and `components/interview/InterviewSessionShell.tsx` diff-empty — plan 10-09 owns them and runs in the same wave.
- `app/api/interaction/` diff-empty; the admin case start/finish bodies including `cohortId` are byte-unchanged.
- `components/HeyGenAvatar/InteractiveAvatar.tsx` diff-empty.
- Every `cameraMode` and `metrics` reference sits inside an `isScenario` branch.
- No Prisma file touched; no migration; `npm run setup` never run.
- Single bracketed path, staged with LITERAL quoted pathspecs, every commit verified with `git show --name-only`.
</verification>

<success_criteria>
Scenario reports light up with real Visual and Vocal scores through the same
consent, lock, block and live-affordance behaviour as interviews, using the same
shared components — while the admin case-study path is completely untouched and
a mid-run Text/Avatar switch never costs the student their Vocal score.
</success_criteria>

<output>
After completion, create `.planning/phases/10-video-audio-metrics/10-10-SUMMARY.md`
</output>
