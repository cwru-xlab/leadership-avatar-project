---
phase: 10-video-audio-metrics
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - prisma/schema.prisma
  - lib/interview/report-dto.ts
  - lib/scenario/report-dto.ts
autonomous: true
requirements: [REQ-36, REQ-38, REQ-45, REQ-47, REQ-48]

must_haves:
  truths:
    - "Both report models can store the camera mode, the derived metric objects, and an explicit cause for an unscored Visual or Vocal category."
    - "A pre-Phase-10 report row still maps cleanly through both DTOs with every new field null, and renders as 'Not yet measured'."
    - "The cause of an unscored category is stored, not re-derived from a null score."
    - "Account-level consent is recorded once on the User and snapshotted per report."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "cameraMode, visualMetrics, vocalMetrics, unscored-reason and consent columns on InterviewReport and ScenarioReport; consent column on User"
      contains: "visualUnscoredReason"
    - path: "lib/interview/report-dto.ts"
      provides: "metrics block on InterviewReportDTO"
      exports: ["InterviewReportDTO", "toInterviewReportDTO", "REPORT_TERMINAL_STATUSES"]
    - path: "lib/scenario/report-dto.ts"
      provides: "metrics block on ScenarioReportDTO"
      exports: ["toScenarioReportDTO", "SCENARIO_REPORT_TERMINAL_STATUSES"]
  key_links:
    - from: "lib/interview/report-dto.ts"
      to: "lib/metrics/types.ts"
      via: "imports VisualMetrics/VocalMetrics/CameraMode for the typed metrics block"
      pattern: "@/lib/metrics/types"
    - from: "lib/scenario/report-dto.ts"
      to: "lib/metrics/types.ts"
      via: "imports the same shared metric types — no second private copy"
      pattern: "@/lib/metrics/types"
---

<objective>
Add the Phase 10 columns to `InterviewReport`, `ScenarioReport` and `User`, apply
the migration to the LOCAL dev database only, and extend both report DTOs with a
typed `metrics` block so the report pages can render four distinct unscored
causes instead of one.

Purpose: without a STORED cause, camera-off and insufficient-data are
indistinguishable after the fact — both are just `visualScore: null`. REQ-45's
four-way distinction is impossible without this plan, and REQ-48's "legacy rows
stay valid" is only provable once the new columns are nullable and the DTOs map
them without guessing.
Output: one migration (the fourth in the unapplied handoff queue) plus two
extended DTOs.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-video-audio-metrics/10-CONTEXT.md
@.planning/phases/10-video-audio-metrics/10-01-SUMMARY.md

@prisma/schema.prisma
@lib/interview/report-dto.ts
@lib/scenario/report-dto.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Phase 10 columns and generate a local-only migration</name>
  <files>prisma/schema.prisma</files>
  <action>
**HARD DATABASE RULES (from STATE.md Environment Notes — non-negotiable):**
- NEVER run `npm run setup`. `scripts/setup.mjs:211` runs `prisma migrate deploy`
  against the shared AWS Lightsail database.
- NEVER migrate the `DATABASE_URL` in `.env`/`.env.local` — they are ONE
  symlinked file pointing at the shared team database.
- Apply the migration with an INLINE local URL only:
  `DATABASE_URL="postgresql://ajabreu79@localhost:5432/leadership_avatar_dev" npx prisma migrate dev --name add_video_audio_metrics`
- Commit the generated SQL as the handoff artifact. This is the FOURTH migration
  in the unapplied handoff queue (after `20260920034855_add_interview_report`,
  `20260921141342_add_interview_customization`,
  `20260921201213_add_scenario_report`). Note that in the SUMMARY.

Add to BOTH `model InterviewReport` (currently `prisma/schema.prisma:285-331`)
and `model ScenarioReport` (`:333-380`) — an identical block in each, placed
directly beneath each model's existing `visualScore`/`vocalScore` group, with a
comment explaining that Phase 10 stores the derived numbers and the CAUSE of an
unscored category, and that every column is nullable so pre-Phase-10 rows stay
valid (REQ-48):

```
  // Phase 10 video/audio metrics. All nullable: a row written before Phase 10
  // has every field null, which is exactly the "Not yet measured" legacy state.
  // Only DERIVED NUMBERS are stored here — never media (REQ-38).
  cameraMode           String?   // "ON" | "OFF"; null = pre-Phase-10 row
  visualMetrics        Json?
  vocalMetrics         Json?
  visualUnscoredReason String?   // "CAMERA_OFF_OPTOUT" | "INSUFFICIENT_DATA"
  vocalUnscoredReason  String?   // "TYPED_ONLY" | "INSUFFICIENT_DATA"
  metricsConsentAt     DateTime? // snapshot of the account-level consent in force for this run
```

Use plain `String?` rather than Prisma enums, following the explicit Phase 8
precedent recorded in STATE.md ("difficulty stored as plain String? on
InterviewReport, not a Prisma enum, keeping `lib/interview/types.ts` as the
single source of truth"). Here the single source of truth is
`lib/metrics/types.ts`'s `CameraMode` / `VisualUnscoredReason` /
`VocalUnscoredReason` unions from plan 10-01.

Add to `model User` (`prisma/schema.prisma:47-78`), in the Account status group:
```
  // REQ-36: account-level acceptance of video/audio analysis, remembered across
  // sessions. Distinct from the browser permission dialog, which is not consent
  // to being ANALYSED. Each report row snapshots this at session start.
  videoAnalysisConsentAt DateTime?
```

Do NOT touch any other model. Do NOT add a NOT NULL column or a default to any
existing column — the migration must contain only `ADD COLUMN` statements with
no `NOT NULL`, matching the 09-01 verification standard.
  </action>
  <verify>
`npx prisma validate` clean. `npx tsc --noEmit` clean after
`npx prisma generate`. Confirm the generated SQL under
`prisma/migrations/*_add_video_audio_metrics/migration.sql` contains only
`ALTER TABLE ... ADD COLUMN` lines and zero `NOT NULL`, zero `DROP`, zero
`CREATE TABLE`: `grep -c "NOT NULL\|DROP\|CREATE TABLE" <file>` returns 0.
Confirm `git diff --stat` touches only `prisma/`.
Confirm the shared DB was never targeted: `git log -p --oneline -1 -- .env .env.local`
shows nothing, and the migrate command in the SUMMARY records the inline local URL verbatim.
  </verify>
  <done>Six new nullable columns exist on each report model plus one on User, the migration is applied to `leadership_avatar_dev` only, and its SQL is committed as a handoff artifact containing no NOT NULL and no destructive statement.</done>
</task>

<task type="auto">
  <name>Task 2: Extend both report DTOs with a typed metrics block</name>
  <files>lib/interview/report-dto.ts, lib/scenario/report-dto.ts</files>
  <action>
Both DTOs document that they map field by field and NEVER spread the Prisma row
(`lib/interview/report-dto.ts:52-59`). Preserve that discipline exactly — add
explicit fields, never a spread.

In BOTH files, import the shared types:
`import type { CameraMode, VisualMetrics, VocalMetrics, VisualUnscoredReason, VocalUnscoredReason } from "@/lib/metrics/types";`
Neither file may declare its own private copy of these shapes — one contract,
imported twice (this is the explicit resolution of the research's Open Question 1
about shared-vs-duplicated metric shapes).

Add to `InterviewReportDTO` (after the existing `scores` block at
`lib/interview/report-dto.ts:27-32`) and to `ScenarioReportDTO` (after its
`scores` block at `lib/scenario/report-dto.ts:31-34`) the identical block:

```ts
  /**
   * Phase 10 derived metrics. `cameraMode === null` marks a pre-Phase-10 row —
   * the "Not yet measured" legacy state (REQ-48). A non-null `visualUnscored`
   * or `vocalUnscored` carries the CAUSE of a null score, so the report page
   * never has to guess it back from the null (REQ-45).
   *
   * Safe to return to its owner: these are the student's own derived numbers
   * and the route this DTO backs is already owner-scoped with a
   * 404-never-403 contract. No media reference can appear here.
   */
  metrics: {
    cameraMode: CameraMode | null;
    visual: VisualMetrics | null;
    vocal: VocalMetrics | null;
    visualUnscored: VisualUnscoredReason | null;
    vocalUnscored: VocalUnscoredReason | null;
  };
```

In each mapper (`toInterviewReportDTO`, `toScenarioReportDTO`) populate it
field by field. The Prisma `Json?` columns come back as `Prisma.JsonValue`, so
add one small local helper per file — do NOT use `as any`:

```ts
function asVisualMetrics(value: unknown): VisualMetrics | null {
  // The column is written only by this app's own metrics pipeline, but a
  // hand-edited or pre-contract row must degrade to null rather than crash the
  // report page.
  if (!value || typeof value !== "object" || Array.isArray(value)) return null;
  const v = value as Partial<VisualMetrics>;
  return typeof v.eye_contact_pct === "number" && v.coverage ? (value as VisualMetrics) : null;
}
```
and the analogous `asVocalMetrics` keyed on `typeof v.words_per_minute === "number" && v.coverage`.

Cast the reason/mode strings through a narrow validator too, not a bare
assertion: a value not in the union becomes `null`.

Do NOT change `REPORT_TERMINAL_STATUSES` or
`SCENARIO_REPORT_TERMINAL_STATUSES`. The single-`status` polling model is
deliberately preserved — see plan 10-04's Q3 measurement task and 10-07's
ingestion design, which attach metrics BEFORE the evaluator call so there is
never a second poll axis to add.

Do NOT add `userId`, `studentEmail`, `interactionLogId`, `transcriptKey`,
`resumeText` or `resumeId` to either DTO. `toScenarioReportDTO`'s existing
narrowing of `avatarsSnapshot` down to `{name, role}[]` (which strips each
character's hidden `additionalInfo` briefing) must remain untouched.
  </action>
  <verify>
`npx tsc --noEmit` clean.
Write a throwaway `tsx` script in the scratchpad that builds two hand-made rows
— one fully legacy (all six new columns null) and one fully populated — passes
each through its mapper, and asserts: the legacy row yields
`metrics.cameraMode === null` and all four sibling fields null with no throw;
the populated row round-trips the metric objects intact; a row with
`visualMetrics: { garbage: true }` yields `metrics.visual === null` rather than
throwing; and `JSON.stringify` of both DTOs contains none of the substrings
`userId`, `resumeText`, `transcriptKey`, `interactionLogId`, `additionalInfo`.
Delete the script.
`grep -n "\.\.\.row\|as any" lib/interview/report-dto.ts lib/scenario/report-dto.ts` returns nothing.
  </verify>
  <done>Both DTOs expose an identically-shaped `metrics` block sourced from one shared type module, a legacy all-null row maps without throwing, a malformed JSON column degrades to null, and no private column leaks.</done>
</task>

</tasks>

<verification>
- `npx prisma validate` and `npx tsc --noEmit` both clean.
- Exactly one new migration directory; its SQL contains only `ADD COLUMN`, no `NOT NULL`.
- `npm run setup` never run; no command in the SUMMARY's transcript used the `.env` `DATABASE_URL`.
- `git diff --name-only` across this plan's commits lists only the three files in `files_modified` plus the new migration directory. Stage with literal paths; verify with `git show --name-only`.
- `grep -rn "interface VisualMetrics\|interface VocalMetrics" lib/interview/ lib/scenario/` returns nothing — the shapes live only in `lib/metrics/types.ts`.
</verification>

<success_criteria>
Both report models can store what was measured, which camera mode produced it,
and why a category went unscored. Both DTOs surface that to their owner and
nothing else. A report written before this phase still maps and still reads
"Not yet measured".
</success_criteria>

<output>
After completion, create `.planning/phases/10-video-audio-metrics/10-02-SUMMARY.md`
</output>
