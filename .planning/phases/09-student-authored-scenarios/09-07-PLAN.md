---
phase: 09-student-authored-scenarios
plan: 07
type: execute
wave: 4
depends_on: ["09-01", "09-04"]
files_modified:
  - app/case-play/[caseId]/page.tsx
autonomous: true
requirements: [REQ-25, REQ-32]

must_haves:
  truths:
    - "A student can start and complete a run of a student-authored scenario, which has no cohort"
    - "Finishing a scenario run takes the student to that run's report"
    - "Admin-authored case studies still start and finish exactly as they did before"
  artifacts:
    - path: "app/case-play/[caseId]/page.tsx"
      provides: "Scenario-aware start and finish branches in the existing player"
  key_links:
    - from: "app/case-play/[caseId]/page.tsx"
      to: "/api/scenario/session/start"
      via: "handleStart branch taken when caseData.ownerId is present"
      pattern: "api/scenario/session/start"
    - from: "app/case-play/[caseId]/page.tsx"
      to: "/case-play/[caseId]/report/[reportId]"
      via: "router.push after a successful scenario finish"
      pattern: "/report/"
---

<objective>
Teach the existing `/case-play` player to run a student-authored scenario: start without
a cohort, finish into the new evaluation pipeline, and land on the run's report — while
leaving the admin case path byte-for-byte behaviourally identical.

Purpose: `/api/interaction/start` hard-requires a `cohortId` (`app/api/interaction/start/route.ts:11`)
that a student scenario will never have, so today a scenario would fail to start at all.
This plan branches around it without touching that route.
Output: three localized edits to one file.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-student-authored-scenarios/09-CONTEXT.md
@.planning/phases/09-student-authored-scenarios/09-04-SUMMARY.md
@app/case-play/[caseId]/page.tsx
@app/api/interaction/start/route.ts
@app/api/interaction/finish/route.ts
</context>

<hard_constraints>
- **This plan touches exactly one file.** No API route may be modified here; 09-04
  already built everything this page calls.
- **The admin-case path must be unchanged.** Every new behaviour hangs off
  `caseData?.ownerId` being present. When it is absent, the existing code runs verbatim:
  same `/api/interaction/start` body including `cohortId`, same `/api/interaction/finish`
  call, same `router.push("/case-play")`. Prove this with a before/after read of both
  functions in the SUMMARY.
- **Never pass a placeholder `cohortId`** into the legacy start route to make a scenario
  work. The scenario branch calls the scenario route instead.
- **No avatar-config change.** Scenario characters carry the same `profileId` admin cases
  do (09-05's picker emits one), so `loadAvatarConfig`, `roleHasAvatarProfile` and the
  Text/Avatar toggle all work untouched. Do NOT add an `avatarId` branch — there is no
  such field.
- Commit with a LITERAL, quoted pathspec. `app/case-play/[caseId]/page.tsx` contains
  brackets, and per `08-08-SUMMARY.md` a bracketed pathspec has previously glob-matched a
  concurrent agent's staged files. Verify with `git show --name-only`.
- The cohort avatar-time-limit effect (lines ~223-248) already early-returns when
  `cohortId` is empty, so a scenario simply has no per-case avatar minutes cap. Leave it
  alone and record that consequence in the SUMMARY rather than "fixing" it.
</hard_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Branch handleStart onto the cohort-free scenario start route</name>
  <files>app/case-play/[caseId]/page.tsx</files>
  <action>
Add a derived flag near the existing `caseData` state, e.g.
`const isScenario = Boolean(caseData?.ownerId);` with a comment explaining that
`ownerId`'s presence is the discriminator between a student-authored scenario and a
legacy admin case study.

Add report id state: `const [scenarioReportId, setScenarioReportId] = useState<string | null>(null);`

In `handleStart(selectedMode)` (~line 425), branch at the top:

- **`isScenario` true:** ignore `selectedMode` and run assessed — a scenario is always an
  evaluated run (call `setMode("assessed")`). POST `/api/scenario/session/start` with
  `{ caseId: caseData.id, language: attemptLanguage.code }` and `credentials: "include"`.
  On success the response carries `{ reportId, log }`: `setScenarioReportId(data.reportId)`,
  `setInteractionLog(data.log)`, `setChatMessages({})`, `setPageState("playing")` —
  identical downstream state to the legacy branch, so every existing feature (auto-save,
  role switching, avatar mode, push-to-talk) keeps working with no further change.
  On a non-ok response, surface the server's message when present, otherwise the existing
  generic toast; a 404 here means the scenario was deleted or unpublished out from under
  the student, so say so.
- **`isScenario` false:** the existing body, completely unchanged, including `cohortId`.

If the mode-selection UI offers explore/assessed before start, hide or disable the
explore option when `isScenario` — a scenario run is always evaluated, and offering a
mode that produces no report would be misleading. Keep that UI exactly as-is for admin
cases.

Also hide any cohort-derived avatar-minutes messaging for a scenario if it would render a
misleading zero/empty limit; do not add a new limit mechanism.
  </action>
  <verify>
`npx tsc --noEmit` clean after `rm -rf .next`.
`grep -n "cohortId" 'app/case-play/[caseId]/page.tsx'` shows the SAME occurrences as
before this task plus none inside the scenario branch (diff the grep output against HEAD).
`git diff 'app/case-play/[caseId]/page.tsx'` shows the legacy `handleStart` body preserved
verbatim inside the `else`.
Runtime with a real seeded student: starting an admin-authored published case still works
end to end (messages send, auto-save fires) — this is the regression check; starting a
student-authored scenario reaches `pageState === "playing"` with no cohort in the request
body (confirm in the Network tab) and a `reportId` in the response.
  </verify>
  <done>A scenario starts without a cohort through the new route, the admin case path is byte-identical, and the player's downstream state is the same in both branches.</done>
</task>

<task type="auto">
  <name>Task 2: Branch handleFinish into the scenario evaluation pipeline and navigate to the report</name>
  <files>app/case-play/[caseId]/page.tsx</files>
  <action>
In `handleFinish` (~line 1040), keep the existing pre-flight verbatim for both branches
(clear auto-save, stop the avatar timer and session when in avatar mode, release the mic
stream) — that teardown is correct for a scenario too.

Then branch on `isScenario`:

- **Scenario:** require `scenarioReportId`; if it is somehow null, fall back to the legacy
  finish rather than losing the session. POST `/api/scenario/session/finish` with
  `{ reportId: scenarioReportId, log: interactionLog }`, `credentials: "include"`.
  Expect **202**. On success `addToast({ title: "Session complete — your report is being
  prepared.", color: "success" })` then
  `router.push(\`/case-play/${caseId}/report/${scenarioReportId}\`)` — the report page
  (09-08) polls until the status is terminal, so navigating immediately is correct and
  mirrors what the interview flow does.
  Treat **409** ("already submitted") as a success for navigation purposes: the run is
  finished, so still navigate to the report rather than stranding the student on a dead
  session.
  On a genuine failure, keep the student in the live session (do not navigate) and toast
  the error — the Phase 6 discipline that a network failure during End leaves a
  retryable session rather than a dead one.
- **Admin case:** the existing body unchanged — `/api/interaction/finish`, the existing
  mode-dependent toast, `router.push("/case-play")`.

Leave `handleSaveAndExit` alone for both branches: leaving without submitting keeps the
S3 log `in_progress` and the `ScenarioReport` row `IN_PROGRESS`, which is the correct
"didn't finish" state. Note in the SUMMARY that an abandoned scenario run therefore leaves
an `IN_PROGRESS` row that never evaluates — same shape as the interview side's already-
accepted behaviour, and the report list (not built in this phase) is where it would
matter.
  </action>
  <verify>
`npx tsc --noEmit` clean.
`git diff 'app/case-play/[caseId]/page.tsx'` shows the legacy finish body preserved
verbatim in the `else`, and `git diff --name-only` across this plan's commits lists ONLY
this file.
Runtime end to end with a real seeded student and real OpenAI: run a student-authored
scenario with a few exchanges, press End → lands on
`/case-play/{caseId}/report/{reportId}`; the row resolves to READY. Regression: finish an
admin-authored case and confirm it still lands on `/case-play` with the old toast and that
its S3 `InteractionLog` still receives the legacy `evalScore`/`evalResult` — the legacy
pipeline must be provably still running for admin cases.
Commit with a literal quoted pathspec; confirm with `git show --name-only`.
  </verify>
  <done>Finishing a scenario submits to the new pipeline and navigates to its report; finishing an admin case behaves exactly as it did before, legacy evaluator included.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- `git diff --name-only` across the whole plan lists exactly one file.
- Admin-case start and finish verified unchanged, including the legacy evaluator still writing `evalScore`.
- Scenario start verified to send no `cohortId`; scenario finish verified to reach a READY report.
</verification>

<success_criteria>
- REQ-25: a student-authored scenario is actually playable end to end.
- REQ-32: finishing a scenario run triggers the new rubric evaluation and shows the student where the report is.
- Zero regression to the admin case path.
</success_criteria>

<output>
After completion, create `.planning/phases/09-student-authored-scenarios/09-07-SUMMARY.md`.
</output>
