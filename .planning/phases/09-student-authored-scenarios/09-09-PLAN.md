---
phase: 09-student-authored-scenarios
plan: 09
type: execute
wave: 5
depends_on: ["09-01", "09-02", "09-03", "09-04", "09-05", "09-06", "09-07", "09-08"]
files_modified: []
autonomous: false
requirements: [REQ-25, REQ-26, REQ-27, REQ-28, REQ-29, REQ-30, REQ-31, REQ-32, REQ-33, REQ-34]

must_haves:
  truths:
    - "A student can author a scenario and immediately practice against it"
    - "A student's own scenarios are private to them unless deliberately shared"
    - "No Phase 9 surface promises real visual or vocal scoring"
    - "The admin case-authoring and case-play flows are unregressed"
  artifacts:
    - path: ".planning/phases/09-student-authored-scenarios/09-09-SUMMARY.md"
      provides: "Verbatim per-check and per-step results plus the deferred-items list"
  key_links:
    - from: "builder save"
      to: "/case-play list"
      via: "the just-saved scenario is present and launchable"
      pattern: "case-play"
    - from: "scenario finish"
      to: "scenario report"
      via: "READY report with null visual/vocal"
      pattern: "report"
---

<objective>
Prove Phase 9 actually achieved its two ROADMAP success criteria, with a static
constraint sweep and a real human end-to-end walkthrough — the same closing discipline
Phases 6, 7 and 8 each used (`06-08`, `07-07`, `08-08`).

Purpose: every prior phase in this project found at least one real defect in this step
that no amount of `tsc` passing would have caught. Assume this one will too.
Output: a SUMMARY with verbatim per-check results and the consolidated deferred list.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-student-authored-scenarios/09-CONTEXT.md
@.planning/phases/09-student-authored-scenarios/09-01-SUMMARY.md
@.planning/phases/09-student-authored-scenarios/09-04-SUMMARY.md
@.planning/phases/09-student-authored-scenarios/09-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static constraint sweep against the Phase 9 baseline</name>
  <files>(no source files — this task fixes defects only if it finds them)</files>
  <action>
First resolve the baseline correctly: it is the commit IMMEDIATELY BEFORE 09-01's first
commit, NOT `main` (`main` predates Phases 1-8, so diffing against it misreports merged
work as Phase 9 violations — the mistake 07-07 and 08-08 both explicitly avoided).
Record the resolved SHA in the SUMMARY.

Run every check and record the VERBATIM result of each — including the passes:

1. `npx tsc --noEmit` clean (after `rm -rf .next`). `tsc` is authoritative; re-confirm
   eslint is still pre-existing-broken repo-wide against an untouched file such as
   `lib/languages.ts`, and say so rather than reporting it as a Phase 9 failure.
2. `npx prisma validate` clean.
3. Exactly ONE new migration directory since the baseline; its SQL creates
   `ScenarioReport` and contains no `ALTER TABLE ... ADD COLUMN ... NOT NULL` on any
   pre-existing table.
4. No shared-database write: no `npm run setup` in any summary's command log, and every
   `prisma migrate` invocation carried an inline local `DATABASE_URL=`. Confirm `.env`
   and `.env.local` are absent from `git log --name-only` since the baseline.
5. `git diff --name-only <baseline>..HEAD -- app/api/case/ app/api/interaction/ app/api/profile/` is EMPTY.
6. `git diff --name-only <baseline>..HEAD -- lib/interview/ components/interview/` is EMPTY.
7. `git diff <baseline>..HEAD -- middleware.ts` is a single-line addition inside
   `STUDENT_ROUTES`; `ADMIN_ROUTES` has no hunk.
8. `grep -rn "403" app/api/scenario/` returns nothing.
9. `grep -rn "ownerId" app/api/scenario/` shows no assignment from a request body — every
   write is from `currentUser.id`.
10. `grep -rn "visualScore: null\|vocalScore: null" lib/scenario/evaluation.ts` matches in
    the validator, and the `ScenarioEvaluationResult` type declares those two fields as
    `null`, not `number | null`.
11. `grep -rni "coming soon\|will be scored\|visual analysis" app/case-play/ components/scenario/`
    returns nothing that promises future visual/vocal scoring.
12. `grep -rn "<Select" components/scenario/` returns nothing (REQ-27).
13. `grep -rn "api/interview/interviewers" components/scenario/ app/case-play/` returns nothing.
14. `grep -rni "cohort\|assignment\|isStaff\|instructor\|professor" app/api/scenario/ components/scenario/`
    returns nothing except, at most, the literal `cohortId: ""` in the scenario start route.
15. `grep -rn "knowledgeId" app/api/scenario/` returns nothing (the student avatar
    catalog projection drops it).
16. `grep -rn "\.\.\.row\|\.\.\.report" lib/scenario/report-dto.ts` returns nothing.
17. `grep -rn "getCase" lib/scenario/evaluation-runner.ts` returns nothing (grading reads
    only the snapshot).
18. `grep -rn "fork" app/case-play/ components/scenario/` returns nothing — forking is
    deferred; only the data model permits it.
19. Every one of REQ-25..REQ-34 traced to a shipped artifact, named explicitly.

If a check fails, FIX it under this task, commit the fix, and record both the failure and
the fix. If everything passes, there is nothing to commit for this task — say so.
  </action>
  <verify>All 19 checks recorded verbatim in the SUMMARY with a PASS/FAIL and, for any FAIL, the commit that fixed it.</verify>
  <done>The static sweep is complete and every constraint either passes or has a recorded fix.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human end-to-end walkthrough of the full student-authored scenario path</name>
  <action>
Prepare the environment described in <what-built>, then walk the user through the 24
steps in <how-to-verify>, recording a verbatim PASS/FAIL for each. Any defect found is
fixed inline under this checkpoint (as 07-07 and 08-08 both did), the fix committed, and
the affected steps re-run before sign-off.
  </action>
  <what-built>
Phase 9 end to end: a guided scenario builder with a card-grid avatar picker, real
server-side per-user ownership on S3 scenarios, a private-by-default publish model, a
two-section `/case-play`, a cohort-free scenario run, and a rubric-scored report that
snapshots the scenario and survives its deletion.

Before starting, Claude has: started `npm run dev` with an inline local `DATABASE_URL`
(reusing an already-running server if one exists rather than restarting someone's live
session — the limitation logged in `07-02`/`08-05`), confirmed two real seeded students
are available (e.g. `alice.johnson@case.edu` and `bob.williams@case.edu`) plus an admin,
and confirmed at least one `VideoAudioProfile` exists in the avatar catalog and at least
one admin-authored case is published.
  </what-built>
  <how-to-verify>
Work through these as student A unless told otherwise. Report PASS/FAIL per step.

**Authoring (REQ-25, REQ-26, REQ-27)**
1. From `/case-play`, click "Create a scenario". The builder opens on step 1 of 4, showing
   only the situation fields.
2. Try to advance with an empty (or one-word) background. "Next" stays disabled.
3. Fill a real situation, advance to Characters. The avatar picker is a GRID OF IMAGE
   CARDS with a selected-state check badge — NOT a dropdown. Confirm visually that it
   looks like the interviewer picker on `/interview/general`.
4. Try to advance with a character that has an avatar but no role. "Next" stays disabled.
5. Add a SECOND character with a different avatar. Advance to Criteria.
6. Try to advance with empty criteria. "Next" stays disabled. Write real criteria; the
   copy says they are applied IN ADDITION to a standard rubric. Advance to Review.
7. Confirm there is no "generate this for me" button anywhere in the builder.

**Save and immediate practice (REQ-28, criterion 1)**
8. Press "Save scenario". You land on `/case-play`, NOT in a live session, and the new
   scenario is ALREADY in the "Practice scenarios" section with a "Private" chip — with
   no refresh and no extra step.
9. Click it. The run starts. This is ROADMAP criterion 1 — author, then immediately
   practice.

**Running and reporting (REQ-32, REQ-33)**
10. Exchange several messages with at least two different characters. Switch one into
    avatar mode if the chosen profile supports it and confirm the avatar loads.
11. Press End. You land on the report page. It polls, then resolves to READY.
12. Content and Behavioral show scores. **Visual and Vocal both read "Not yet measured".**
    Confirm nothing anywhere on the page promises visual or vocal scoring.
13. The report shows a snapshot strip with the scenario name and both character names.
    Confirm no character's private "additional info" briefing appears anywhere.

**Snapshot truthfulness and deletion (REQ-33, REQ-34)**
14. Edit the scenario: rename it and rename a character. Save. Reopen the report from
    step 11 — the snapshot strip still shows the OLD name. The report is unchanged.

**Privacy and publishing (REQ-29, REQ-30, criterion 2)**
15. As student B, go to `/case-play`. A's scenario is NOT visible anywhere.
16. As B, open A's scenario URL directly (`/case-play/{id}`) and try to start it.
    It must not run as a scenario.
17. As B, open A's report URL directly. You get the "Report not found" shell — the same
    one a made-up id gives, with no hint the report exists.
18. As A, publish the scenario. As B, reload `/case-play` — it now appears in the
    "Practice scenarios" section marked as a classmate's, with NO edit/publish/delete
    controls. B can start it.
19. As B, try `/case-play/{A's id}/edit` directly. Not-found, not an editor.

**Two sections (REQ-31)**
20. Confirm `/case-play` shows two clearly labelled sections and that the published
    student scenario appears ONLY in the scenarios section, while the admin-authored
    published case appears ONLY in the case-studies section.

**Delete lifecycle (REQ-34)**
21. As A, try to delete the published scenario. You are told to unpublish it first.
22. Unpublish, then delete. It disappears for both A and B. **Reopen A's report from step
    11 — it still loads and still reads correctly.**

**Admin regression**
23. As the admin, open `/case-management`, edit a case, and save. Unchanged.
24. As a student, play an ADMIN-authored published case end to end and press End. It
    still lands on `/case-play` with the old toast (no report page) — the legacy pipeline
    is untouched.
  </how-to-verify>
  <resume-signal>Type "approved" to sign off Phase 9, or describe the failing step numbers and what you saw.</resume-signal>
</task>

</tasks>

<verification>
- All 19 static checks recorded verbatim with results.
- All 24 walkthrough steps human-confirmed, or failures fixed under the checkpoint and
  the affected steps re-run.
- Both ROADMAP success criteria explicitly asserted with the step numbers that prove them.
- REQ-25..REQ-34 ticked in `.planning/REQUIREMENTS.md` only after the walkthrough passes.
</verification>

<success_criteria>
- Criterion 1 proven by steps 8-9: a student authors a scenario and immediately practices against it.
- Criterion 2 proven by steps 15-19: a student's scenarios are private until deliberately shared.
- No Phase 9 surface promises visual or vocal scoring (step 12 plus static check 11).
- The admin case flow is proven unregressed (steps 23-24 plus static check 5).
</success_criteria>

<output>
After completion, create `.planning/phases/09-student-authored-scenarios/09-09-SUMMARY.md`
with verbatim per-check and per-step results and a consolidated deferred-items list, then
update `.planning/STATE.md` with the Phase 9 completion block and tick REQ-25..REQ-34 in
`.planning/REQUIREMENTS.md`.
</output>
