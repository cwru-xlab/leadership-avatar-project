---
phase: 11-cohort-staff-teardown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/cohort-management/**
  - app/codes/**
  - app/teacher/**
  - app/student-history/**
  - app/join/**
  - app/users-and-usages/**
  - components/cohort-card.tsx
  - config/site.ts
  - app/page.tsx
  - middleware.ts
autonomous: true
requirements: []   # Phase 11 has NO REQ IDs in REQUIREMENTS.md (IDs stop at REQ-49 = Phase 10). Anchored on ROADMAP success criteria only.

must_haves:
  truths:
    - "No staff/assignment page (cohort management, access codes, gradebook, teacher class views, student history, join-by-code, per-user usage) is reachable in the app — each returns a plain 404."
    - "No navigation entry or dashboard card links to a deleted page."
    - "The app still type-checks with zero errors after the deletions."
  artifacts:
    - path: "config/site.ts"
      provides: "navItems without the Cohort Management entry"
      absent: "href: \"/codes\""
    - path: "app/page.tsx"
      provides: "navigationCards without the Cohorts card"
      absent: "href: \"/codes\""
    - path: "middleware.ts"
      provides: "route arrays with no entries pointing at deleted PAGES"
  key_links:
    - from: "components/auth-navbar.tsx"
      to: "config/site.ts navItems"
      via: "siteConfig.navItems render — inherits the nav cleanup automatically"
      pattern: "siteConfig\\.(navItems|studentNavItems)"
---

<objective>
Delete the staff/assignment PAGE surfaces from the repo and remove every link that
pointed at them.

Purpose: This is the core of success criterion 1 — after this plan, no user-facing
surface exists for cohorts, access codes, gradebooks, per-student monitoring, or
join-by-code. Removed paths return a plain 404; deleting the route file is the whole
implementation (no redirect map, no "feature removed" page).
Output: Six page trees plus one orphaned component removed; nav/dashboard/middleware
references to them pruned; `npx tsc --noEmit` still clean.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/11-cohort-staff-teardown/11-CONTEXT.md
@.planning/phases/11-cohort-staff-teardown/11-RESEARCH.md
@config/site.ts
@middleware.ts
</context>

<constraints>
HARD CONSTRAINTS — violating any of these fails the plan:
- Do NOT modify `prisma/schema.prisma`. Do NOT create any migration file.
- Do NOT delete data (no Postgres rows, no S3 objects). No cleanup script.
- Do NOT delete or modify ANY file under `app/api/cohort/`, `app/api/codes/`, or
  `app/api/student/cases/`. Their fate is decided by the user at the 11-03 checkpoint.
- Do NOT delete `types/cohort.ts` — `app/api/cohort/add/route.ts` and `lib/s3-client.ts`
  both import from it and both survive this plan.
- Do NOT delete `lib/cohort-storage.ts` — it exists only to call the kept `/api/cohort/*`
  routes; its fate tracks theirs at the 11-03 checkpoint.
- Do NOT touch `lib/s3-client.ts` — one shared class serving avatars, chat, profiles,
  cases and interaction logs; pruning it breaks surviving features.
- KIOSK is out of scope: do not touch `app/kiosk/**`, kiosk auto-login, or any kiosk
  entry in `middleware.ts`.
- Do NOT touch `prisma/seed.ts` or `scripts/sync-s3-to-db.ts` — the schema stays, so
  they keep compiling. No decision calls for changing them.
- `npm run setup` / `prisma migrate deploy` are FORBIDDEN (shared team Postgres).
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Delete the staff/assignment page trees and their orphaned component</name>
  <files>app/cohort-management/**, app/codes/**, app/teacher/**, app/student-history/**, app/join/**, app/users-and-usages/**, components/cohort-card.tsx</files>
  <action>
Delete these directories and file outright with `git rm -r`:

- `app/cohort-management/` — `layout.tsx`, `page.tsx`, `edit/layout.tsx`,
  `edit/[cohort-id]/page.tsx`, `learners/[cohort-id]/page.tsx`
- `app/codes/` — `layout.tsx`, `page.tsx`, `[codeId]/page.tsx`, `[codeId]/edit/page.tsx`,
  `[codeId]/gradebook/page.tsx`, `[codeId]/student/[studentEmail]/page.tsx` and its
  `time-usage/[caseId]`, `score/[caseId]`, `conversations/[caseId]`,
  `learning-curve/[caseId]` subpages
- `app/teacher/` — `layout.tsx`, `class/[classId]/page.tsx`,
  `class/[classId]/case/[caseId]/page.tsx`,
  `class/[classId]/case/[caseId]/student/[studentId]/page.tsx`
- `app/student-history/` — `layout.tsx`, `page.tsx`,
  `[sectionId]/[studentId]/[caseId]/layout.tsx` + `page.tsx`
- `app/join/` — the whole `[accessCode]` tree (`layout.tsx`, `page.tsx`). Delete
  `app/join/` entirely if `[accessCode]` is its only child; otherwise delete only
  `app/join/[accessCode]/`.
- `app/users-and-usages/` — `layout.tsx`, `page.tsx`. (Per-user usage analytics are the
  monitoring model being torn down. Basic user administration is NOT the target and an
  aggregate/anonymous operator view is a deferred future phase — do not build a
  replacement here.)
- `components/cohort-card.tsx` — sole importer was `app/codes/page.tsx`, now deleted.

Do NOT delete anything under `app/api/`. This task is pages and one component only.
`app/api/student-history/**` is handled in plan 11-02, and the three
checkpoint-gated API groups are handled in 11-03.

Before deleting, run `git status` and `git log --oneline -5` to confirm no sibling agent
has landed conflicting work in these trees.
  </action>
  <verify>
`ls app/cohort-management app/codes app/teacher app/student-history app/users-and-usages components/cohort-card.tsx 2>&1` → every entry reports "No such file or directory".
`ls app/join/[accessCode] 2>&1` → "No such file or directory".
`npx tsc --noEmit` → expect errors ONLY from files still importing the deleted
component/pages; if any appear, they are fixed in Task 2. Record the error list.
  </verify>
  <done>All six page trees and `components/cohort-card.tsx` are gone from the working tree; `git status` shows them as deletions.</done>
</task>

<task type="auto">
  <name>Task 2: Prune nav entries, dashboard cards, and dead middleware PAGE routes</name>
  <files>config/site.ts, app/page.tsx, middleware.ts</files>
  <action>
1. `config/site.ts` — remove the `navItems` entry
   `{ label: "Cohort Management", href: "/codes", icon: "GraduationCap" }` (around line
   23-27). Leave `studentNavItems` alone (it has no cohort references).
   `components/auth-navbar.tsx` renders `siteConfig.navItems` and inherits this fix —
   do NOT edit auth-navbar.tsx in this plan (its `role === "student"` branch is handled
   in 11-06).

2. `app/page.tsx` — remove the `{ title: "Cohorts", ..., href: "/codes" }` entry from the
   `navigationCards` array (around lines 23-28). This is a second, independent hardcoded
   `/codes` link, separate from `config/site.ts`. Remove the card; do not repoint it.
   Do NOT touch the `user?.role === "student"` branch on line ~47 — that belongs to 11-06.

3. `middleware.ts` — remove ONLY entries whose targets are deleted PAGES:
   - from `ADMIN_ROUTES`: `/cohort-management`, `/codes` (the page prefix), `/teacher`,
     `/student-history`, `/users-and-usages`
   - from `PUBLIC_ROUTES`: `/join`
   LEAVE ALONE, in this plan: `/api/cohort/add`, `/api/cohort/edit`, `/api/cohort/delete`,
   `/api/cohort/list`, `/api/cohort/send-invitations`, `/api/codes`, `/api/cohort/join`,
   `/api/cohort/get`, and `/api/student/cases`. Those API entries are pruned in 11-03
   only for routes the user approves for deletion at the checkpoint.
   Careful: `/codes` in `ADMIN_ROUTES` is matched by `pathname.startsWith(route)`, so
   removing it must not also remove a distinct `/api/codes` entry — remove the page
   prefix only, keep the API prefix.
   Do not touch any kiosk entry or any role-comparison logic in this file.
  </action>
  <verify>
`grep -rn "cohort-management\|users-and-usages\|/student-history\|href: \"/codes\"" config/site.ts app/page.tsx` → no output.
`grep -n "codes\|cohort\|teacher\|student-history\|join" middleware.ts` → remaining hits
are `/api/...` prefixes and kiosk logic only; no bare page prefixes.
`npx tsc --noEmit` → exit 0, zero errors (this is the authoritative typecheck; `eslint`
and `next build` are known-broken repo-wide and are NOT valid gates).
`grep -rln "cohort-card" app components lib` → no output.
  </verify>
  <done>`npx tsc --noEmit` is clean, and no surviving file links to any deleted page.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0.
- `git diff --stat prisma/schema.prisma` → empty (schema untouched).
- `ls prisma/migrations` → unchanged from before this plan (no new migration).
- `git status --short app/api/cohort app/api/codes app/api/student` → no changes.
- `git status --short app/kiosk lib/s3-client.ts types/cohort.ts lib/cohort-storage.ts prisma/seed.ts scripts/sync-s3-to-db.ts` → no changes.
- `npx next dev` boots; visiting `/codes` or `/cohort-management` returns a 404 page.
</verification>

<success_criteria>
Supports ROADMAP success criterion 1 ("No user-facing surface depends on cohort
membership or staff roles"): every cohort/assignment/monitoring PAGE is deleted and
unlinked, and the app type-checks clean without them.
</success_criteria>

<output>
After completion, create `.planning/phases/11-cohort-staff-teardown/11-01-SUMMARY.md`.
Record the exact list of deleted paths — plan 11-03's checkpoint re-verifies its caller
map against this post-deletion tree.
</output>
