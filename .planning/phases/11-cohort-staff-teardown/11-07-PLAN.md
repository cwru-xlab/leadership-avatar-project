---
phase: 11-cohort-staff-teardown
plan: 07
type: execute
wave: 5
depends_on: ["11-02", "11-03", "11-04", "11-06"]
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: []   # Phase 11 has NO REQ IDs. Anchored on ROADMAP success criteria only.

must_haves:
  truths:
    - "No user-facing surface depends on cohort membership or staff roles — proven by a static sweep and confirmed by a human walkthrough."
    - "Individual users create and own all of their own practice work — a user can author, publish, unpublish and run their own scenario with no staff involvement."
    - "All four live flows still work: interview, case-play (admin case), student-authored scenarios, and both report types."
    - "The Prisma schema, all migrations, all Postgres rows, all S3 objects, and all KIOSK behavior are provably unchanged by this phase."
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Phase 11 completion state"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 11 marked complete in the progress table"
  key_links:
    - from: "the whole phase diff"
      to: "prisma/schema.prisma + prisma/migrations"
      via: "must be EMPTY — this phase writes no schema change and no migration"
      pattern: "prisma/(schema\\.prisma|migrations)"
---

<objective>
Close the phase: a static constraint sweep that proves nothing forbidden happened and
nothing dangling remains, then a human end-to-end walkthrough of the four live flows.

Purpose: This is the Phase 7-10 closing pattern. Deletion phases fail quietly — a dead
import, a 404 nav link, or a 400 on a start route only shows up when a human clicks
through. The sweep catches the static breakage; the walkthrough catches the rest.
Output: A clean sweep report, a human sign-off, and updated STATE.md / ROADMAP.md.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-cohort-staff-teardown/11-CONTEXT.md
@.planning/phases/11-cohort-staff-teardown/11-01-SUMMARY.md
@.planning/phases/11-cohort-staff-teardown/11-03-SUMMARY.md
@.planning/phases/11-cohort-staff-teardown/11-04-SUMMARY.md
@.planning/phases/11-cohort-staff-teardown/11-06-SUMMARY.md
@.planning/STATE.md
</context>

<constraints>
- `next build` is KNOWN-BROKEN in this repo on `/about`'s `EDGE_CONFIG` prerender, and
  `eslint` is KNOWN-BROKEN repo-wide (ESLint 9 / `@next/next` flat-config incompatibility).
  NEITHER is a valid gate and NEITHER is a Phase 11 regression. Do not try to fix them.
  The authoritative checks are `npx tsc --noEmit`, targeted greps, and `npx next dev`.
- NEVER run `npm run setup` (it runs `prisma migrate deploy` against the SHARED team
  Postgres). `.env` is a symlink to `.env.local` and `DATABASE_URL` points at the shared
  DB — never migrate it.
- If the sweep or walkthrough finds a defect, FIX IT in place only if it is a one-line
  dangling-reference fix. Anything larger gets recorded as a finding, not improvised.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Static constraint sweep</name>
  <files>(read-only sweep; fixes only for trivial dangling references)</files>
  <action>
Run and record the output of every check below. Each must pass.

Build / references:
- `npx tsc --noEmit` → exit 0, zero errors.
- `grep -rn "cohort-management\|users-and-usages\|/student-history\|/teacher/\|join/\[accessCode\]" app lib components config middleware.ts` → no output.
- `grep -rn "href=\"/codes\|href: \"/codes" app components config` → no output.
- `grep -rln "cohort-card\|student-history-service" app lib components` → no output.
- `grep -n "!cohortId" app/api/interaction/start/route.ts` → no output.
- `grep -rn "=== \"professor\"\|=== \"student\"" app lib components middleware.ts` → no output.
- `grep -rn "function toAppRole" lib` → exactly one definition.
- Every remaining `/api/...` entry in `middleware.ts`'s route arrays points at a route
  file that still exists — walk the arrays and `ls` each target.

Locked-constraint proofs (each MUST come back empty/unchanged):
- `git diff main -- prisma/schema.prisma` → empty. The schema was NOT modified.
- `git diff main --name-only -- prisma/migrations` → empty. NO migration was produced.
- `git diff main --name-only -- app/kiosk app/api/auth/kiosk-auto-login lib/s3-client.ts prisma/seed.ts scripts/sync-s3-to-db.ts` → empty. KIOSK, the shared S3 client, and the operator scripts are untouched.
- `grep -c "PROFESSOR" prisma/schema.prisma` → non-zero. The Role enum is intact.
- No data-deletion code was added: `git diff main --name-only` contains no cleanup/export
  script, and no `deleteMany`, S3 `deleteObject`, or equivalent was introduced.
- Deferred ideas stayed out: no access-code signup, no aggregate usage view, no schema
  cleanup, no export tooling anywhere in the phase diff.

Deleted-surface 404 proof — with `npx next dev` running, each returns 404:
`/codes`, `/cohort-management`, `/teacher/class/x`, `/student-history`,
`/users-and-usages`, `/join/anything`. Plus each API route the user approved for deletion
at the 11-03 checkpoint (read `11-03-SUMMARY.md` for that list). And each API route the
user KEPT still returns its normal admin-gated response — 401/403 unauthenticated, NOT 404.

Write the full results into the SUMMARY as a pass/fail table.
  </action>
  <verify>Every check above produced its expected result, and the SUMMARY contains the pass/fail table. Any failure is either fixed (trivial dangling reference) or recorded as an explicit finding.</verify>
  <done>`npx tsc --noEmit` is clean, all dead-reference greps are empty, and every locked constraint has a recorded proof.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human end-to-end walkthrough of the four live flows</name>
  <action>
Ensure `npx next dev` is running and reachable at http://localhost:3000, then hand the
walkthrough script below to the user and wait. Do not click through it on their behalf
and do not mark the phase complete without their explicit sign-off.
  </action>
  <what-built>
The cohort/staff teardown is complete: six staff page trees deleted, the legacy
student-history API tree removed, the caller-map checkpoint applied, the
`/api/interaction/start` cohortId requirement relaxed, and role gating collapsed to an
app-layer ADMIN/USER model. No database schema change, no migration, no data deleted, and
KIOSK untouched. The static sweep passes. What a human has to confirm is that the four
real flows still work and that nothing asks for a cohort or an assignment anywhere.
  </what-built>
  <how-to-verify>
Start the dev server: `npx next dev`, then visit http://localhost:3000.

1. **Interview flow** — log in as a normal (student/user) account. Go to `/interview`,
   pick an interview type, complete the setup wizard (interviewer, resume step, camera
   step), start a session, finish it. Expected: the session starts and completes with no
   cohort or access-code prompt anywhere.
2. **Case-play, admin case** — from `/case-play`, open an admin-authored case (one from
   the legacy published library, no owner) and click Start. Expected: **the session
   starts.** This is the specific regression the phase was at risk of — if you get an
   error about missing required fields or a 400, report it; it means the
   `/api/interaction/start` fix did not land.
3. **Student-authored scenarios (criterion 2)** — create a new scenario of your own,
   publish it with the owner toggle, confirm it appears in the shared library, unpublish
   it, confirm it disappears from shared but still appears under your own scenarios. Then
   run one of your own scenarios end to end. Expected: no staff approval step exists
   anywhere in that loop.
4. **Both report types** — open one interview report and one scenario report from
   `/reports`. Expected: both render with their scores and metrics as before.
5. **Navigation** — check the home dashboard and the navbar on both a student account and
   an admin account. Expected: no "Cohorts" / "Cohort Management" entry, and every
   remaining link leads somewhere real (no 404s).
6. **Admin tooling still works** — visit `/case-management`, `/avatar-management`,
   `/system-settings`, `/cta-management` as an admin. Expected: all still load.
7. **If you have a PROFESSOR-role account**, log in with it. Expected: it now behaves like
   an admin — admin dashboard, admin navbar, admin tooling reachable, and `/interview` and
   `/case-play` reachable too. This is an INTENTIONAL fix to a pre-existing bug where
   professors were locked out of both paths; it is not a regression.
8. **Kiosk** — if kiosk mode is set up locally, confirm kiosk auto-login and kiosk routes
   behave exactly as before. Nothing about kiosk was meant to change.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 11, or describe what broke (which step, what you saw).</resume-signal>
  <verify>The user has explicitly confirmed all eight walkthrough steps, in particular step 2 (admin case-play start does not 400) and step 3 (self-service publish/unpublish loop).</verify>
  <done>Human sign-off recorded; any reported defect is either fixed or written up as a finding before Task 3 runs.</done>
</task>

<task type="auto">
  <name>Task 3: Record phase completion</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
Only after the walkthrough is approved.

1. `.planning/STATE.md` — update Current Position to Phase 11 complete. Record the
   decisions that later phases depend on:
   - the user's per-route outcome from the 11-03 checkpoint (verbatim), since the deferred
     schema-cleanup phase is gated on it;
   - that the ADMIN/USER model exists in APP CODE ONLY (`lib/auth.ts` `toAppRole` /
     `isAdminRole`), with the Postgres `Role` enum still carrying all four values — a real
     enum collapse plus backfill is a separate, deliberate future migration;
   - that orphaned cohort / assignment / attempt rows remain in place in Postgres and all
     cohort S3 objects remain in S3, untouched and unexported;
   - the still-open deferred items: Prisma schema cleanup, the real Role enum collapse,
     the aggregate/anonymous operator usage view, access-code/invite signup, and export
     tooling for the orphaned rows.
2. `.planning/ROADMAP.md` — mark Phase 11 complete in the Progress table (plans complete,
   status, completion date) and tick the Phase 11 checkbox at the top.

Do not edit `.planning/REQUIREMENTS.md` — Phase 11 has no REQ IDs and must not invent any.
  </action>
  <verify>
`grep -n "Phase 11" .planning/ROADMAP.md` → shows the phase as complete in both the checklist and the progress table.
`grep -n "toAppRole\|Role enum" .planning/STATE.md` → the app-layer-only note is recorded.
`git diff --stat .planning/REQUIREMENTS.md` → empty.
  </verify>
  <done>STATE.md and ROADMAP.md reflect Phase 11 complete, with the checkpoint outcome and the deferred follow-ups recorded for the next phase.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0.
- The static sweep table in the SUMMARY is all-pass.
- Human walkthrough approved.
- `git diff main -- prisma/schema.prisma prisma/migrations` → empty.
</verification>

<success_criteria>
Both ROADMAP success criteria are demonstrably met:
1. No user-facing surface depends on cohort membership or staff roles — proven statically
   (greps, typecheck, 404s) and confirmed live by a human.
2. Individual users create and own all of their own practice work — a user authored,
   published, unpublished and ran their own scenario without any staff involvement.
</success_criteria>

<output>
After completion, create `.planning/phases/11-cohort-staff-teardown/11-07-SUMMARY.md`
with the static sweep pass/fail table and the human walkthrough result.
</output>
