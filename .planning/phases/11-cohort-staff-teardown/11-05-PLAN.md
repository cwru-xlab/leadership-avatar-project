---
phase: 11-cohort-staff-teardown
plan: 05
type: execute
wave: 3
depends_on: ["11-02", "11-03"]
files_modified:
  - lib/auth.ts
  - middleware.ts
autonomous: true
requirements: []   # Phase 11 has NO REQ IDs. Anchored on ROADMAP success criteria only.

must_haves:
  truths:
    - "A single app-layer helper is the source of truth for the ADMIN/USER role model: PROFESSOR is treated as ADMIN, STUDENT as USER, KIOSK stays a distinct third branch."
    - "A PROFESSOR-role account can reach both the student surfaces (/interview, /case-play) and the surviving admin tooling — the pre-existing lockout is fixed."
    - "A STUDENT-role account keeps exactly its current access; a KIOSK account keeps exactly its current behavior."
    - "The Postgres Role enum still has all four values and no migration was written."
    - "middleware.ts contains no route-array entry pointing at a deleted route tree — including any /api/student-history entry reported by 11-02."
  artifacts:
    - path: "lib/auth.ts"
      provides: "toAppRole / isAdminRole helpers mapping PROFESSOR->admin, STUDENT->user, KIOSK->kiosk"
      exports: ["toAppRole"]
    - path: "middleware.ts"
      provides: "Role gates routed through the helper instead of raw string comparisons, and no route-array entry pointing at a deleted route"
      absent: "student-history"
    - path: "prisma/schema.prisma"
      provides: "Role enum UNCHANGED with all four values"
      contains: "PROFESSOR"
  key_links:
    - from: "middleware.ts route gates"
      to: "lib/auth.ts toAppRole"
      via: "import + call, replacing userRole === \"admin\" style comparisons"
      pattern: "toAppRole|isAdminRole"
---

<objective>
Introduce the ADMIN/USER mapping helper and route `middleware.ts`'s role gates through it.

Purpose: The target role model is ADMIN / USER, collapsed at the APPLICATION LAYER ONLY.
A single helper in app code becomes the source of truth so no gate compares raw enum
strings. This also closes a real pre-existing bug: `PROFESSOR` appears in NEITHER
`STUDENT_ROUTES`' allow-list nor the admin gate, so a professor account is currently
locked out of both the student surfaces and the admin tooling.
Output: Helper functions in `lib/auth.ts`, middleware wired through them, the
`/api/student-history` route-array cleanup that 11-02 deliberately deferred to this plan
applied, and Postgres untouched.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-cohort-staff-teardown/11-CONTEXT.md
@.planning/phases/11-cohort-staff-teardown/11-RESEARCH.md
@.planning/phases/11-cohort-staff-teardown/11-02-SUMMARY.md
@.planning/phases/11-cohort-staff-teardown/11-03-SUMMARY.md
@lib/auth.ts
@middleware.ts
</context>

<constraints>
HARD CONSTRAINTS:
- APPLICATION LAYER ONLY. The Postgres `Role` enum keeps all four values —
  ADMIN, PROFESSOR, STUDENT, KIOSK. NO enum change, NO backfill, NO migration file, NO
  change to `prisma/schema.prisma`. A genuine enum collapse is an explicitly deferred phase.
- KIOSK IS OUT OF SCOPE AND MUST NOT BE FOLDED INTO EITHER BRANCH. Kiosk routes, the
  kiosk auto-login, and the KIOSK role keep their current behavior exactly. KIOSK remains
  a distinct third branch everywhere it is one today (`isKioskRoute` logic in
  `middleware.ts`, `app/api/auth/kiosk-auto-login/route.ts`).
- The student path must be COHORT-free, not ROLE-free. A role check gating `/interview`
  or `/case-play` is acceptable and expected.
- Do NOT rewrite `middleware.ts`'s structure, its route arrays, or its redirect behavior.
  Swap comparisons for helper calls. There is exactly ONE sanctioned route-array edit in
  this plan: removing a stale `/api/student-history` entry if — and only if — 11-02's
  SUMMARY reports one (Task 3). Nothing else in the arrays changes.
- This plan is the LAST writer of `middleware.ts` in Phase 11 (serialization is
  11-01 wave 1 -> 11-03 wave 2 -> 11-05 wave 3). 11-02 is a wave-2 sibling of 11-03 and
  was deliberately barred from editing this file, which is why its finding lands here.
- This is the most likely place in Phase 11 to silently lock out a real user. Enumerate
  every role x route outcome before and after, and make the diff auditable.
- Re-read `middleware.ts` fresh: 11-01 and 11-03 both already edited its route arrays.
  Do not trust research's line numbers.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Add the app-layer role mapping helper to lib/auth.ts</name>
  <files>lib/auth.ts</files>
  <action>
`lib/auth.ts` already owns this concern: it has the private
`roleToString(role: Role): string` (returns `role.toLowerCase()`, producing the
`"admin" | "professor" | "student" | "kiosk"` strings the JWT carries and middleware
compares against). Follow that file's existing single-responsibility pattern.

Add and EXPORT:

```ts
export type AppRole = "admin" | "user" | "kiosk";

/**
 * Phase 11: the ADMIN/USER role model, collapsed at the application layer ONLY.
 * The Postgres `Role` enum still has all four values — this is the single source of
 * truth for how app code interprets them.
 *   PROFESSOR -> admin   (professors keep access to the surviving operator tooling)
 *   STUDENT   -> user
 *   KIOSK     -> kiosk   (deliberately a distinct third branch; never folded in)
 */
export function toAppRole(role: string | Role | null | undefined): AppRole { ... }

/** True for ADMIN and PROFESSOR. Never true for KIOSK. */
export function isAdminRole(role: string | Role | null | undefined): boolean { ... }
```

Implementation requirements:
- Accept BOTH the Prisma `Role` enum values (`"ADMIN"`, `"PROFESSOR"`, `"STUDENT"`,
  `"KIOSK"`) and the lowercase JWT strings (`"admin"`, `"professor"`, `"student"`,
  `"kiosk"`). Normalize with `.toLowerCase()` first. Call sites carry both forms: the JWT
  payload is lowercase, Prisma reads are uppercase.
- `"admin"` and `"professor"` → `"admin"`. `"kiosk"` → `"kiosk"`. Everything else,
  including `"student"`, `"user"`, `null`, `undefined` and any unrecognized value →
  `"user"`. Defaulting unknown to `"user"` matches `createToken`, which already signs
  `role: user.role || "user"`, and is the fail-safe direction (least privilege).
- `isAdminRole(r)` is exactly `toAppRole(r) === "admin"`.
- Do NOT change `roleToString`, `createToken`, `createOrUpdateCWRUUser`, or any existing
  export. Note `createOrUpdateCWRUUser`'s comment: PROFESSOR/KIOSK roles are real and
  managed out-of-band, so the enum values must keep round-tripping unchanged.
- Add nothing else to this file.
  </action>
  <verify>
`grep -n "export function toAppRole\|export function isAdminRole\|export type AppRole" lib/auth.ts` → all three present.
`npx tsc --noEmit` → exit 0.
Behavior spot-check via `npx tsx -e` (or a scratch script under the scratchpad, deleted
after): `toAppRole("PROFESSOR") === "admin"`, `toAppRole("professor") === "admin"`,
`toAppRole("ADMIN") === "admin"`, `toAppRole("STUDENT") === "user"`,
`toAppRole("student") === "user"`, `toAppRole("KIOSK") === "kiosk"`,
`toAppRole(undefined) === "user"`, `isAdminRole("KIOSK") === false`.
`git diff --stat prisma/schema.prisma` → empty.
  </verify>
  <done>`toAppRole` and `isAdminRole` are exported from `lib/auth.ts`, handle both casings, keep KIOSK distinct, default unknown to `"user"`, and the Prisma schema is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Route middleware.ts's role gates through the helper</name>
  <files>middleware.ts</files>
  <action>
Re-read `middleware.ts` fresh first (11-01 and 11-03 already edited its route arrays).
Locate the three role comparison points by grep, not by line number:

```
grep -n "userRole" middleware.ts
```

Research found these three (line numbers approximate and now stale):
1. The student-surface gate, currently
   `userRole === "student" || userRole === "admin" || (isKioskRoute && userRole === "kiosk")`.
   Replace the role comparisons with helper calls so the allow-list becomes:
   `toAppRole(userRole) === "user" || isAdminRole(userRole) || (isKioskRoute && toAppRole(userRole) === "kiosk")`.
   **This is where the PROFESSOR lockout is fixed** — PROFESSOR now maps to admin and is
   admitted. That behavior change is INTENTIONAL and required by the locked decision; it
   is NOT a regression. Say so in a code comment and in the summary, so a later reader or
   verifier does not "fix" it back.
2. The kiosk-route gate, currently `userRole !== "kiosk" && userRole !== "admin"`.
   Rewrite as `toAppRole(userRole) !== "kiosk" && !isAdminRole(userRole)`. KIOSK behavior
   must be byte-for-byte identical for an actual kiosk account; the only change is that
   PROFESSOR now passes the admin half, consistent with point 1.
3. The admin-route gate, currently `userRole !== "admin"`. Rewrite as
   `!isAdminRole(userRole)`. Same intentional PROFESSOR admission.

Import the helpers from `lib/auth.ts`. If `middleware.ts` runs on the Edge runtime and
`lib/auth.ts` pulls in Node-only dependencies (crypto, Prisma) at module scope, do NOT
restructure `lib/auth.ts` to suit it — instead check whether middleware already imports
from `lib/auth.ts` today; if the import causes a bundling/runtime error, extract ONLY the
two pure helper functions plus `AppRole` into a dependency-free `lib/roles.ts` and have
`lib/auth.ts` re-export them so there is still exactly one source of truth. Prefer the
plain import; take the `lib/roles.ts` path only if the import actually fails.

Do not touch the route arrays, the redirect targets, the token verification, or anything
else in this file.
  </action>
  <verify>
`grep -n "userRole === \"admin\"\|userRole === \"student\"\|userRole !== \"admin\"\|userRole === \"professor\"\|\"professor\"" middleware.ts` → no raw role-string comparisons remain.
`grep -n "toAppRole\|isAdminRole" middleware.ts` → present at all three gate sites.
`grep -n "kiosk" middleware.ts` → the kiosk branch still exists as its own distinct condition.
`npx tsc --noEmit` → exit 0.
`npx next dev` boots with no middleware/module-resolution error, and an unauthenticated
request to an admin route still redirects/403s exactly as before (`curl -s -o /dev/null
-w "%{http_code}" http://localhost:3000/case-management`).
In the summary, include a role x surface table (ADMIN / PROFESSOR / STUDENT / KIOSK x
student routes / admin routes / kiosk routes) showing before vs after, with the only
changed cells belonging to PROFESSOR.
  </verify>
  <done>All three middleware gates call the helper, KIOSK remains a distinct branch, STUDENT and ADMIN behavior is unchanged, and PROFESSOR's newly-granted access is documented as the intended fix.</done>
</task>

<task type="auto">
  <name>Task 3: Apply 11-02's deferred middleware route-array cleanup</name>
  <files>middleware.ts</files>
  <action>
11-02 deleted the entire `app/api/student-history/**` tree but was barred from touching
`middleware.ts` (it is a wave-2 sibling of 11-03, which also writes that file). It
recorded its finding instead. Act on it now — this plan is the last writer of
`middleware.ts` in the phase.

1. Open `11-02-SUMMARY.md` and read its `## middleware.ts finding (for 11-05)` section.
   - If it says no `/api/student-history` entry exists: this task is a NO-OP. Record
     "no-op, 11-02 reported no entry" in the summary and stop. Do not invent work.
   - If it names one or more entries: remove exactly those entries from their route arrays
     (`ADMIN_ROUTES` / `STUDENT_ROUTES` / `PUBLIC_ROUTES`). Nothing else.
2. Independently re-confirm with `grep -n "student-history" middleware.ts` before and
   after — do not rely on the SUMMARY alone, since 11-03 also edited this file in wave 2
   and may have already removed it.
3. While you are here, walk EVERY remaining `/api/...` entry in the route arrays and `ls`
   its target route file. Any entry whose target no longer exists (deleted by 11-01, 11-02
   or the 11-03 checkpoint) is a dangling entry — remove it and list each removal in the
   summary. This is the phase's single consolidated route-array sweep; 11-07's static
   sweep asserts the result rather than fixing it.
4. Do NOT add any entry. Do NOT change a gate on a route that still exists. Do NOT touch
   the kiosk branch.
  </action>
  <verify>
`grep -n "student-history" middleware.ts` → no output.
Every `/api/...` string remaining in the route arrays resolves to an existing file (walk and `ls` each one; record the list in the summary).
`npx tsc --noEmit` → exit 0.
`npx next dev` boots with no middleware error.
`git diff middleware.ts` → contains only the Task 2 helper swaps plus the route-array removals listed in the summary; no additions.
  </verify>
  <done>`middleware.ts` has zero route-array entries pointing at a deleted route, 11-02's recorded finding is provably acted on (or provably a no-op), and every removal is itemized in the summary.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0.
- `grep -n "student-history" middleware.ts` → no output.
- `git diff --stat prisma/schema.prisma` → empty; `ls prisma/migrations` unchanged.
- `grep -c "PROFESSOR" prisma/schema.prisma` → still non-zero (enum intact).
- `git status --short app/kiosk app/api/auth/kiosk-auto-login` → no changes.
- `npx next dev` boots clean.
</verification>

<success_criteria>
Supports ROADMAP success criterion 1: role gating is now a single, auditable app-layer
ADMIN/USER mapping with no dependence on cohort membership, and no user is locked out
who should not be.
</success_criteria>

<output>
After completion, create `.planning/phases/11-cohort-staff-teardown/11-05-SUMMARY.md`
with the role x surface before/after table, the explicit note that PROFESSOR gaining
access is the intended fix to a pre-existing bug, and an itemized list of every
route-array entry removed in Task 3 (or an explicit "no-op" if 11-02 reported none).
</output>
