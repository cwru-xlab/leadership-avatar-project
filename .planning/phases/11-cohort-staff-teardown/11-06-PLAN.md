---
phase: 11-cohort-staff-teardown
plan: 06
type: execute
wave: 4
depends_on: ["11-05"]
files_modified:
  - app/api/interaction/finish/route.ts
  - app/api/interaction/get/route.ts
  - app/api/interaction/save/route.ts
  - app/api/cohort/get/route.ts
  - app/api/student/cases/route.ts
  - app/page.tsx
  - app/login/page.tsx
  - components/auth-navbar.tsx
autonomous: true
requirements: []   # Phase 11 has NO REQ IDs. Anchored on ROADMAP success criteria only.

must_haves:
  truths:
    - "No route handler, page, or component compares a raw role string — every role decision goes through the lib/auth.ts helper."
    - "A PROFESSOR account gets the same dashboard, post-login redirect, navbar, and privileged-data access as an ADMIN account."
    - "A STUDENT account still lands on the student dashboard with the student navbar and unchanged data access."
  artifacts:
    - path: "app/page.tsx"
      provides: "Dashboard branching via toAppRole instead of role === \"student\""
      pattern: "toAppRole|isAdminRole"
    - path: "components/auth-navbar.tsx"
      provides: "navItems vs studentNavItems selection via the helper"
      pattern: "toAppRole|isAdminRole"
  key_links:
    - from: "app/login/page.tsx post-login redirect"
      to: "lib/auth.ts toAppRole"
      via: "role branch — a professor must not be redirected to the student dashboard"
      pattern: "toAppRole"
---

<objective>
Sweep the remaining role-comparison call sites onto the helper: the duplicated
`isPrivileged` blocks in route handlers and the three `role === "student"` UI branches.

Purpose: The mapping helper is only a source of truth if nothing bypasses it. Research
found ~11 call sites total; 11-05 converted the three in `middleware.ts`, and this plan
converts the rest. Today these are inconsistent — `components/auth-navbar.tsx` already
treats PROFESSOR as admin-like (it just isn't `"student"`), while middleware blocked them
from the very routes those nav items link to.
Output: Every role decision in the app funnels through `toAppRole`/`isAdminRole`.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-cohort-staff-teardown/11-CONTEXT.md
@.planning/phases/11-cohort-staff-teardown/11-RESEARCH.md
@.planning/phases/11-cohort-staff-teardown/11-03-SUMMARY.md
@.planning/phases/11-cohort-staff-teardown/11-05-SUMMARY.md
@lib/auth.ts
</context>

<constraints>
HARD CONSTRAINTS:
- Two of the target files — `app/api/cohort/get/route.ts` and
  `app/api/student/cases/route.ts` — MAY HAVE BEEN DELETED by the user's decision at the
  11-03 checkpoint. Read `11-03-SUMMARY.md` first and SKIP any file that no longer exists.
  Do NOT recreate a deleted route. Do NOT delete a surviving one.
- Semantics must not change for ADMIN, STUDENT or KIOSK. The ONLY intended behavior delta
  is PROFESSOR being treated as ADMIN — which at these five route handlers is already the
  de-facto behavior (`role === "admin" || role === "professor"`), so those are pure
  refactors with zero behavior change.
- Do NOT fold KIOSK into either branch anywhere.
- Do NOT restructure these files, change response shapes, or touch anything outside the
  role comparison itself. Minimal churn.
- `prisma/schema.prisma` untouched, no migration, no data deleted.
- Do NOT touch `app/api/auth/kiosk-auto-login/route.ts` (`role: "kiosk"`) or
  `app/api/auth/cwru-sso-callback/route.ts` (`const role = isAdmin ? "admin" : "user"`).
  Both WRITE a role rather than gate on one, and both already produce values the helper
  handles correctly.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Replace the duplicated isPrivileged blocks in route handlers</name>
  <files>app/api/interaction/finish/route.ts, app/api/interaction/get/route.ts, app/api/interaction/save/route.ts, app/api/cohort/get/route.ts, app/api/student/cases/route.ts</files>
  <action>
Find every instance of the repeated pattern:

```
grep -rn "isPrivileged" app lib components
```

Research found five identical copies of
`const isPrivileged = currentUser.role === "admin" || currentUser.role === "professor";`
at `app/api/cohort/get/route.ts:12`, `app/api/interaction/finish/route.ts:140`,
`app/api/interaction/get/route.ts:32`, `app/api/interaction/save/route.ts:33`, and
`app/api/student/cases/route.ts:23`. Trust the grep, not the line numbers.

For each file that still exists (check `11-03-SUMMARY.md` — the cohort and student/cases
routes may be gone):
- Replace the local expression with `const isPrivileged = isAdminRole(currentUser.role);`
- Add the import from `lib/auth.ts`.
- Change NOTHING else — keep the variable name `isPrivileged` and every downstream use of
  it exactly as-is, so the diff is one line plus an import per file.

This is a pure refactor: `isAdminRole` returns true for exactly `admin` and `professor`,
which is what these five lines already computed.
  </action>
  <verify>
`grep -rn "role === \"admin\" || currentUser.role === \"professor\"\|=== \"professor\"" app lib components` → no output.
`grep -rn "isPrivileged = isAdminRole" app` → one hit per surviving file (3 to 5 depending on 11-03's outcome).
`git diff --stat` for this task → each touched file shows ~2 changed lines.
`npx tsc --noEmit` → exit 0.
  </verify>
  <done>Every surviving `isPrivileged` block delegates to `isAdminRole`; no raw `"professor"` string comparison remains in any route handler.</done>
</task>

<task type="auto">
  <name>Task 2: Route the three UI role branches through the helper</name>
  <files>app/page.tsx, app/login/page.tsx, components/auth-navbar.tsx</files>
  <action>
Find them with `grep -rn "role === \"student\"" app components`.

1. `app/page.tsx` (~line 47): `if (user?.role === "student")` chooses `InteractionDashboard`
   vs the admin dashboard cards. Change to `if (toAppRole(user?.role) === "user")`.
   (11-01 already removed the "Cohorts" `navigationCards` entry from this file — do not
   re-add it, and do not otherwise modify the array.)
2. `app/login/page.tsx` (~line 29): `if (meData.user?.role === "student")` post-login
   redirect. Change to `if (toAppRole(meData.user?.role) === "user")`. Consequence: a
   PROFESSOR now lands on the admin dashboard instead of the student one — intended.
3. `components/auth-navbar.tsx` (~line 65):
   `user?.role === "student" ? studentNavItems : navItems`. Change to
   `toAppRole(user?.role) === "user" ? studentNavItems : navItems`. Behavior for ADMIN,
   PROFESSOR and STUDENT is unchanged here (PROFESSOR already fell into the admin branch);
   the win is that it now agrees with middleware instead of coincidentally matching it.

Import `toAppRole` from `lib/auth.ts` in each. If any of these are client components and
the import fails at build/runtime because `lib/auth.ts` is server-only, use the
dependency-free helper module `lib/roles.ts` if 11-05 created one; if it did not, create
it then (pure functions only, `lib/auth.ts` re-exports from it) rather than duplicating
the mapping logic. There must remain exactly ONE implementation of the mapping.

Change nothing else in these three files.
  </action>
  <verify>
`grep -rn "role === \"student\"" app components` → no output.
`grep -rln "toAppRole" app/page.tsx app/login/page.tsx components/auth-navbar.tsx` → all three.
`grep -rn "function toAppRole" lib/` → exactly ONE definition across the repo.
`npx tsc --noEmit` → exit 0.
With `npx next dev`: log in as a student account → student dashboard + student navbar;
log in as an admin account → admin dashboard + admin navbar. Both unchanged from before.
  </verify>
  <done>No raw `role === "student"` comparison remains in any page or component, exactly one mapping implementation exists, and student/admin behavior is visibly unchanged.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0.
- `grep -rn "=== \"professor\"\|=== \"student\"" app lib components middleware.ts` → no output.
- `grep -rn "\"kiosk\"" app middleware.ts` → kiosk logic still present and distinct.
- `git diff --stat prisma/schema.prisma` → empty; `ls prisma/migrations` unchanged.
- `git status --short app/api/auth/kiosk-auto-login app/kiosk` → no changes.
</verification>

<success_criteria>
Supports ROADMAP success criterion 1: every role decision in the app is a single helper
call on an ADMIN/USER model, with no cohort dependency and no raw enum comparison left to
drift.
</success_criteria>

<output>
After completion, create `.planning/phases/11-cohort-staff-teardown/11-06-SUMMARY.md`,
listing each converted call site and noting which (if any) were skipped because 11-03's
checkpoint deleted them.
</output>
