---
phase: 11-cohort-staff-teardown
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - .planning/phases/11-cohort-staff-teardown/11-CALLER-MAP.md
  - app/api/cohort/**
  - app/api/codes/**
  - app/api/student/cases/route.ts
  - lib/cohort-storage.ts
  - types/cohort.ts
  - middleware.ts
autonomous: false
requirements: []   # Phase 11 has NO REQ IDs. Anchored on ROADMAP success criteria only.

must_haves:
  truths:
    - "A per-route caller map exists for every /api/cohort/*, /api/codes/* and /api/student/cases endpoint, re-verified against the post-deletion tree."
    - "The user has made an explicit per-route keep-or-delete decision; no API route was deleted on Claude's own judgment."
    - "Every route the user chose to keep is still reachable and still admin-gated exactly as before."
    - "Every route the user chose to delete returns a plain 404 and has no dangling middleware entry or importer."
  artifacts:
    - path: ".planning/phases/11-cohort-staff-teardown/11-CALLER-MAP.md"
      provides: "Per-route table: route -> call sites -> alive/dead after 11-01 deletions"
      contains: "/api/student/cases"
  key_links:
    - from: "middleware.ts route arrays"
      to: "surviving app/api/cohort + app/api/codes route files"
      via: "every remaining ADMIN_ROUTES entry must point at a route file that still exists"
      pattern: "/api/(cohort|codes)"
---

<objective>
Produce the mandatory caller map, put it in front of the user for a per-route decision,
and then apply exactly what they decide — nothing more.

Purpose: CONTEXT.md's "delete the UI, keep the APIs" was a CAUTION decision, not an
architectural one, and this checkpoint is what converts it into a real decision. The rule
is absolute: **do NOT delete an API route on Claude's own judgment.** The research already
did most of the legwork; this plan RE-VERIFIES that map against the actual post-11-01 tree
rather than re-deriving it, then presents it.
Output: `11-CALLER-MAP.md`, a recorded user decision per route, and the resulting
deletions/retentions applied cleanly.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-cohort-staff-teardown/11-CONTEXT.md
@.planning/phases/11-cohort-staff-teardown/11-RESEARCH.md
@.planning/phases/11-cohort-staff-teardown/11-01-SUMMARY.md
@middleware.ts
</context>

<constraints>
HARD CONSTRAINTS:
- Do NOT delete any `/api/cohort/*`, `/api/codes/*`, or `/api/student/cases` route file
  without an explicit user decision for THAT route at the Task 2 checkpoint.
- Do NOT loosen or add authentication gates. Kept routes stay admin-gated exactly as they
  are today.
- `prisma/schema.prisma` is NOT modified; no migration file is produced; no data deleted.
- Do NOT touch `lib/s3-client.ts` — its `saveCohort/getCohort/getCohortByAccessCode/
  deleteCohort/listCohorts` methods live in one shared class also serving avatars, chat,
  profiles, cases and interaction logs.
- `types/cohort.ts` may be deleted ONLY if, after the approved deletions, nothing imports
  it. Note `lib/s3-client.ts` imports `type { Cohort }` from it — so if `lib/s3-client.ts`
  keeps that import, `types/cohort.ts` STAYS. Verify by grep, do not assume.
- KIOSK untouched.
- Do NOT edit role-comparison logic in `middleware.ts` — this plan only prunes route
  arrays. Role checks belong to 11-05.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify the caller map against the post-deletion tree and write 11-CALLER-MAP.md</name>
  <files>.planning/phases/11-cohort-staff-teardown/11-CALLER-MAP.md</files>
  <action>
Start from the table in `11-RESEARCH.md` Section B — do NOT re-derive it from scratch.
Re-verify each row against the CURRENT tree (post-11-01 page deletions) with a grep per
route, e.g.:

```
grep -rn "api/cohort/add" app lib components --include=*.ts --include=*.tsx
grep -rn "api/codes/" app lib components --include=*.ts --include=*.tsx
grep -rn "api/student/cases" app lib components middleware.ts
grep -rn "cohortStorage\." app lib components
```

Write `.planning/phases/11-cohort-staff-teardown/11-CALLER-MAP.md` with three tables —
`/api/cohort/*` (add, delete, edit, get, join, list, send-invitations), `/api/codes/*`
(gradebook, learner-performance, and the five `student/[studentEmail]/*/[caseId]` routes),
and `/api/student/cases` — each with columns:
`Route | Call sites BEFORE 11-01 | Call sites NOW | Verdict`.

Research's expected verdicts, to confirm or correct:
- Every route in all three groups drops to zero live callers after the 11-01 page deletions.
- `/api/codes/[codeId]/learner-performance` and `/api/student/cases` were already at zero
  callers BEFORE Phase 11 started.
- `/api/cohort/get` has one surviving textual reference: the effect at
  `app/case-play/[caseId]/page.tsx` ~lines 303-328, gated on `cohortId` being truthy.
  `cohortId` comes from `searchParams.get("cohortId") || ""` and nothing sets that query
  param anymore, so that path is unreachable at runtime. Record it as
  "1 textual reference, 0 reachable callers" — be precise, do not round it to zero.

Add a fourth short section, "Adjacent items whose fate tracks these routes":
- `lib/cohort-storage.ts` — client-side fetch wrapper around `/api/cohort/*`; all its
  importers were deleted in 11-01; dead after deletion, kept only because its target API
  is kept.
- `types/cohort.ts` — currently imported by `app/api/cohort/add/route.ts` AND
  `lib/s3-client.ts` (the latter survives regardless). State the grep result.
- `PUBLIC_ROUTES` entries `/api/cohort/join` and `/api/cohort/get` — CONTEXT grants Claude
  discretion here, but research recommends asking anyway since it is a security-relevant
  default. State the finding: after the page deletions nothing on any auth path needs
  either to be public, and `/case-play` callers are already authenticated.
  </action>
  <verify>
`test -f .planning/phases/11-cohort-staff-teardown/11-CALLER-MAP.md` succeeds.
The file contains a row for all 7 `/api/cohort/*` routes, all 7 `/api/codes/*` routes, and `/api/student/cases`.
No API route file has been deleted yet: `ls app/api/cohort app/api/codes app/api/student/cases` all still present.
  </verify>
  <done>11-CALLER-MAP.md exists with a verified per-route verdict for all 15 routes plus the adjacent-items section, and zero route files have been touched.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: CHECKPOINT — per-route keep-or-delete decision</name>
  <action>
Present the full contents of `11-CALLER-MAP.md` to the user and collect a per-route
keep-or-delete decision, plus the PUBLIC_ROUTES answer. Do NOT delete any route file in
this task — this task only gathers the decision. Record the user's exact words so Task 3
and the 11-03 SUMMARY can be applied and audited against them.
  </action>
  <decision>For each of the 15 checkpoint-gated API routes: keep it (admin-gated, unchanged) or delete it? Plus: do `/api/cohort/join` and `/api/cohort/get` leave `PUBLIC_ROUTES`?</decision>
  <context>
CONTEXT.md's "keep the APIs" was caution, because it was not certain what still calls
them. The caller map now answers that. Present the full contents of
`11-CALLER-MAP.md` to the user verbatim — all three route tables plus the
adjacent-items section — and summarize the bottom line: every gated route drops to zero
live callers after the page deletions, and two were already dead before Phase 11 began.
No route may be deleted without the user saying so for that route.
  </context>
  <options>
    <option id="delete-all">
      <name>Delete all 15 routes (plus lib/cohort-storage.ts)</name>
      <pros>Leaves no dead code; makes the deferred schema-cleanup phase trivially unblocked.</pros>
      <cons>Irreversible in this phase if an out-of-repo consumer exists that the caller map cannot see.</cons>
    </option>
    <option id="keep-all">
      <name>Keep all 15 routes exactly as they are, admin-gated</name>
      <pros>Zero risk; matches CONTEXT.md's literal default.</pros>
      <cons>Leaves an entire dead API surface behind; schema cleanup stays blocked.</cons>
    </option>
    <option id="per-route">
      <name>Per-route mix — the user names which routes go and which stay</name>
      <pros>Exactly what the checkpoint rule was written for.</pros>
      <cons>Requires walking the table row by row.</cons>
    </option>
  </options>
  <resume-signal>Reply with one of: "delete-all", "keep-all", or a per-route list of which routes to delete. Also answer: remove `/api/cohort/join` and `/api/cohort/get` from `PUBLIC_ROUTES`? (yes/no)</resume-signal>
  <verify>The user's decision covers every one of the 15 routes plus the PUBLIC_ROUTES question, and it is written down verbatim before Task 3 begins.</verify>
  <done>An explicit, recorded user decision exists for each route; zero route files have been modified in this task.</done>
</task>

<task type="auto">
  <name>Task 3: Apply exactly the user's decisions</name>
  <files>app/api/cohort/**, app/api/codes/**, app/api/student/cases/route.ts, lib/cohort-storage.ts, types/cohort.ts, middleware.ts</files>
  <action>
Apply ONLY what the user approved in Task 2. For each route approved for deletion:
1. `git rm` the route file (and its now-empty parent directories).
2. Remove its matching entry from `middleware.ts`'s `ADMIN_ROUTES` / `STUDENT_ROUTES` /
   `PUBLIC_ROUTES`. Careful with prefix entries: `/api/codes` covers the whole `/api/codes/*`
   family, so only remove it if ALL `/api/codes/*` routes were deleted.
3. Do not add, loosen, or restructure any gate on a route that survives.

Then the adjacent items:
- `lib/cohort-storage.ts`: delete it only if ALL `/api/cohort/*` routes it wraps were
  deleted, OR the user explicitly approved deleting it. Otherwise keep it untouched.
- `types/cohort.ts`: after the deletions, run
  `grep -rn "types/cohort" app lib components`. Delete it ONLY if that returns zero hits.
  Expect it to SURVIVE, because `lib/s3-client.ts` imports `type { Cohort }` from it and
  `lib/s3-client.ts` is not being touched.
- `PUBLIC_ROUTES`: if the user said yes, remove the `/api/cohort/join` and
  `/api/cohort/get` entries. If no, leave both.

If the user chose `keep-all` and answered "no" on PUBLIC_ROUTES, this task is a no-op —
record that outcome in the summary rather than inventing work.

Do NOT touch `app/case-play/[caseId]/page.tsx` in this plan even if the user mentions the
unreachable `/api/cohort/get` effect — that file belongs to 11-04. If the user asked for
that effect to be removed, record the request in the SUMMARY as an instruction for 11-04.
  </action>
  <verify>
`npx tsc --noEmit` → exit 0, zero errors.
For each deleted route: `ls <route path> 2>&1` → "No such file or directory", and
`grep -n "<route path fragment>" middleware.ts` → no output.
For each kept route: file still present, and `git diff <file>` → empty (unchanged, still admin-gated).
`grep -rn "types/cohort" app lib components` → matches the keep/delete decision recorded above.
`git diff --stat prisma/schema.prisma lib/s3-client.ts` → empty.
`npx next dev` boots; a deleted route returns 404; a kept route still returns its normal
admin-gated response (401/403 when unauthenticated, not 404).
  </verify>
  <done>The working tree matches the user's per-route decision exactly — no route deleted that they did not name, no route kept that they said to delete — and typecheck is clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0.
- `git diff --stat prisma/schema.prisma lib/s3-client.ts app/kiosk` → empty.
- `ls prisma/migrations` unchanged (no migration produced).
- No `git status` change to `app/case-play/`, `app/api/interaction/`, or `app/api/scenario/`.
- `11-CALLER-MAP.md` exists and the SUMMARY records the user's verbatim decision.
</verification>

<success_criteria>
Supports ROADMAP success criterion 1: the cohort/assignment API surface has been
adjudicated route by route by the user, with the caution rule honored — nothing deleted
on Claude's judgment, nothing surviving with a weakened gate.
</success_criteria>

<output>
After completion, create `.planning/phases/11-cohort-staff-teardown/11-03-SUMMARY.md`.
Record the user's per-route decision verbatim — the deferred schema-cleanup phase is
gated on it, and 11-06 needs to know which role-check route handlers still exist.
</output>
