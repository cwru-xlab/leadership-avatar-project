---
phase: 11-cohort-staff-teardown
plan: 04
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - app/api/interaction/start/route.ts
autonomous: true
requirements: []   # Phase 11 has NO REQ IDs. Anchored on ROADMAP success criteria only.

must_haves:
  truths:
    - "Starting a Case Study interaction succeeds with no cohortId — the admin case-play start flow does not 400 after the cohort pages are deleted."
    - "The /api/interaction/start request and response shapes are unchanged: cohortId is still accepted, still stored, just no longer required."
    - "Any user can publish their own scenario to the shared library and sees their own drafts, with no staff or cohort involvement."
  artifacts:
    - path: "app/api/interaction/start/route.ts"
      provides: "Validation guard that no longer requires a truthy cohortId"
      contains: "cohortId"
    - path: "app/api/scenario/publish/route.ts"
      provides: "Owner-scoped published toggle (pre-existing from Phase 9, unchanged)"
    - path: "app/api/scenario/list/route.ts"
      provides: "{ mine, shared } projection excluding ownerId and cohortIds (pre-existing, unchanged)"
  key_links:
    - from: "app/case-play/[caseId]/page.tsx handleStart (!isScenario branch)"
      to: "/api/interaction/start"
      via: "POST with cohortId: \"\" — must return 200, not 400"
      pattern: "api/interaction/start"
    - from: "app/api/scenario/publish/route.ts"
      to: "loadOwnedScenario(body.id, currentUser.id)"
      via: "owner scoping — 404s if caller does not own the scenario"
      pattern: "loadOwnedScenario"
---

<objective>
Fix the one real bug that the page deletions surface, and verify — without editing — that
the rest of the live student path is already neutralized and already owner-scoped.

Purpose: `app/api/interaction/start/route.ts` hard-requires `cohortId`, which
`app/case-play/[caseId]/page.tsx` only ever received from a query param set by links on
the pages deleted in 11-01. Left alone, the admin Case Study start flow 400s on EVERY
attempt. This is the ONE place in Phase 11 where "neutralize, don't restructure" demands
a genuine behavior change. Everything else in this area is already inert and must be
confirmed, not modified.
Output: A one-line validation relaxation, plus a documented audit proving the scenario
routes, case-play, and the self-service publishing path need no changes.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-cohort-staff-teardown/11-CONTEXT.md
@.planning/phases/11-cohort-staff-teardown/11-RESEARCH.md
@app/api/interaction/start/route.ts
</context>

<constraints>
HARD CONSTRAINTS:
- MINIMAL CHURN IS THE POINT. `app/api/scenario/{add,edit,list}/route.ts`,
  `app/api/interaction/start/route.ts` and `app/case-play/[caseId]/page.tsx` carry working
  Phase 9 and Phase 10 behavior; churn there is the main regression risk in this phase.
  Neutralize, do not restructure.
- Do NOT change any request or response shape. `cohortId` stays in the destructure, stays
  in the `InteractionLog` object, stays in `types/index.ts`
  (`CaseStudy.cohortIds`, `InteractionLog.cohortId` are explicitly meant to survive).
- Do NOT re-implement self-service publishing. It is ALREADY BUILT by Phase 9. This plan
  verifies it; it does not touch it.
- `app/case-play/[caseId]/page.tsx` is the single file Phase 10 and Phase 11 both touch
  most. Phase 10's work is in the `isScenario === true` branches; Phase 11's interest is
  the `isScenario === false` branches. RE-READ THE FILE FRESH before looking at any line
  number, and re-run `git log --oneline -5` / `git status` first — do not trust a stale
  mental model of its ~2000 lines.
- `prisma/schema.prisma` untouched, no migration, no data deleted, KIOSK untouched.
- Do NOT touch `app/api/cohort/**`, `app/api/codes/**`, or `app/api/student/cases/**` —
  checkpoint-gated in 11-03.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Relax the cohortId requirement in /api/interaction/start</name>
  <files>app/api/interaction/start/route.ts</files>
  <action>
The guard near line 11 currently reads:

```ts
if (!studentEmail || !caseId || !cohortId || !mode) {
  return NextResponse.json(
    { error: "Missing required fields: studentEmail, caseId, cohortId, mode" },
    { status: 400 }
  );
}
```

Make exactly these changes:
- Drop `|| !cohortId` from the condition.
- Update the error string to drop `cohortId` from the list of required fields (it is no
  longer required, so the stale message would mislead).

Do NOT remove `cohortId` from the body destructure on line ~9. Do NOT remove the
`cohortId,` entry from the `InteractionLog` object at line ~38. The field stays present
and gets persisted as `""` — "fields stay present but are always null/ignored" is the
locked decision, and keeping them preserves the request/response shape exactly.

Add a short comment above the guard noting that `cohortId` is retained for shape
compatibility but is no longer populated by any caller (Phase 11).

Nothing else in this file changes.
  </action>
  <verify>
`grep -n "!cohortId" app/api/interaction/start/route.ts` → no output.
`grep -n "cohortId" app/api/interaction/start/route.ts` → still shows the destructure and the InteractionLog field.
`git diff --stat app/api/interaction/start/route.ts` → a handful of changed lines only, all inside the guard/comment.
`npx tsc --noEmit` → exit 0.
Runtime check with `npx next dev` running:
`curl -s -o /dev/null -w "%{http_code}" -X POST http://localhost:3000/api/interaction/start -H 'Content-Type: application/json' -d '{"studentEmail":"x@y.z","caseId":"test","caseName":"t","cohortId":"","mode":"text","language":"en"}'`
→ NOT 400 for a missing-field reason. (An auth 401/403 is an acceptable result here — the
point is that the field-validation 400 is gone. If a 400 still comes back, read its body:
it must not mention a missing cohortId.)
  </verify>
  <done>A POST to /api/interaction/start with `cohortId: ""` no longer fails field validation, and the request/response shape is byte-identical apart from the relaxed guard.</done>
</task>

<task type="auto">
  <name>Task 2: Audit the remaining live student files — confirm inert, change nothing</name>
  <files>(read-only audit: app/api/scenario/add/route.ts, app/api/scenario/edit/route.ts, app/api/scenario/list/route.ts, app/case-play/[caseId]/page.tsx)</files>
  <action>
Re-read each file FRESH (especially `app/case-play/[caseId]/page.tsx` — see constraints)
and confirm the already-neutralized state. Expected findings, each to be confirmed or
corrected in the summary:

- `app/api/scenario/add/route.ts` (~line 88): writes `cohortIds: []` always. Inert. No edit.
- `app/api/scenario/edit/route.ts` (~line 65): carries `cohortIds: existing.cohortIds`
  forward unchanged. Inert. No edit.
- `app/api/scenario/list/route.ts`: `toSharedProjection` already omits `cohortIds` (and
  `ownerId`) from the shared projection. Inert. No edit.
- `app/case-play/[caseId]/page.tsx` ~line 108: `const cohortId = searchParams.get("cohortId") || ""`.
  Nothing in the app sets that query param anymore (every link that did lived in `/codes`
  or `/cohort-management`, deleted in 11-01). Permanently `""`. No edit required.
- `app/case-play/[caseId]/page.tsx` ~lines 303-328: the `avatar-time-limit` effect that
  fetches `/api/cohort/get`, gated on `if (!user?.email || !cohortId || !caseId) return;`.
  Permanently short-circuits. **LEAVE IT IN PLACE** — deleting it would be restructuring,
  which the locked decision forbids. The ONLY exception: if 11-03's SUMMARY records that
  the user explicitly asked at the checkpoint for this effect to be removed, remove just
  that effect block and nothing else.
- `app/case-play/[caseId]/page.tsx` ~line 731: `cohortId` passed into the
  `/api/interaction/start` body in the `!isScenario` branch. No edit — Task 1 is what
  makes this safe.

Then verify the self-service publishing path against success criterion 2 ("Individual
users create and own all of their own practice work"). Read, do not modify:
- `app/api/scenario/publish/route.ts` — POST, owner-scoped via
  `loadOwnedScenario(body.id, currentUser.id)` (404s for non-owners), toggles `published`.
- `app/api/scenario/list/route.ts` — returns `{ mine, shared }`: `mine` = caller's own
  scenarios in any publish state; `shared` = other users' `published === true` scenarios.
- `app/case-play/page.tsx` — admin cases via `/api/case/list?publishedOnly=true` filtered
  to `!c.ownerId`; student scenarios via `/api/scenario/list`.

Confirm in the SUMMARY that decision 10 is satisfied by pre-existing Phase 9 code with NO
staff or cohort involvement, and that legacy admin-authored S3 cases remain a published,
unowned library visible to everyone. If anything genuinely falls short of the decision,
do NOT fix it here — record it as a finding so it can be planned deliberately.
  </action>
  <verify>
`git status --short app/api/scenario app/case-play` → no modifications (unless the 11-03
checkpoint explicitly authorized removing the case-play cohort effect, in which case
exactly one file shows exactly that one hunk).
`grep -rn "cohortId\|cohortIds" app/api/scenario/` → shows only the inert `[]` /
pass-through / projection-omission references described above.
`grep -n "loadOwnedScenario" app/api/scenario/publish/route.ts` → present.
`npx tsc --noEmit` → exit 0.
  </verify>
  <done>Every live student file is confirmed inert with respect to cohorts, no unnecessary edit was made, and the publishing path is documented as already satisfying success criterion 2.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0.
- `git diff --stat` for this plan shows `app/api/interaction/start/route.ts` only (plus at most the one authorized case-play hunk).
- `git diff --stat prisma/schema.prisma` → empty; `ls prisma/migrations` unchanged.
- `git status --short app/api/cohort app/api/codes app/api/student app/kiosk` → no changes.
</verification>

<success_criteria>
Supports criterion 1 (the case-play start path no longer depends on a cohort in any
functional way) and criterion 2 (self-service, owner-scoped publishing verified end to
end in code).
</success_criteria>

<output>
After completion, create `.planning/phases/11-cohort-staff-teardown/11-04-SUMMARY.md`,
including the publishing-path verification finding and an explicit note that the
`/api/interaction/start` guard change was a deliberate, required behavior change — not
churn.
</output>
