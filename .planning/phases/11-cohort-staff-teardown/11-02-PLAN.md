---
phase: 11-cohort-staff-teardown
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - app/api/student-history/**
  - lib/student-history-service.ts
autonomous: true
requirements: []   # Phase 11 has NO REQ IDs. Anchored on ROADMAP success criteria only.

must_haves:
  truths:
    - "No API route remains whose only purpose was serving the deleted teacher/student-history monitoring pages."
    - "No live application code reads the Prisma Attempt or CaseAssignment models (seed/operator scripts excepted)."
    - "The app type-checks clean with the legacy tree removed."
    - "middleware.ts is NOT modified by this plan; any stale /api/student-history route-array entry is recorded in the SUMMARY for 11-05 to remove."
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Cohort, CohortMember, CaseAssignment, Attempt, CohortMemberStatus — ALL STILL PRESENT, unchanged"
      contains: "model Attempt"
  key_links:
    - from: "app (live code)"
      to: "prisma.attempt / prisma.caseAssignment"
      via: "must have ZERO live call sites after this plan"
      pattern: "prisma\\.(attempt|caseAssignment)"
---

<objective>
Delete the legacy `app/api/student-history/**` route tree and its sole backing service,
`lib/student-history-service.ts`.

Purpose: This tree was MISSED by CONTEXT.md's blast-radius list. It is a separate legacy
API family — NOT `/api/cohort/*`, NOT `/api/codes/*`, NOT `/api/student/cases` — so the
checkpoint caution rule does not cover it. Its only callers anywhere in the app were
`app/student-history/**` and `app/teacher/class/**`, both deleted in 11-01. It falls
squarely under the granted discretion: "whether the old Attempt / CaseAssignment tracking
path is dead enough to remove at the code level." It is, and removing it is what makes
the live app free of the old assignment-tracking record type.
Output: 11 route files plus one service lib deleted; a documented rationale; schema
untouched.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-cohort-staff-teardown/11-CONTEXT.md
@.planning/phases/11-cohort-staff-teardown/11-RESEARCH.md
@.planning/phases/11-cohort-staff-teardown/11-01-SUMMARY.md
</context>

<constraints>
HARD CONSTRAINTS:
- `prisma/schema.prisma` is NOT modified. `Attempt`, `CaseAssignment`, `Cohort`,
  `CohortMember`, `CohortMemberStatus` and the `User` relations ALL STAY. Schema cleanup
  is an explicitly deferred, separate phase. No migration file is produced.
- No data is deleted — no Postgres rows, no S3 objects. `lib/student-history-service.ts`
  going away deletes zero S3 bytes.
- Do NOT touch `app/api/cohort/**`, `app/api/codes/**`, or `app/api/student/cases/**` —
  checkpoint-gated in 11-03.
- Do NOT touch `prisma/seed.ts` or `scripts/sync-s3-to-db.ts`. They reference `Attempt`
  and `CaseAssignment` directly, the schema stays, so they keep compiling. They are
  operator/seed tooling, explicitly out of scope.
- Do NOT touch `lib/s3-client.ts`.
- Do NOT edit `middleware.ts`. It is serialized 11-01 (wave 1) -> 11-03 (wave 2) ->
  11-05 (wave 3). This plan runs in wave 2 IN PARALLEL with 11-03, so any edit here — even
  a one-line route-array removal — is a parallel-write collision. `middleware.ts` is
  deliberately absent from this plan's `files_modified` and must show a zero diff.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Prove the tree is dead, then delete it</name>
  <files>app/api/student-history/**, lib/student-history-service.ts</files>
  <action>
First PROVE, against the post-11-01 tree, that nothing live calls this:

```
grep -rn "api/student-history" app lib components --include=*.ts --include=*.tsx
grep -rln "student-history-service" app lib components
```

Expected after 11-01: the only hits are inside `app/api/student-history/**` itself
(self-references) and `lib/student-history-service.ts`. If ANY hit is outside those two
locations, STOP and report it in the summary instead of deleting — an unexpected live
caller invalidates the discretion call.

Then `git rm -r`:
- `app/api/student-history/` — all 11 route files: `search-students`, `search-cases`,
  `search-sections`, `section/[sectionId]`, `section/[sectionId]/cases`,
  `section/[sectionId]/students`, `overview/[sectionId]/[studentId]`,
  `gradebook/[classId]/[caseId]`, `detail/[sectionId]/[studentId]/[caseId]`,
  `interaction-log/[sectionId]/[studentId]/[caseId]`
- `lib/student-history-service.ts`

DO NOT EDIT `middleware.ts` IN THIS PLAN, UNDER ANY CIRCUMSTANCE — see constraints.

Instead, RECORD the finding so 11-05 can act on it. Run:

```
grep -n "student-history" middleware.ts
```

and write the verbatim result into the 11-02 SUMMARY under a heading
`## middleware.ts finding (for 11-05)`, as EITHER:
- the exact matching line(s), with the route array each sits in (`ADMIN_ROUTES` /
  `STUDENT_ROUTES` / `PUBLIC_ROUTES`) and the exact entry string to remove; OR
- the explicit sentence "No `/api/student-history` entry exists in `middleware.ts` — no
  cleanup needed."

One of those two must appear in the SUMMARY — do not omit the section. 11-05 depends on
this plan, loads this SUMMARY, and removes any entry you report there. Research did not
list such an entry, so "no cleanup needed" is the expected outcome.
  </action>
  <verify>
`ls app/api/student-history lib/student-history-service.ts 2>&1` → "No such file or directory" for both.
`grep -rn "student-history" app lib components` → no output.
`git diff --stat middleware.ts` → EMPTY (this plan must not modify middleware.ts).
`grep -n "student-history" middleware.ts` → whatever it returns is RECORDED in the SUMMARY, never fixed here.
`npx tsc --noEmit` → exit 0, zero errors.
  </verify>
  <done>Both paths are gone, no dangling references in app/lib/components, typecheck clean, `middleware.ts` unmodified, and the middleware finding is written into the SUMMARY for 11-05.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the Attempt/CaseAssignment path is dead in live code and record the rationale</name>
  <files>(read-only audit — no source files modified)</files>
  <action>
Run the audit that justifies this deletion as a discretion call:

```
grep -rn "prisma\.attempt\|prisma\.caseAssignment" app lib components scripts prisma
grep -rn "Attempt\b\|CaseAssignment" app lib components --include=*.ts --include=*.tsx
```

Expected result: the only remaining hits are in `prisma/seed.ts` and
`scripts/sync-s3-to-db.ts` (operator/seed tooling, deliberately left alone) and in
`prisma/schema.prisma` (deliberately unchanged). ZERO hits in `app/`, `lib/`, or
`components/`.

If a hit appears in `app/`/`lib/`/`components/` that is NOT inside a
checkpoint-gated route (`app/api/cohort/**`, `app/api/codes/**`,
`app/api/student/cases/**`), do not modify it — record it in the summary as a finding for
the 11-03 checkpoint conversation.

In the SUMMARY, write an explicit "Discretion call" section stating: (a) the tree was
outside the three checkpoint-gated API groups named in CONTEXT.md, (b) its only callers
were pages deleted in 11-01, (c) the audit above showed zero live readers of
Attempt/CaseAssignment, and (d) the Prisma schema was left fully intact regardless,
because schema cleanup is a deferred phase gated on the 11-03 checkpoint.
  </action>
  <verify>
`grep -rn "prisma\.attempt\|prisma\.caseAssignment" app lib components` → no output.
`git diff --stat prisma/schema.prisma` → empty.
`grep -c "model Attempt" prisma/schema.prisma` → 1 (still present, proving schema untouched).
  </verify>
  <done>Zero live readers of Attempt/CaseAssignment outside seed/operator scripts; schema verifiably unchanged; rationale documented in the SUMMARY.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0.
- `git diff --stat prisma/schema.prisma` → empty; `ls prisma/migrations` unchanged.
- `git status --short app/api/cohort app/api/codes app/api/student prisma/seed.ts scripts/sync-s3-to-db.ts lib/s3-client.ts app/kiosk middleware.ts` → no changes.
- `git diff --stat middleware.ts` → empty. The SUMMARY contains the `## middleware.ts finding (for 11-05)` section.
</verification>

<success_criteria>
Supports ROADMAP success criterion 1: the legacy per-student monitoring API tree is gone
and no live surface reads the old assignment-tracking record types, while the database
schema and all data remain untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/11-cohort-staff-teardown/11-02-SUMMARY.md`
including the "Discretion call" rationale section AND the
`## middleware.ts finding (for 11-05)` section.
</output>
