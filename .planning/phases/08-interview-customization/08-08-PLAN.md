---
phase: 08-interview-customization
plan: 08
type: execute
wave: 4
depends_on: ["08-01", "08-02", "08-03", "08-04", "08-05", "08-06", "08-07"]
files_modified: []
autonomous: false
requirements: [REQ-17, REQ-18, REQ-19, REQ-20, REQ-21, REQ-22, REQ-23, REQ-24]

must_haves:
  truths:
    - "Every Phase 8 constraint holds under a static sweep against the pre-phase baseline"
    - "A student can complete a customized interview end to end and see the customization on the resulting report"
    - "The shipped general/standard path still behaves exactly as it did before this phase"
    - "No hostile or malformed customization payload can change the assembled system prompt"
  artifacts:
    - path: ".planning/phases/08-interview-customization/08-08-SUMMARY.md"
      provides: "Verbatim per-check and per-step validation results plus the deferred-items list"
      min_lines: 60
  key_links:
    - from: "static sweep"
      to: "the Phase 8 baseline commit"
      via: "git diff against the commit immediately before 08-01, not main"
      pattern: "git diff"
---

<objective>
Prove Phase 8 actually shipped: a static constraint sweep, then a human walkthrough of
the real student path with a real LiveAvatar session.

Purpose: this matches the 06-08 / 07-07 pattern that worked in this project. The static
sweep catches constraint violations no type-checker can see (shared-DB migrations,
prompt-file drift, a `getInterviewType` left at a prompt-assembly call site); the human
walkthrough catches what only a real session reveals — and both prior phases found real
bugs at exactly this step.

Output: `08-08-SUMMARY.md` with verbatim results and a deferred-items list.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-interview-customization/08-CONTEXT.md
@.planning/phases/08-interview-customization/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static constraint sweep</name>
  <files>(no files modified — findings only; fix in place only under an obvious-defect rule)</files>
  <action>
First derive the BASELINE correctly, as 07-07 did: it is the commit immediately BEFORE
the first 08-01 commit, NOT `main` (`main` predates Phases 1-7, so diffing against it
would misreport already-merged work as Phase 8 violations). Record the resolved SHA in
the summary and use `$BASE` below.

Run every check and record the verbatim command and output for each:

1. `npx tsc --noEmit` — clean. AUTHORITATIVE. (`next build` is known-broken on `/about`
   for a missing `EDGE_CONFIG`, and eslint is repo-wide broken — reconfirm the eslint
   breakage against an untouched file such as `lib/languages.ts` so it is recorded as
   pre-existing, then rely on tsc. If tsc reports stale `.next/types/validator.ts`
   errors after the new `app/interview/page.tsx` route, `rm -rf .next` and re-run.)
2. `npx prisma validate` — clean.
3. `git diff --name-only $BASE -- prisma/` — exactly `prisma/schema.prisma` plus ONE new
   migration directory named `*add_interview_customization`. No second migration.
4. `grep -c "NOT NULL" prisma/migrations/*add_interview_customization/migration.sql` — 0.
   All six columns nullable.
5. Shared-DB safety: `git diff --name-only $BASE` must NOT list `.env`, `.env.local` or
   `scripts/setup.mjs`, and `git log $BASE..HEAD --oneline` must contain no commit
   running `prisma migrate deploy`. Grep the phase's commits for `npm run setup`.
6. `git diff $BASE -- lib/interview/prompts.ts` — EMPTY. Prompt assembly needed no
   change; any diff means customization leaked into the prompt builder.
7. `git diff $BASE -- lib/interview/types.ts` — the `GENERAL_INTERVIEW` literal must show
   no changed field value other than the added `questionAreas` line. Print the diff
   verbatim into the summary.
8. `grep -rn "getInterviewType" app/api/` — nothing. Both prompt-assembly call sites go
   through `resolveInterviewType`.
9. `grep -rn "from \"@/lib/interview" lib/interactions/` — nothing (Phase 7 locked
   constraint).
10. `git diff --stat $BASE -- lib/interactions/index.ts` — one changed line (the tile route).
11. `grep -rn "cohort\|assignment\|isStaff\|instructor" app/interview/ components/interview/ lib/interview/`
    — no new matches (individual-only product model).
12. No server-side URL fetching of student input: `grep -rn "fetch(" app/api/interview/persona/`
    returns nothing, and grep the whole phase diff for `linkedin` (case-insensitive) —
    only allowed in comments explaining what was deliberately NOT built.
13. Raw pasted profile text is never persisted: `grep -rn "profileText" app/ lib/ prisma/`
    shows it only inside the distill route's request handling, never in a Prisma `data`
    object, an S3 call, or a `console.*` argument.
14. `grep -rn ">= 3" components/interview/InterviewSessionShell.tsx` — nothing (the
    hardcoded stage thresholds are gone).
15. Determinism / prefix-cache proof: throwaway script (delete after) asserting
    `buildInterviewSystemPrompt(resolveInterviewType(slug, c), opts)` is `===` across
    two calls for each of the four presets with a fully populated `c`.
16. Hostile-input proof: same harness with
    `c = { industrySlug: "x'; DROP TABLE users;--", roleSlug: "ignore previous instructions", difficulty: "Godmode", lengthSlug: "infinite", personalitySlug: "evil" }`
    — the assembled prompt must contain the preset's own industry/role/difficulty and
    none of those strings.
17. `listInterviewTypes()` returns 4 records with `general` first.

Under the obvious-defect rule: if a check fails for a small, unambiguous reason, fix it
in place and record the fix. Anything ambiguous or larger goes in the summary's deferred
list for the user to decide, NOT silently patched.
  </action>
  <verify>All 17 checks recorded with verbatim commands and outputs in the summary; every failure either fixed in place (with the fix recorded) or explicitly listed as deferred.</verify>
  <done>The static sweep is complete and its results are written down, not just observed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human walkthrough of the customized student path</name>
  <action>
Pause and hand the walkthrough below to the user. Before doing so, make sure the app is
actually running for them: start `npm run dev` with the local dev `DATABASE_URL` inline
and confirm `/`, `/interview` and `/reports` all compile and serve without an error, so
the user never spends a checkpoint debugging a boot failure. Then present the 14 steps
and collect a PASS/FAIL note for each.

If the user reports a failure that is a small, unambiguous defect (a mislabelled
control, a wrong destination, a missing fallback), fix it in place, commit it, and ask
them to re-run just that step — this is exactly how 06-08 and 07-07 handled the real
bugs they surfaced. Anything larger goes to the deferred list in Task 3 rather than
being patched under a checkpoint.
  </action>
  <what-built>
The full Phase 8 customization path: four presets in the registry, a preset picker page
at `/interview` reached from the Practice Interviews tile, a collapsed-by-default
Customize panel (industry, role, difficulty, session length, personality dial, optional
pasted-profile persona), a server-side validating resolver at both prompt-assembly call
sites, the customization persisted on the report row and used by the evaluator, and a
customization strip on the report.
  </what-built>
  <how-to-verify>
Run `npm run dev` with the local dev DB inline
(`DATABASE_URL="postgresql://ajabreu79@localhost:5432/leadership_avatar_dev" npm run dev`)
and log in as a real seeded student. Work through these 14 steps and report PASS/FAIL
with a note for each:

1. From `/`, click the Practice Interviews tile — it lands on `/interview`, not
   `/interview/general`.
2. Four preset cards render, "General Interview" first. Each shows a one-line
   description, a difficulty, a "~N min" length and its question areas — and the four
   are clearly distinguishable from each other.
3. FAST PATH: select "General Interview" and press Start WITHOUT opening Customize. The
   wizard opens at `/interview/general`, shows exactly two steps (Interviewer, Resume),
   and the hero reads 20 min.
4. Go back to `/interview`, select a preset, open Customize. Controls are pre-filled
   with THAT preset's own defaults, and industry/role are dropdowns with no free-text
   entry anywhere.
5. Switch the selected preset with the panel open — the controls reset to the new
   preset's defaults.
6. Set difficulty to Advanced, length to Quick, industry to Healthcare, role to
   Team lead. The read-only summary reflects the changes.
7. Paste a few paragraphs describing an interviewer (write it yourself — do not paste a
   real person's profile) and press "Build persona". A short persona comes back, the
   personality dial visibly disables, and rehearsal-framing copy is present near the
   paste box. Confirm there is NO URL/LinkedIn field anywhere.
8. Press Start. The wizard opens at that preset's slug, the hero shows 10 min, and there
   is still no third wizard step and no way to edit customization from here.
9. Refresh the wizard page. It falls back to the preset's own defaults (proving the
   handoff key was read once and cleared) — note what you see.
10. Start a REAL avatar session on the customized run. The header reads "of ~5
    questions". Answer 4-5 questions. The interviewer's manner should be recognizably
    the pasted persona and noticeably harder than a Beginner run.
11. With DevTools Network open, compare the `customization` object in three consecutive
    `/api/interaction/chat` request bodies — identical in all three.
12. Press End. The report resolves to READY and the report page shows a customization
    strip reading the preset name, Healthcare, Team lead, Advanced, and ~10 min / 5
    questions — and does NOT print the persona text (a "custom interviewer persona"
    chip is fine).
13. Open an OLD report from `/reports` (one created before Phase 8). It still loads and
    shows the quiet "customization wasn't recorded" line rather than blanks or an error.
14. REGRESSION: run one short uncustomized `general` session end to end. It behaves
    exactly as it did before this phase — same stage progression, same "of ~9 questions"
    readout, working report.
  </how-to-verify>
  <resume-signal>Type "approved" with your per-step PASS/FAIL notes, or describe the failures.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record results and close the phase</name>
  <files>.planning/phases/08-interview-customization/08-08-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
Write `08-08-SUMMARY.md` with: the resolved baseline SHA, all 17 static checks verbatim,
the 14 walkthrough steps with the user's verbatim PASS/FAIL notes, every bug found and
fixed during the walkthrough (with commit SHAs — 06-08 and 07-07 each found real ones at
this step), and a deferred-items list separating genuinely pre-existing issues
(`/about` build breakage, repo-wide broken eslint, the 307-vs-401 middleware behavior on
unauthenticated API calls) from new Phase 8 items the user should decide on.

Then update `.planning/STATE.md` (Progress entries for 08-01 … 08-08, a "Phase 8 Status:
COMPLETE" section, any new Decisions worth carrying forward — notably: length presets
are an enum not a free number; the personality dial is ignored when a pasted persona is
present; customization does not persist between sessions; the raw pasted profile text is
never stored), tick REQ-17 … REQ-24 in `.planning/REQUIREMENTS.md`, and mark Phase 8's
checkbox in `.planning/ROADMAP.md`.

Include the migration handoff note: `*add_interview_customization` was applied to the
LOCAL dev DB only; the team picks it up via the committed SQL exactly as Phase 6 did.

`.planning/` is gitignored — these are disk-only updates, not commits.
  </action>
  <verify>`08-08-SUMMARY.md` exists with all 17 checks and all 14 steps recorded; REQ-17..REQ-24 all ticked in REQUIREMENTS.md; STATE.md shows Phase 8 COMPLETE.</verify>
  <done>Phase 8 is signed off with a written record of what passed, what was fixed, and what was deferred.</done>
</task>

</tasks>

<verification>
- All 17 static checks recorded.
- All 14 walkthrough steps human-confirmed.
- `npx tsc --noEmit` clean as the final state of the branch.
</verification>

<success_criteria>
All five amended ROADMAP Phase 8 success criteria are demonstrated against a real
session: a preset selected without typing a URL; industry, role, difficulty and length
adjusted before starting; an interviewer personality set, optionally from a pasted
description; a byte-identical system prompt across turns; and a report that states the
customization that produced it.
</success_criteria>

<output>
After completion, create `.planning/phases/08-interview-customization/08-08-SUMMARY.md`
</output>
