---
phase: 08-interview-customization
plan: 07
type: execute
wave: 3
depends_on: ["08-02", "08-04"]
files_modified:
  - app/interview/[type]/report/[reportId]/page.tsx
  - components/interview/ReportCustomizationStrip.tsx
autonomous: true
requirements: [REQ-24]

must_haves:
  truths:
    - "A report shows which preset, industry, role, difficulty and length produced it"
    - "Two reports from the same preset with different settings are visibly distinguishable"
    - "A pre-Phase-8 report with no stored customization still renders without empty or broken fields"
    - "The report never exposes the student's raw pasted profile text"
  artifacts:
    - path: "components/interview/ReportCustomizationStrip.tsx"
      provides: "Summary strip rendering the customization the session ran with"
      min_lines: 30
    - path: "app/interview/[type]/report/[reportId]/page.tsx"
      provides: "Customization strip wired into the report header area"
      contains: "ReportCustomizationStrip"
  key_links:
    - from: "app/interview/[type]/report/[reportId]/page.tsx"
      to: "InterviewReportDTO.customization"
      via: "strip props read from the fetched DTO"
      pattern: "customization"
---

<objective>
Show, on the report, the customization that produced it.

Purpose: REQ-24 / success criterion 5. Once presets are tunable, `typeSlug` no longer
identifies a session — two students who both picked "General Interview" can have had
very different interviews. The report has to say which one this was, so a student can
compare two reports and understand why one felt harder.

Output: a summary strip component plus its wiring into the existing report page.
</objective>

<execution_context>
@/Users/ajabreu79/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ajabreu79/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-interview-customization/08-02-SUMMARY.md
@app/interview/[type]/report/[reportId]/page.tsx
@lib/interview/report-dto.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the customization summary strip</name>
  <files>components/interview/ReportCustomizationStrip.tsx</files>
  <action>
New client component taking the DTO's `typeSlug` and `customization` block and
rendering a compact labelled strip above the score cards: Preset, Industry, Role,
Difficulty, Length.

- Preset: resolve `typeSlug` through `getInterviewType(typeSlug)?.label`, falling back
  to the raw slug if the preset was later removed from the registry — a historical
  report must never render a blank or crash on an unknown slug.
- Length: render as `~{targetMinutes} min · {targetQuestionCount} questions`, and omit
  the clause for whichever of the two is null.
- A null field renders as an explicit em-dash or "Not recorded" rather than an empty
  cell. Pre-Phase-8 rows have all six columns null and must still produce a readable
  strip; if EVERY customization field is null, render the whole strip as a single quiet
  "Customization wasn't recorded for this session" line instead of five empty rows.

DO NOT render `customization.interviewerPersona` on this strip. The persona can be the
distilled description of a real named person; per 08-CONTEXT.md's retention constraint
it is a third party's derived personal information, and the report is a shareable-
feeling artifact. Show at most a neutral marker that a custom interviewer persona was
used (a chip reading "Custom interviewer persona"), not its text. Record this decision
in a comment on the component.

Match the report page's existing visual language — reuse its palette, HeroUI `Chip`
usage and card border treatment so the strip reads as part of the page, not bolted on.
  </action>
  <verify>
- `npx tsc --noEmit` clean.
- `grep -n "interviewerPersona" components/interview/ReportCustomizationStrip.tsx` shows no render of the string's value, only a presence check.
- Component renders without error when handed an all-null customization block (exercise via the manual check in Task 2).
  </verify>
  <done>The strip renders five labelled values, degrades cleanly to "not recorded" for nulls, and never prints the persona text.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the strip into the report page and verify both row shapes</name>
  <files>app/interview/[type]/report/[reportId]/page.tsx</files>
  <action>
Render `<ReportCustomizationStrip />` inside the report body, between the `ReportShell`
header (which already shows `interviewerName` and `completedAt`) and `ReportScoreCards`,
passing `report.typeSlug` and `report.customization` from the fetched DTO.

Show the strip in the READY and FAILED content branches. Do NOT show it in the pending/
polling branch or the 404 shell — there is no DTO to read in the 404 case, and the
pending branch should stay a clean waiting state.

Do not change the polling logic, the owner-only 404 handling, the
`report.typeSlug !== params.type` redirect, the score-card "Not yet measured" vs
"Not scored" distinction, or the back-navigation controls — all were validated in 06-07,
06-08 and 07-07 and are out of scope here.
  </action>
  <verify>
- `npx tsc --noEmit` clean.
- `git diff "app/interview/[type]/report/[reportId]/page.tsx"` shows only the import and one render site — no change to polling, 404 or score-card logic.
- Against the local dev DB with a real logged-in user: view two reports — one whose row has customization columns populated with distinct values (e.g. healthcare / Advanced / 10 min), one whose columns are all null (simulate a pre-Phase-8 row by nulling them with `psql`). Confirm the first renders all five values and the second renders the single "not recorded" line, and that neither page shows persona text or any resume content. Clean up test data afterwards.
  </verify>
  <done>A report visibly states the customization it ran with; an uncustomized/legacy report degrades to one quiet line; nothing private leaks.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- `grep -rn "resumeText\|transcriptKey" app/interview/\[type\]/report/ components/interview/ReportCustomizationStrip.tsx` returns nothing.
- `git diff --name-only` lists only the two files in `files_modified`.
</verification>

<success_criteria>
Two reports from the same preset with different settings are visibly distinguishable on
the report page, and legacy rows still render.
</success_criteria>

<output>
After completion, create `.planning/phases/08-interview-customization/08-07-SUMMARY.md`
</output>
